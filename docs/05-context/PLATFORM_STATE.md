# Estado da plataforma

> **Project Atlas — Estado vivo detalhado da plataforma**
>
> Movido do `CLAUDE.md` raiz em 2026-07-20 para reduzir o contexto carregado em toda sessão.
> Mantido pela skill `doc-sync` no fechamento de cada SPEC. O `CLAUDE.md` raiz guarda só o resumo.

**Estado em julho/2026: interface executável (SPEC-0003) + acesso a modelos de IA (SPEC-0004) + primeira resposta cognitiva ponta a ponta (SPEC-0005) + conversa interativa multi-turno (SPEC-0006) + Context Service (SPEC-0007) + Persona Service (SPEC-0008) + Memory Service (SPEC-0009) + espinha de execução Planner/Runtime/Tools (SPEC-0010) + Permission Service e primeiras Tools de leitura de sistema de arquivos (SPEC-0011) + primeira Tool de escrita (`write_file`) sob política `writeRoots` opt-in (SPEC-0012) + fluxo interativo `confirm` real + Tools destrutivas `delete_file`/`mkdir`/`append_file` (SPEC-0013) + Tools e `confirm` também em `atlas chat` (SPEC-0014, `respond` passou a orquestrar Planejamento + Execução como `ask`) + endurecimento de symlink no Permission Service via `realpath` (SPEC-0015, `evaluate` segue síncrono) + primeira infraestrutura de desenvolvimento: repositório publicado num remote privado no GitHub (`nahoL13/Atlas`) + CI em `.github/workflows/ci.yml` validando lint/typecheck/test/format:check a cada push/PR (SPEC-0016; runner `ubuntu-latest`/Node 24/pnpm via `pnpm/action-setup`, **não** corepack; sem tocar nenhum módulo de produto) + fecho atômico da janela TOCTOU do componente final em `read_file`/`write_file`/`append_file` (SPEC-0017, consumindo o [ADR-0014](docs/06-adr/ADR-0014-toctou-atomic-enforcement.md): as portas de IO abrem o fd com `O_NOFOLLOW`, ancoram na identidade do fd (`fstat` × `stat(realpath)`) e aplicam sobre o fd um veredicto de contenção do Permission Service — método novo `isContained(canonicalPath, access)`, puro/síncrono; `evaluate` segue intacto como pré-check e o Runtime inalterado) + múltiplas raízes de leitura/escrita na borda da CLI (SPEC-0018: `--allow-read`/`--allow-write` viraram flags repetíveis e `ATLAS_ALLOW_READ`/`ATLAS_ALLOW_WRITE` aceitam lista por `path.delimiter`; o núcleo já avaliava listas — a fatia foi só `apps/cli/src/gateway/input-gateway.ts`, zero linha em packages) + Observação (Etapa 5 do ciclo cognitivo) fechada como laço de replanejamento determinístico no Cognitive Core (SPEC-0019, consumindo o [ADR-0015](docs/06-adr/ADR-0015-observation-replan-loop.md)): o `runPlanCycle` — compartilhado por `ask`/`respond` — deixou de ser passe único e virou laço com teto fixo (**1 replanejamento**); a função pura `observe(executionResult)` decide `replan` (há passo `ok:false` **sem** `denialKind` = falha de Tool) × `complete` (bloqueio de permissão e recusa no `confirm` são terminais), consumindo o campo novo `ExecutedStep.denialKind?: 'blocked' | 'declined'` (única mudança em `@atlas/contracts`, rotulado pelo Runtime nos 2 branches de negação; ferramenta desconhecida e TOCTOU-no-uso ficam **sem** `denialKind` → replan); `steps` acumulam todos os passes; falha de Tool replaneja com resumo compacto das falhas; Planner/Permissões/Tools/Model Gateway intactos + Aprendizado (Etapa 6 do ciclo cognitivo) fechado como extração pós-turno (SPEC-0020, consumindo o [ADR-0016](docs/06-adr/ADR-0016-learning-proposed-extraction.md)) — **o Cognitive Lifecycle está implementado nas sete etapas**: um `learner` **puro** novo em `@atlas/cognitive` (`learner.ts`, molde do Planner/Observer: `instruction()` restrita ao Artigo 13 — só fatos afirmados/fortemente implicados pelo usuário, nunca inferidos — + `parse(saída) → readonly string[]`, teto fixo = 3 por turno, tipo interno) decide o que preservar; após a resposta final de cada turno (com e sem plano, em `ask` **e** `respond`), o `runPlanCycle` faz +1 `generate()` dedicada de extração (falha → nada aprendido, o turno nunca quebra); o Cognitive **propõe** os candidatos como dado (`AskResult.learned?`/`ConversationTurn.learned?`, aditivos) e **a borda grava e avisa**: `atlas ask`/`atlas chat` gravam via `atlas.memory.remember(texto, 'learned')` e imprimem `💡 lembrado: <fato>`; proveniência `Fact.source?: 'user' | 'learned'` (aditivo, `remember` ganha parâmetro opcional com default `'user'`), `atlas memory list` exibe a origem; Cognitive segue puro/sem estado, Memory mantém autoridade exclusiva de gravação; Planner/Observer/Runtime/Permissões/Tools/Model Gateway intactos + recomposição ao vivo do `memoryPrompt` no Cognitive Core (SPEC-0021, consumindo o [ADR-0011](docs/06-adr/ADR-0011-memory-service-persistence.md) — notas de atualização nos ADR-0008/0011/0016, sem ADR novo): `CognitiveCoreDeps.memoryPrompt` deixou de ser `string?` e passou a ser um **provider síncrono** interno — `() => string | undefined` (não sobe a `@atlas/contracts`) — **amostrado exatamente 1x por turno** no início de `ask` e de `respond`; o `systemPrompt` do turno (`compose(memoryValue) = [personaPrompt, memoryValue, TASK_FRAMING]`) é compartilhado por todas as `generate` do turno (planejamento, replanejamento, composição, extração); só a fatia de memória é recomposta (`personaPrompt`/`TASK_FRAMING` seguem estáticos). Em `respond`, o helper puro `withFreshSystemHead` **substitui** o conteúdo da primeira mensagem `system` da `Conversation` (ou **insere** uma no topo quando não houver) tanto nas mensagens enviadas ao modelo **quanto** na `Conversation` retornada (fonte única, sem prompt morto); demais mensagens `system` intactas; `respond` segue **puro** e o Core sem estado. A chamada de extração passa a **incluir os fatos já conhecidos** (`learner.instruction(knownFacts?)`) com instrução de **não os re-propor** — supressão da duplicação intra-sessão **por instrução ao modelo, sem garantia determinística** (não é dedup programático; Artigo 13 intacto). `@atlas/core` fia `memoryPrompt: () => memory.prompt()` (provider sempre definido; spread condicional removido, `exactOptionalPropertyTypes` satisfeito). **Fecha o laço de aprendizado dentro da própria sessão**: fato gravado no turno N (por `atlas remember` ou pela borda do Aprendizado) entra no `systemPrompt` do turno N+1, sem depender de nova invocação da CLI — resolvendo a limitação documentada da SPEC-0020. **Zero mudança em `@atlas/contracts` e em `apps/cli`** (diff vazio); deduplicação determinística segue fatia futura documentada. Planner/Observer/Runtime/Permissões/Tools/Model Gateway/Persona/Context/Memory intactos.** + deduplicação **determinística** dos fatos aprendidos, fechando essa mesma fatia futura (SPEC-0022, consumindo o [ADR-0011](docs/06-adr/ADR-0011-memory-service-persistence.md) por nota de atualização, sem ADR novo): `MemoryService.remember` normaliza o texto por um helper puro interno `normalize(text)` (`trim` → `toLowerCase` → colapso de `\s+`, sem IO, não sobe a `@atlas/contracts`) e, ao reconhecer duplicata (mesma normalização de um fato já carregado), faz um **no-op idempotente** — não cria `Fact`, não chama `storage.save`, não promove `source` — devolvendo o `Fact` existente; vale para todo chamador (`'user'` e `'learned'`). A assinatura de `remember(text, source?)` muda (**não-aditiva**) de `Promise<Fact>` para `Promise<{ fact: Fact; created: boolean }>`, sinalizando criado × existente; `ask.ts`/`chat.ts` só chamam `renderLearned` (`💡 lembrado:`) quando `created`, e `atlas remember` distingue "Lembrado" de "Já conhecido". A garantia é **determinística só para escritas novas** — o acervo legado (duplicatas já gravadas) permanecia intacto até a SPEC-0023 (abaixo). `Fact`/`forget`/`list`/`prompt` inalterados por esta fatia; `@atlas/cognitive`, `@atlas/runtime`, `@atlas/permissions`, `@atlas/tools`, `@atlas/model-gateway`, `@atlas/persona`, `@atlas/context` intactos + **consolidação determinística do acervo legado de fatos** (SPEC-0023, `atlas memory dedupe`, fechando o residual explícito que a SPEC-0022 deixou nomeado — nota de atualização no [ADR-0011](docs/06-adr/ADR-0011-memory-service-persistence.md), sem ADR novo): `MemoryService` ganha o método **aditivo** `dedupe(options?: { apply?: boolean }): Promise<DedupeReport>` (`@atlas/contracts` exporta `DedupeReport { applied; groups }`/`DedupeGroup { survivor; duplicates }`), reusando o mesmo `normalize(text)` interno da SPEC-0022 para agrupar os fatos carregados. Por default é **dry-run** — computa e relata os grupos com duplicata sem mutar `facts` nem chamar `storage.save`; `dedupe({ apply: true })` remove as duplicatas em memória (preservando ordem de carga) e persiste por write-through (`storage.save`, 1x). Sobrevivente de cada grupo = o de menor `createdAt` (comparação lexicográfica de string ISO, `createdAt` sempre gerado via `toISOString()`), empate desempatado pela ordem de carga; preservado intacto (`id`/`text`/`createdAt`/`source`), sem merge de campos, sem promover `source`, sem reescrever `text`. Acervo sem duplicatas é no-op determinístico (`groups` vazio, `applied: false`, sem `save` mesmo com `apply: true`). CLI: novo subcomando `atlas memory dedupe [--apply]` (`apps/cli/src/commands/memory.ts`, `runMemoryDedupe`; parsing em `input-gateway.ts`; despacho e `HELP_TEXT` em `run.ts`) — só invoca `atlas.memory.dedupe` e renderiza o `DedupeReport` (dry-run: sobreviventes + duplicatas que **seriam** removidas + dica `--apply`; aplicado: confirma o que **foi** removido; sem grupos: "Nenhuma duplicata encontrada"). `remember`/`forget`/`list`/`prompt` inalterados. **Achado do gate (F1, corrigido no `architecture-reviewer`):** adicionar um método a `MemoryService` — contrato **já público** — não é "promoção de tipo" no sentido do ADR-0007; o fundamento para subir a operação ao contrato é o Artigo 11 (autoridade exclusiva da Memory) + Artigo 5 (a CLI não acessa `normalize`/`storage` internos). O membro aditivo à interface de contrato ainda quebrou o **typecheck** dos fakes de `MemoryService` tipados diretamente (só `apps/cli/tests/status.test.ts`); os fakes com `as unknown as` em `ask.test.ts`/`chat.test.ts` seguiram escapando (só `pnpm test` os pega) — 3ª+ confirmação do padrão recorrente. `@atlas/cognitive`, `@atlas/runtime`, `@atlas/permissions`, `@atlas/tools`, `@atlas/model-gateway`, `@atlas/persona`, `@atlas/context` intactos (diff de produção vazio). Roadmap 1.3/l. 93: candidato "consolidação do acervo legado no storage" marcado **entregue**. Além de `@atlas/contracts` e `@atlas/core` (`createAtlas()` sobe até `ready`, expõe config validada/congelada, compõe o Model Gateway + Tool Registry + Permission Service + Runtime + Cognitive Core + Context Service + Persona Service + Memory Service e desliga com segurança), existe `apps/cli` (`@atlas/cli`): `atlas status` sobe a plataforma pelo terminal, mostra estado e config resolvida (precedência `flags > env > defaults`, incluindo `readRoots`) e desliga com segurança; `atlas ask "<objetivo>"` envia um objetivo ao núcleo cognitivo e imprime a resposta (default provider `local`/Ollama; `remote`/`fake` por config); `atlas chat` abre uma conversa interativa multi-turno (mesmos provedores; histórico lembrado durante a sessão via `@atlas/context`, ADR-0008/0009; desde a SPEC-0014, cada turno também planeja e executa Tools — incluindo `delete_file` com o fluxo `confirm` inline, apoiado no `LineReader` da própria sessão, injetado via `CreateAtlasDeps.confirm`, sem abrir um segundo `readline`; o traço de `steps` de cada turno é impresso reusando a renderização do `ask`); `atlas --help`/`--version` também existem. Execução do fonte via `tsx` (ADR-0005), sem `dist/`. Existe também `@atlas/model-gateway`: `createModelGateway(config)` expõe `generate()` (geração única) com provedor selecionável por config — `fake` (testes), `local`/Ollama (grátis) e `remote` (pago, OpenAI-compatible); provedores de rede recebem `fetch` por parâmetro (testados sem rede) e há um smoke script para verificação real. Seu primeiro consumidor é `@atlas/cognitive`: `createCognitiveCore({ gateway, runtime, personaPrompt?, memoryPrompt? })` → `ask(objetivo)` **orquestra** Planejamento + Execução (ADR-0012, deixou de ser tiro único): 1ª chamada `generate` (system prompt composto — `personaPrompt` (identidade) + `memoryPrompt` (fatos) + `TASK_FRAMING`, na ordem identidade → memória → tarefa, ADR-0010/0011 — mais a instrução do Planner com o catálogo de Tools) → o **Planner** puro (consolidado no package: `createPlanner()` com `instruction`/`parse`, sem gateway) parseia a saída num `Plan` ou `null`; sem plano, responde direto em 1 chamada; com plano, `runtime.execute` roda as Tools e uma 2ª chamada compõe a resposta final. `ask` retorna `AskResult { text; steps? }`. `startConversation()`/`respond(conversation, input)` sustentam conversa multi-turno como dado (ADR-0008) e, desde a SPEC-0014, `respond` orquestra Planejamento + Execução no mesmo padrão de `ask` (instrução do Planner na 1ª chamada; sem plano, resposta direta; com plano, `runtime.execute` + 2ª chamada de composição), permanecendo função pura e o Core sem estado; `ConversationTurn` ganha `steps?: readonly ExecutedStep[]`, e quando Tools rodam a `Conversation` retornada ganha uma mensagem `system` compacta de resumo (nunca exibida ao usuário), para que o modelo lembre do que executou nos turnos seguintes. Existe `@atlas/tools` (`createToolRegistry` + Tools puras `clock`/`calc`, sem `requirements`, `calc` sem `eval`; as primeiras Tools com **IO de leitura** — `read_file`/`list_dir` — que declaram `requirements(args) → ActionRequest` (`access: 'read'`) e leem o disco por uma porta injetável `FsReadPort`, default `nodeFsReadPort()`; a Tool de **escrita** — `write_file` (`{ path, content }`, cria/sobrescreve) — que declara `requirements` com `access: 'write'`; e as três Tools novas da SPEC-0013 — `delete_file` (`{ path }`, remove um arquivo, `access: 'delete'`, a única ação irreversível), `mkdir` (`{ path }`, cria diretório recursivamente/idempotente) e `append_file` (`{ path, content }`, acrescenta conteúdo, cria se ausente; ambas `access: 'write'`) — todas sobre a porta injetável `FsWritePort` (`writeFile`/`deleteFile`/`mkdir`/`appendFile`), default `nodeFsWritePort()`; desde a SPEC-0017, `readFile`/`writeFile`/`appendFile` executam internamente o fecho atômico de TOCTOU — `open(O_NOFOLLOW)` → ancoragem de identidade do fd (`fstat`×`stat(realpath)`) → predicado estreito injetado `Verify = (realpath, access) => boolean` (tipo interno, não sobe a `@atlas/contracts`) → opera sobre o fd e fecha; `nodeFsReadPort`/`nodeFsWritePort` recebem `{ verify?, primitives? }` (`verify` default permissivo, `@atlas/core` fia `permissions.isContained`; `primitives` interno para fakes nos testes); `readdir`/`deleteFile`/`mkdir` **não mudam**) e `@atlas/runtime` (`createRuntime({ registry, permissions, confirm })` → `execute(plan)` sequencial com falhas estruturadas que nunca derrubam a execução + `tools()`; por passo com `requirements`, consulta `permissions.evaluate` antes de rodar a Tool — veredicto `allowed` executa; veredicto `confirm` **pausa e aguarda** `confirm.request(requirement)` (`ConfirmPort`, real via `nodeReadlineConfirmPort()` sobre `node:readline`, nunca trava em EOF/não-TTY) — aprovado executa a Tool, recusado vira `ExecutedStep` negado com motivo distinto de bloqueio; qualquer outro veredicto vira `ExecutedStep` negado e a Tool não roda); o modelo produz o plano em JSON (Planner-driven) e o Runtime o executa deterministicamente, com o Model Gateway **intacto**. `atlas ask` mostra um traço compacto das Tools executadas (incluindo passos bloqueados, com o motivo). O contrato do Model Gateway e o `CognitiveCore` vivem em `@atlas/contracts` (ADR-0007). Existe também `@atlas/context` (`createContextService()` → store de sessão em memória que guarda a `Conversation` por sessão; o `atlas chat` usa-o como detentor, ADR-0008/0009; `respond` segue puro). Existe também `@atlas/persona` (`createPersonaService()` → registro de Personas `jarvis`/`neutral`; a identidade da Persona ativa é injetada na geração do Cognitive, ADR-0010; `atlas ask`/`atlas chat` respondem na voz da Persona; `atlas status` a exibe, `atlas chat` a saúda; seleção por `--persona`/`ATLAS_PERSONA`, default `jarvis`). Existe também `@atlas/memory` (`createMemoryService({ storage })` → autoridade de conhecimento persistente; fatos/preferências explícitos guardados atrás de uma porta de storage injetável, adapter JSON default; `memory.prompt()` é injetado na geração do Cognitive, ADR-0011; `atlas remember "<fato>"`/`atlas forget <id>`/`atlas memory list` gerenciam; caminho por `--memory-path`/`ATLAS_MEMORY_PATH`, default `~/.atlas/memory.json`; Memória persistente × Contexto temporário seguem distintos; **desde a SPEC-0022**, `remember` normaliza o texto (`trim`/`toLowerCase`/colapso de `\s+`) e faz no-op idempotente em duplicata — `Promise<{ fact, created }>` sinaliza criado × existente; **desde a SPEC-0023**, `dedupe(options?: { apply? })` consolida o acervo já persistido — dry-run por default, `--apply` remove duplicatas e persiste, sobrevivente = mais antigo por `createdAt`, `atlas memory dedupe [--apply]` na CLI). Existe também `@atlas/permissions` (`createPermissionService({ readRoots, writeRoots, pathResolver? })` → `evaluate(action)` **puro e síncrono** do ponto de vista de decisão; roteia por `access` — `read` contra `readRoots`, `write`/`delete` contra `writeRoots` — julgando por **contenção lexical** (dentro da raiz → `allowed` para `write`, `confirm` para `delete`; fora/vazio → `blocked` com motivo) sobre caminhos **resolvidos por `realpath`** desde a SPEC-0015 (fecha o escape por symlink dentro de uma raiz permitida, limitação conhecida desde o ADR-0013): nova porta injetável síncrona interna `PathResolverPort` (`realpathSync`, default `nodePathResolverPort()` sobre `node:fs`, sem 2º consumidor → não sobe a `@atlas/contracts`); `readRoots`/`writeRoots` resolvidas uma única vez na criação e o alvo a cada `evaluate`, ambos pelo algoritmo do ancestral existente mais profundo (sobe aos pais até achar um segmento existente, recompõe por cima); fail-closed em qualquer erro de resolução ≠ `ENOENT` (nunca lança — no alvo vira `blocked`, numa raiz na criação só aquela raiz fica fail-closed); `evaluate` segue síncrono, Runtime inalterado. Grants de leitura e escrita **independentes**; `confirm` real desde a SPEC-0013 — quem aplica o veredicto (pausar e perguntar) é o Runtime, nunca o Permission Service, que segue puro; ADR-0013; desde a SPEC-0017/ADR-0014, `PermissionService` ganha `isContained(canonicalPath, access): boolean` — método novo, puro/síncrono, sem IO, sem re-resolver caminho, reusando `within()` sobre as raízes já resolvidas — julgado sobre um caminho **já canônico** (o `realpath` que uma porta de IO ancorou num fd), fiado por `@atlas/core` como o `verify` de `@atlas/tools`; é a segunda barreira do fecho atômico de TOCTOU, aplicada no instante do uso, sem duplicar a autoridade de contenção). `config.permissions.readRoots` (default `[cwd]`, override `--allow-read`/`ATLAS_ALLOW_READ`) e `config.permissions.writeRoots` (default `[]` — não escreve sem opt-in explícito, override `--allow-write`/`ATLAS_ALLOW_WRITE`), precedência `flags > env > defaults`, definem as políticas de leitura e escrita. Desde a SPEC-0018 **ambos aceitam múltiplas raízes**: a flag é repetível (`--allow-read A --allow-read B`) e a env é lista por `path.delimiter` (`ATLAS_ALLOW_READ=A:B`); a lista da fonte de maior precedência substitui a inferior por inteiro (sem merge), segmentos vazios são filtrados e fonte que colapsa para vazio cai no default. **O default do predicado `verify` das portas de FS agora é fail-closed** (SPEC-0024, ramo micro da Emenda v1.2, fechando o residual explícito da lição da SPEC-0017): em `packages/tools/src/fs-port.ts`, `nodeFsReadPort`/`nodeFsWritePort` trocaram `deps.verify ?? (() => true)` por `deps.verify ?? (() => false)` — uma porta construída **sem** `verify` injetado passa a **recusar** `readFile`/`writeFile`/`appendFile` no instante do uso, em vez de permitir por omissão; `readdir`/`deleteFile`/`mkdir` não passam pelo fecho atômico e seguem inalterados. Comportamento em produção **idêntico** ao de hoje — `createAtlas` sempre fiou `verify = permissions.isContained.bind(permissions)` desde a SPEC-0017, então o default nunca é consultado nesse caminho; o efeito é só sobre portas cruas/futuras. Sem mudança de assinatura (`Verify`/`NodeFsPortDeps` intactos, `verify` segue opcional); diff de produção confinado a `packages/tools/src/fs-port.ts`; `@atlas/core`/`@atlas/contracts`/`@atlas/permissions`/`@atlas/runtime`/`apps/cli` intactos + **o conceito de Skill passa a existir na implementação, não só documentado** (SPEC-0025, consumindo o [ADR-0017](docs/06-adr/ADR-0017-skills-registry-builder.md) — Registry passivo + Builder, decisão humana da Emenda v1.1): novo package `packages/skills` (`@atlas/skills`), com os dois componentes de Extensão reservados desde sempre pelo Module Catalog. **Skill Registry** — `createSkillRegistry({ skills? })` → catálogo **em memória**, no molde do Tool Registry (SPEC-0010): `register`/`get`/`deactivate`/`remove`/`list`; distingue `scope: 'permanent' | 'temporary'` e ativo/inativo; `register` faz upsert por `id` mas **rejeita** (`SkillError`, `ATLAS_SKILL`) qualquer registro que envolva uma Skill `permanent` já registrada por aquele `id` — uma `temporary` nunca sobrescreve nem é sobrescrita numa colisão `permanent`; sem persistência, o Registry vive só no processo. **Skill Builder** — `createSkillBuilder({ gateway, registry, tools })` → `build({ capability })` percorre os 8 passos do Module Catalog: **uma única** `gateway.generate` (passo 5) produz um `SkillDraft` estruturado em JSON (`name`/`description`/`instructions`/`toolIds`, todos derivados do modelo), parseado no molde do learner (ADR-0016) — JSON malformado/incompleto vira `issues`, nunca lança; validação estática (passos 6-7) checa contrato bem-formado (campos presentes, `name`/`instructions` não vazios após `trim`, `toolIds` array de strings) **e** que todo `toolId` resolve no `ToolRegistry` injetado; em sucesso (passo 8), completa **deterministicamente** `id` (namespace `tmp-…` via `crypto.randomUUID()`), `version` (`'1.0.0'`) e `scope: 'temporary'` — nunca do modelo, mesmo que o JSON tente injetá-los — e registra; em falha, devolve `{ ok: false, issues }` sem registrar. **Nunca** promove `temporary`→`permanent`. `BUILTIN_SKILLS` semeia o Registry com ao menos uma Skill `permanent` (dado inerte, ids fora do namespace `tmp-`). Contratos novos em `@atlas/contracts` (`skill.ts`): `Skill`/`SkillScope`/`SkillDescriptor`/`SkillDraft`/`SkillRegistry`/`SkillBuilder`/`SkillBuildRequest`/`SkillBuildResult`, todos aditivos; `AtlasPlatform` ganha `readonly skills: SkillRegistry` e `readonly skillBuilder: SkillBuilder`. `@atlas/core` cria/semeia o Registry e fia o Builder com `gateway`+`registry`+Tool Registry já compostos, expondo `atlas.skills`/`atlas.skillBuilder`. CLI: `atlas skills list` (catálogo com `id`/nome/`scope`/ativo-inativo/versão) e `atlas skills build "<capacidade>"` (dispara o Builder, imprime a Skill produzida ou as pendências de validação); `HELP_TEXT` atualizado. **Nada no laço cognitivo consome Skills nesta fatia** — Planner, Cognitive Core, Runtime e Observer têm diff de produção vazio (decisão humana registrada no ADR-0017); sem seleção pelo usuário (sem flag/config de Skill ativa); sem persistência em disco (uma Skill construída por `atlas skills build` não sobrevive à invocação); sem execução real da Skill no "teste controlado" (validação é só estática); Skill sem canal com o usuário (Module Catalog, regra 4). Fecha o gate 1.4 do Roadmap ("Skills existem como conceito implementado, não só documentado"), item 1.4 marcado **parcialmente entregue** (consumo de Skills no laço cognitivo segue candidato futuro) + **o laço cognitivo passa a selecionar e aplicar Skills automaticamente** (SPEC-0026, consumindo o [ADR-0018](docs/06-adr/ADR-0018-planner-skill-consumption.md) — decisão humana, forma A: seleção pelo Planner/modelo, não pelo usuário — que ativa o consumo e ganha nota de atualização no [ADR-0017](docs/06-adr/ADR-0017-skills-registry-builder.md)): `Plan.skillId?: string` (`@atlas/contracts`, aditivo, molde do `denialKind?` da SPEC-0019) registra a Skill selecionada; `planner.instruction(tools, skills?)` passou a listar também o catálogo de Skills **ativas** por `id`/`name`/`description` (sem `instructions`), com framing pedindo **no máximo uma** Skill por `id` — `skills` vazio/ausente preserva a string de antes; `planner.parse` lê `skillId` opcional ao lado de `steps`. `CognitiveCoreDeps` ganha a porta **opcional** `skillCatalog?: SkillCatalogPort` (`{ list(): readonly SkillDescriptor[]; get(id): Skill | undefined }`, tipo interno a `@atlas/cognitive`, não sobe a `@atlas/contracts` — mesmo molde do provider `memoryPrompt` da SPEC-0021, menor privilégio: o Cognitive só lê, nunca `register`/`deactivate`/`remove`); amostrada (`list()`) 1x por turno em `ask` **e** `respond` e passada a `planner.instruction`. O `runPlanCycle` rastreia o `skillId` do **plano em vigor** (`activeSkillId`, reatribuído a cada replanejamento) e, só na `generate()` de **composição**, resolve-o contra o catálogo (`get`) — resolve → as `instructions` da Skill entram no `systemPrompt` na ordem Persona → Memory → **Skill** → `TASK_FRAMING`; não resolve/ausente → turno segue idêntico ao comportamento anterior, sem lançar. Em `respond`, a injeção usa `withFreshSystemHead` (SPEC-0021) só na mensagem enviada à chamada de composição — a `Conversation` retornada **nunca** carrega a Skill, preservando `respond` puro e a invisibilidade da Skill (Constituição, Artigos 2 e 9); `AskResult`/`ConversationTurn` não ganham `skillId`. `toolIds` da Skill **não** filtram/priorizam Tools no Runtime nesta fatia — Runtime, Permission Service, Tools, Model Gateway e `apps/cli` **intactos** (diff de produção vazio). `@atlas/core` fia a projeção somente-leitura `{ list: () => skills.list(), get: (id) => skills.get(id) }` no `createCognitiveCore`. O consumo alcança só as Skills `permanent` semeadas (`BUILTIN_SKILLS`); Skills `temporary` construídas por `atlas skills build` seguem não-consumíveis entre invocações — persistência de Skills fica fora desta fatia (mesma fronteira adiada pelo ADR-0017). **O gate 1.4 do Roadmap fecha por inteiro.** Nenhum campo obrigatório novo em contrato consumido por fakes — `Plan.skillId?`/`CognitiveCoreDeps.skillCatalog?`/`instruction(tools, skills?)` são todos opcionais/aditivos, sem disparar o padrão recorrente de quebra de `typecheck` em fakes. Suíte: 423 testes. Verificação verde: `pnpm lint`/`format:check`/`typecheck`/`test` + **busca/recuperação determinística de fatos no Memory Service** (SPEC-0027, Perfil `completo`, nota de atualização no [ADR-0011](docs/06-adr/ADR-0011-memory-service-persistence.md), sem ADR novo — candidato independente de 1.3/l. 91 do Roadmap, **não** fecha o gate de 1.3 por inteiro, que segue exigindo mais de uma categoria de conhecimento): `MemoryService` ganha o método **aditivo** `search(query: string, options?: { readonly limit?: number }): readonly Fact[]` em `@atlas/contracts`, implementado em `packages/memory/src/memory-service.ts` — puro, síncrono, sem IO, opera só sobre os `facts` já carregados. Reusa o **mesmo** `normalize(text)` interno da SPEC-0022 (não duplicado) para tokenizar consulta e fato; ranking determinístico por overlap de tokens (`score = |tokens(query) ∩ tokens(fact)|`), exclui `score = 0`, ordena por `score` desc, empate desempatado pela ordem de carga (índice ascendente em `facts`, mesmo critério de desempate do `dedupe`/SPEC-0023). `options.limit` opcional corta os `N` primeiros já ordenados; ausente devolve todos os fatos com overlap; consulta vazia/sem tokens após `normalize` devolve `[]`; retorno `Fact[]` homogêneo com `list()`, **sem** expor scores (detalhe interno). CLI: novo subcomando `atlas memory search "<consulta>"` (`apps/cli/src/gateway/input-gateway.ts` reconhece o subcomando + captura a consulta; `apps/cli/src/commands/memory.ts` ganha `runMemorySearch`, só invoca e renderiza — id + texto, ou "Nenhum fato relevante encontrado."; despacho e `HELP_TEXT` em `apps/cli/src/run.ts`). **Não** altera `prompt()` nem o caminho de injeção de memória na geração — o `memoryPrompt` do Cognitive Core continua recebendo o dump de todos os fatos via `prompt()`; consumir `search` a partir da consulta do turno no Cognitive Core segue fatia futura explícita. `remember`/`forget`/`list`/`prompt`/`dedupe`/`Fact` inalterados. **Intactos** (diff de produção vazio): `@atlas/cognitive`, `@atlas/runtime`, `@atlas/permissions`, `@atlas/tools`, `@atlas/model-gateway`, `@atlas/persona`, `@atlas/context`, `@atlas/skills`, `@atlas/core`. **Padrão recorrente confirmado de novo (6ª+ ocorrência)**: membro novo obrigatório em `MemoryService` (interface de contrato já pública) quebrou o `typecheck` só do fake tipado diretamente (`apps/cli/tests/status.test.ts`), coerente com SPECs 0017/0019/0022/0023/0025/0026. Suíte: 437 testes (era 423). Verificação verde: `pnpm lint`/`format:check`/`typecheck`/`test` + **as primeiras Tools da especialidade declarada do Atlas — desenvolvimento de software** (SPEC-0028, Perfil `completo` — o `architecture-reviewer` vetou a 1ª versão da SPEC no gate `Draft → Ready`, 1 rodada de correção, sem escalação; item **1.4 (Capacidades de Plataforma)** do Roadmap, l. 100, `candidato · SPEC direta`, marcado **parcialmente entregue** — entrega a primeira das três fatias nomeadas nesse item; **não** fecha nenhum gate, o único gate de 1.4 (Skills) já fechado pela SPEC-0025): `@atlas/tools` ganha `createGitStatusTool({ git?, cwd? })`/`createGitDiffTool({ git?, cwd? })`/`createGitLogTool({ git?, cwd? })` — Tools **somente-leitura** que declaram `requirements(args) → { resource: { type: 'directory', path }, access: 'read' }` (o mesmo `AccessMode`/`ResourceType` já existentes, nenhuma mudança em `@atlas/contracts`) e chamam a porta interna `GitReadPort` (`status`/`diff`/`log`, sem 2º consumidor, molde de `FsReadPort`), adaptador default `nodeGitReadPort({ verify?, exec?, realpath? })` sobre `node:child_process` `execFile` (argv array, nunca `shell: true`). **O achado que definiu a fatia**: `git status`/`diff`/`log` **descobrem o repositório subindo pelos diretórios pais** — diferente de `list_dir`, que nunca lê acima do path declarado; um alvo em subdiretório de um repositório maior faria o git relatar (e, no `diff`, imprimir o conteúdo de) toda a árvore acima dele, inclusive do que `read_file` seria `blocked` de ler. Por isso a porta, antes de qualquer subcomando, **descobre o toplevel real** (`git --no-optional-locks -C <alvo> rev-parse --show-toplevel`), resolve o `realpath` e aplica `verify(toplevel, 'read')` — o mesmo predicado estreito `Verify` do ADR-0014, reusado de `fs-port.ts`, **fail-closed por default** (SPEC-0024); `@atlas/core` fia `verify: permissions.isContained.bind(permissions)`, a mesma fiação já usada para `nodeFsReadPort`/`nodeFsWritePort` — nenhuma config nova. Toplevel fora das `readRoots` → recusa estruturada com o toplevel descoberto na mensagem, e o subcomando **não roda** — asseverado por spy sobre o `exec` fake (exatamente 1 chamada, a de `rev-parse`). Todo argv começa com `--no-optional-locks` (inclusive no `rev-parse`), porque `git status` pode atualizar `.git/index` e tomar lock — escrita real sob veredicto `read` num repositório onde `writeRoots` é vazio por default; sem a flag o gate mentiria sobre o que a ação faz (Artigo 8). `execFile` com `maxBuffer` explícito de 10 MiB (estouro → erro estruturado, nunca opaco) e a saída capturada truncada em 64 KiB com marcador visível + `truncated: true` (vale para os três, `git_diff` inclusive — o pior caso de volume). `GitOutput.repository` (o toplevel real) entra no `output` de sucesso antes do texto do git, mesmo com `path` ausente (Artigo 7 — o alvo é o dado que o usuário não forneceu). `git_diff` aceita `staged?: boolean` (`--cached`); `git_log` aceita `maxCount?: number`, default **20**, valor inválido cai no default sem erro (allowlist bounded — é o que mantém esta fatia fora do território `ADR primeiro` de "execução de comandos" reservado no mesmo item do Roadmap). Saída em texto cru do git, sem parsing estruturado (molde de `read_file`). **Residuais conhecidos, documentados e não fechados** (mesmo critério do ADR-0014 para `list_dir`): sem fecho atômico de TOCTOU do diretório-alvo (quem lê é um subprocesso sobre um diretório, não há fd a ancorar); troca de ancestral; Windows fora de escopo; `~/.gitconfig` do usuário segue lido fora do portão; a mensagem de recusa expõe um toplevel **descoberto** (não um path que o chamador já forneceu), um vazamento pequeno com contrapartida de transparência — candidato natural à fatia futura de "leitura de estrutura de projeto" nomeada no mesmo item do Roadmap. **Diff vazio** em `@atlas/contracts`, `@atlas/permissions`, `@atlas/runtime` — nenhum `access` novo, nenhum contrato tocado, nenhum ADR aberto. Fora de escopo (explicitamente, mesmo item do Roadmap): mutação do repositório (`git_commit`/`git_checkout`/`git_push`/…), execução de comandos arbitrários (novo `access`, `ADR primeiro`), leitura de estrutura de projeto, contexto de ambiente lido pelo Planner/Cognitive (ADR-0009). Suíte: 477 testes (era 437). Verificação verde: `pnpm lint`/`format:check`/`typecheck`/`test` + **categorias de conhecimento no Memory Service — memória episódica e memória de projetos** (SPEC-0029, Perfil `completo`, nota de atualização no [ADR-0011](docs/06-adr/ADR-0011-memory-service-persistence.md), sem ADR novo; item **1.3 (Memory Service — Próximas Fatias)** do Roadmap, l. 90, `gate · SPEC direta` — **fecha por inteiro o último gate aberto da Fase 1**, com 1 veto do `architecture-reviewer` no gate, corrigido numa rodada): `@atlas/contracts` ganha `MemoryCategory = 'fact' | 'episode' | 'project'` (exportado); `Fact` ganha `category?: MemoryCategory` e `subject?: string`, ambos **opcionais** (ausência de `category` ≡ `'fact'`, acervo legado não é migrado nem revalidado no `load`); `MemoryService.remember` ganha um terceiro parâmetro opcional `options?: { category?; subject? }`, 100% compatível com os chamadores de 1/2 argumentos; `MemoryService.list` ganha `options?: { category? }`. Em `@atlas/memory` (`packages/memory/src/memory-service.ts`): a invariante do modelo persistido — "memória de projeto exige projeto" — passa a viver **dentro do módulo** (Artigo 11), rejeitando com `MemoryError` (classe já existente, nenhuma nova), **antes** de qualquer mutação/`storage.save`, tanto `category: 'project'` sem `subject` válido quanto `subject` presente com categoria diferente de `'project'`; um `subject` que colapse para vazio após `normalize` (`''`/`'   '`/`'\t\n'`) conta como **ausente e inválido**, não como "sem projeto" — achado F2 do gate. A **chave de duplicata** (SPECs 0022/0023) passa a incluir categoria+`subject` normalizados (tupla com separador que não pode aparecer no texto), reusada em `remember` e `dedupe`, para o mesmo texto em categorias/projetos diferentes não colapsar; acervo legado sem `category` segue tratado como `'fact'` para efeito de duplicata (não-regressão da SPEC-0022). `list(options?)` filtra por categoria (ausente ≡ `'fact'`). `prompt()` passa a compor **seções por categoria**, ordem fixa `fact → project → episode`, separadas por uma linha em branco, seções vazias omitidas; a seção `fact` é **byte a byte idêntica** à de antes desta SPEC (não-regressão com string literal); a seção `project` agrupa por `subject` com subcabeçalho `[projeto <subject>]` (texto original, não normalizado), ordem entre projetos pela **primeira ocorrência** na ordem de carga — formato e ordem fixados literalmente porque o texto entra no system prompt de toda geração (achado F3 do gate). `dedupe`/`search` mantêm assinatura e comportamento (agrupamento/varredura por categoria coerente com a nova chave; `search` não ganha filtro por categoria — fora de escopo por observação do próprio `architecture-reviewer`). CLI: `atlas remember "<texto>" --category <fact|episode|project> [--subject <projeto>]` e `atlas memory list [--category <c>]` (`apps/cli/src/gateway/input-gateway.ts`, `commands/remember.ts`, `commands/memory.ts`, `run.ts` + `HELP_TEXT`) — a CLI **antecipa** a invariante com `CliUsageError` amigável (categoria/`subject` inválidos, `subject` em branco, `subject` sem `category: 'project'`), mas a garantia real vive no módulo. O Learner (SPEC-0020) continua produzindo só `category: 'fact'`, `source: 'learned'` — nenhuma mudança em `@atlas/cognitive`/`@atlas/core`. **Intactos** (diff de produção vazio): `@atlas/cognitive`, `@atlas/core`, `@atlas/runtime`, `@atlas/permissions`, `@atlas/tools`, `@atlas/model-gateway`, `@atlas/persona`, `@atlas/context`, `@atlas/skills`. **Padrão recorrente NÃO disparado (2ª vez, após a SPEC-0026)**: como `Fact.category?`/`subject?` e os parâmetros novos de `remember`/`list` são todos opcionais/aditivos, o fake tipado diretamente (`apps/cli/tests/status.test.ts`) compilou sem alteração. **Com esta SPEC, todos os gates da Fase 1 estão entregues** — o Critério de Conclusão da Fase 1 ("a Memory Service cobre mais de uma categoria de conhecimento") passa a ser atendido; a decisão de iniciar a Fase 2 continua sendo do usuário, não automática. Suíte: 532 testes (era 477). Verificação verde: `pnpm lint`/`format:check`/`typecheck`/`test` + **injeção de memória guiada pela consulta do turno** (SPEC-0030, Perfil `completo`, notas de atualização no [ADR-0011](docs/06-adr/ADR-0011-memory-service-persistence.md) e no [ADR-0016](docs/06-adr/ADR-0016-learning-proposed-extraction.md), sem ADR novo; item **1.3 (Memory Service — Próximas Fatias)** do Roadmap, l. 91, `candidato · SPEC direta` — **não** gate, todos os gates da Fase 1 já entregues desde a SPEC-0029; fecha a fatia futura nomeada textualmente pela SPEC-0027, "consumo de `search` pelo Cognitive a partir da consulta do turno"): a fatia de memória injetada no system prompt de cada turno deixa de ser o **dump completo** do acervo e passa a ser uma **seleção guiada pela consulta do turno**, com um **orçamento fixo de fatos**, produzida inteiramente pela Memory Service (autoridade exclusiva sobre recuperação, Artigo 11). `@atlas/contracts`: `MemoryService.prompt` ganha um parâmetro **opcional/aditivo** — `prompt(options?: { readonly query?: string; readonly limit?: number }): string | undefined` — `Fact`/`MemoryCategory`/`remember`/`forget`/`list`/`dedupe`/`search` inalterados. `@atlas/memory` (`packages/memory/src/memory-service.ts`): helper puro top-level `selectFacts(facts, searchFn, options)` reusa `service.search` (**nenhuma** segunda definição de relevância no arquivo) — sem `limit`, ou com `facts.length <= limit`, a seleção é o acervo inteiro na ordem de carga (`prompt()` sem argumento segue **byte a byte idêntico** ao de antes); com `limit` e acervo maior, a seleção é `search(query, { limit })` (os relevantes primeiro) completada pelos fatos restantes em **ordem de carga** até atingir exatamente `limit` (piso: nunca menos que `min(total, limit)`), sempre **reordenada pela ordem de carga original** antes de `composePrompt` — a relevância decide **quem entra**, nunca a ordem nem o formato; a composição por seções da SPEC-0029 (`fact → project → episode`, vazias omitidas, seção `fact` byte a byte estável) é preservada sobre o subconjunto. `@atlas/cognitive`: `CognitiveCoreDeps.memoryPrompt` passa de `() => string | undefined` para **`(query: string, limit: number) => string | undefined`** (mesma porta, tipo **interno**, não sobe a `@atlas/contracts` — mesmo molde do `skillCatalog?`/SPEC-0026); constante embutida `MEMORY_RECALL_LIMIT = 20` (mesmo molde de `REPLAN_BUDGET`/teto de 3 do learner, não configurável); amostragem **1x por turno** preservada (invariante da SPEC-0021) nos três pontos — `ask` amostra com `objective`, `respond` com `input`, `startConversation` com `''` — sempre com `MEMORY_RECALL_LIMIT` como segundo argumento; o valor amostrado alimenta todas as `generate` do turno, inclusive `learner.instruction(...)`. `@atlas/core`: uma linha — `memoryPrompt: (query, limit) => memory.prompt({ query, limit })`. **Diff de produção vazio** em `apps/cli`, `@atlas/runtime`, `@atlas/permissions`, `@atlas/tools`, `@atlas/model-gateway`, `@atlas/persona`, `@atlas/context`, `@atlas/skills`, e em `planner.ts`/`observer.ts`/`learner.ts` dentro de `@atlas/cognitive`. **Padrão recorrente de quebra de `typecheck` em fakes NÃO disparado** (3ª vez, após as SPECs 0026 e 0029): o parâmetro opcional em `prompt` manteve válidos, por atribuibilidade de aridade, os fakes de `memoryPrompt` zero-arg já existentes em `@atlas/cognitive` — nenhuma migração obrigatória. Gate do `architecture-reviewer`: **aprovado sem veto**, com 5 achados não-bloqueantes incorporados: (1) o orçamento limita o **número** de fatos, não bytes — "fecha o custo do ADR-0011" seria overclaim, por isso a nota registra contenção **parcial**; (2) o no-op de `remember` (SPEC-0022) só cobre duplicata **textual exata normalizada** — uma reproposição parafraseada pelo learner é gravada como novo `Fact`; (3) com acervo > 20 e consulta sem match, o completamento por ordem de carga favorece sistematicamente os fatos **mais antigos** (fato recém-aprendido fica invisível até casar lexicalmente com alguma consulta, interagindo com o achado 2); (4) o critério de aceitação sobre reuso de `search` foi reescrito por estar mecanicamente frouxo; (5) o valor `20` é estimativa de bom senso, não verificável no repo (arquivo de memória é local). Suíte: **550 testes** (era 532, 47 arquivos). Verificação verde: `pnpm lint`/`format:check`/`typecheck`/`test` + **a primeira interface gráfica do Atlas: `apps/desktop` (`@atlas/desktop`), sobre Electron** (SPEC-0031, Perfil `completo`, abre a **Fase 2** do Roadmap — decisão humana explícita de iniciar a fase; consome o **[ADR-0019](docs/06-adr/ADR-0019-desktop-electron-stack.md)** novo, `Status: Accepted`, formalizando a escolha de stack e registrando honestamente a tensão com o Artigo 13 — nenhuma dependência permanente de framework — como uma exceção consciente e contida a `apps/desktop`; item **2.1 Fundação da Interface** do Roadmap, l. 139-143, primeira fatia entregue, não fecha o item inteiro): o app espelha exatamente o que a SPEC-0003 fez no terminal — um único round-trip com o Core resolvendo `status` (estado + config resolvida + Persona ativa), nenhuma etapa cognitiva, nenhuma Tool, nenhum multi-turno. `src/core-bridge.ts` é a camada testável sem Electron (molde `commands`/`gateway` da CLI): `resolveStatusSnapshot(configOverride?)` chama `createAtlas({ config })`, lê `state`/`config`/`persona`, chama `atlas.shutdown()` sempre (inclusive sucesso) e devolve um `StatusSnapshot` plano serializável por IPC — local a `apps/desktop`, não `@atlas/contracts` (sem 2º consumidor). `src/main.ts` é casca fina do main process (ciclo de vida `app.whenReady`/`window-all-closed`, cria a `BrowserWindow` com `contextIsolation: true`/`nodeIntegration: false`, registra `ipcMain.handle('atlas:status', () => resolveStatusSnapshot())`); o Core vive exclusivamente no main process — o renderer nunca importa `packages/*`, falando só por IPC via `contextBridge` (`src/preload.cjs`, CommonJS por causa de `require('electron')` colidindo com `"type": "module"` do `package.json`) e `src/renderer/{index.html,renderer.js}` (JavaScript plano, contexto Chromium, sem bundler nesta fatia). **Atrito de execução resolvido sem `dist`/bundler**: `tsx` como flag direta do binário Electron não funciona (`--import tsx` com espaço falha silenciosamente — parser de argv trata `tsx` como app-path; `--import=tsx` com `=` resolve o parsing mas o hook `.js`→`.ts` do `tsx` não se propaga ao carregamento do main process do Electron, `ERR_MODULE_NOT_FOUND`); a correção que preserva o padrão sem-`dist` do [ADR-0005](docs/06-adr/ADR-0005-app-typescript-execution.md) é registrar o hook via variável de ambiente — `NODE_OPTIONS=--import=tsx electron ./src/main.ts` (script `start` do `package.json`), validado ponta a ponta. `electron` entra como `devDependency` (build aprovado em `pnpm-workspace.yaml`, `allowBuilds: { electron: true }`, molde do `esbuild`/`tsx`); ESLint ganhou globals de contexto browser/CommonJS para `preload.cjs`/`renderer/*.js`. Smoke manual (janela abre com `Atlas: ready` + config resolvida, encerra limpo) **confirmado pelo usuário** em sessão gráfica real — o shell de automação não tem WindowServer/display, então a validação final dessa camada é sempre humana. **Diff vazio** em `@atlas/contracts` e `@atlas/core` — a superfície pronta (`createAtlas`, `AtlasConfig`, `AtlasPlatform`) já bastou para o segundo consumidor; `@atlas/cognitive`, `@atlas/runtime`, `@atlas/permissions`, `@atlas/tools`, `@atlas/model-gateway`, `@atlas/persona`, `@atlas/context`, `@atlas/memory`, `@atlas/skills`, `apps/cli` intactos. Fora de escopo desta fatia (fatias futuras 2.1-restante/2.2-2.4, cada uma sua própria SPEC): comando/etapa cognitiva na janela (`ask`/`respond`, chat visual), `ConfirmPort`/`renderSteps` visuais, voz, gerência visual de memória/Persona/permissões, empacotamento/distribuição, bundler/framework de UI, E2E de Electron em CI. Suíte: **554 testes** (era 550). Verificação verde: `pnpm lint`/`format:check`/`typecheck`/`test` + **adapters de GUI que fecham a fatia 2.1-restante: `ConfirmPort` via diálogo nativo + traço visual de `steps` + consumo `ask` de tiro único** (SPEC-0032, Perfil `completo`, sem veto do `architecture-reviewer` no gate; item **2.1 Fundação da Interface** do Roadmap, l. 139-143 — fecha a fatia 2.1-restante nomeada pela SPEC-0031; `respond`/multi-turno/sessão do Context Service seguem para o item **2.2**): `apps/desktop` ganha `src/confirm-port.ts` (`createDialogConfirmPort({ showMessageBox })`, sem import de `electron`, testável no Vitest) — satisfaz estruturalmente o `ConfirmPort` interno do `@atlas/runtime`, fail-closed (só o índice do botão "Confirmar" resolve `true`; Cancelar/`cancelId`/qualquer outro retorno ou rejeição resolve `false`, nunca lança — espelho do `EOF ⇒ false` do `nodeReadlineConfirmPort`); e `src/steps-view.ts` (`formatSteps(steps) → StepLine[]`, função pura), equivalente **de dado** ao `renderSteps` da CLI, preservando `denialKind` (`'blocked' | 'declined'`), `undefined`/vazio ⇒ `[]`. `src/core-bridge.ts` ganha `resolveAskSnapshot(objective, { confirm?, configOverride? })`: sobe `createAtlas({ config }, { confirm })` (default fail-closed), chama `atlas.cognitive.ask`, grava `learned` via `atlas.memory.remember(fact, 'learned')` (paridade com o `runAsk` da CLI, nunca gravação silenciosa), monta `AskSnapshot { text, steps: StepLine[], learned }` plano/serializável por IPC, desliga sempre (`finally`) — **stateless**, como `resolveStatusSnapshot` (sobe e desliga o Core por round-trip, sem `Conversation` viva entre chamadas). `src/main.ts` constrói `createDialogConfirmPort({ showMessageBox: dialog.showMessageBox })` e registra `ipcMain.handle('atlas:ask', …)`; `src/preload.cjs` expõe `window.atlas.ask(objective)`; `renderer/{index.html,renderer.js}` ganham campo de objetivo + botão que pinta `text`/`steps` (`🔧 <tool> → <outcome>`, negações marcadas)/`learned`, sem conhecer `ExecutedStep`. `ConfirmPort`/`AskSnapshot`/`StepLine`/canal `'atlas:ask'` permanecem **locais** a `apps/desktop` (mesma regra de `StatusSnapshot`/`'atlas:status'` da SPEC-0031 — sem 2º consumidor real, promoção só via ADR). **Diff confinado a `apps/desktop`**: zero alteração em `@atlas/contracts`/`@atlas/core`/`@atlas/runtime`/`@atlas/permissions`/`@atlas/cognitive` — a superfície pronta (`AskResult`, `ExecutedStep.denialKind`, `createAtlas(config, { confirm })`) já bastou. Achado de implementação: `dialog.showMessageBox` exige `type` como union literal e `buttons: string[]` mutável (não `readonly`/`string` genérico) — só aparece ao compilar `main.ts` real, não nos testes do adapter isolado. Testes novos/estendidos: `confirm-port.test.ts`, `steps-view.test.ts`, casos de `resolveAskSnapshot` em `core-bridge.test.ts` — todos sobre o gateway `fake` determinístico, sem exercitar round-trip real com `delete_file`/`ConfirmPort` (fica só para o smoke manual). **Smoke manual visual** (janela real + diálogo de confirmação de `delete_file`) segue **pendente de confirmação humana** em sessão gráfica real — mesmo atrito estrutural da SPEC-0031 (shell de automação sem WindowServer), agora confirmado recorrente em toda fatia visual da Fase 2. Suíte: **570 testes** (era 554, 50 arquivos). Verificação verde: `pnpm lint`/`format:check`/`typecheck`/`test` + **chat visual multi-turno na janela — o Core mantido vivo entre turnos no main process, equivalente GUI do `atlas chat`** (SPEC-0033, Perfil `completo`; item **2.2 Experiência Conversacional** do Roadmap, l. 145-149 — entrega a primeira das três linhas do item, chat multi-turno; traço de `steps` e diálogo de confirmação já entregues como adapters pela SPEC-0032; voz (2.3) e gerência visual de estado (2.4) ficam de fora): `src/core-bridge.ts` ganha o ciclo de vida de **chat vivo** — ao contrário de `resolveStatusSnapshot`/`resolveAskSnapshot` (sobem e desligam o Core a cada chamada), o chat mantém o Core vivo entre turnos. `openChatSession(deps?: { confirm?; configOverride? }): Promise<SessionId>` chama `createAtlas({ config }, { confirm })` **uma vez** (default `confirm` fail-closed), abre uma sessão do Context Service com uma conversa fresca (`atlas.context.openSession(atlas.cognitive.startConversation())`) e registra `{ atlas }` num `Map<SessionId, { atlas }>` interno chaveado pela própria `SessionId` devolvida pelo Context (não um identificador próprio) — devolve essa `SessionId` como handle opaco. `sendChatTurn(session, input): Promise<TurnSnapshot>` é a mediação que o ADR-0009 atribui à aplicação: lê a conversa do Context (`getConversation`), chama `atlas.cognitive.respond` (puro), grava o histórico de volta (`updateConversation`) e persiste `learned` via `atlas.memory.remember(fact, 'learned')` (só `created`), devolvendo `TurnSnapshot { reply, steps: StepLine[], learned: string[] }` — dado plano serializável por IPC, via `formatSteps` (reuso da SPEC-0032); **não** desliga o Core nem fecha a sessão; se o turno falhar, a promessa rejeita mas a sessão **permanece viva** (paridade com a resiliência do `runChat` frente a `ATLAS_MODEL_GATEWAY`). `closeChatSession(session): Promise<void>` fecha a sessão do Context e chama `atlas.shutdown()` (uma vez), removendo o registro; idempotente/tolerante a handle já encerrado. Handle desconhecido ou já encerrado em `sendChatTurn`/`closeChatSession` ⇒ rejeita com `Error` estruturado, nunca `TypeError` de acesso a `undefined` — "nunca aberta" e "já encerrada" tratados identicamente (rejeição estruturada). `src/main.ts` registra os três canais (`'atlas:chat:open'`/`'atlas:chat:send'`/`'atlas:chat:close'`), reusando o **mesmo** `confirm` de diálogo (`createDialogConfirmPort`, SPEC-0032) construído uma vez para `ask`; rastreia as `SessionId` abertas por esta janela (`Set<SessionId>` próprio, `openChatSessionIds`, não exposto pelo `core-bridge`) só para o teardown — em `window-all-closed`/`before-quit`, encerra todas as sessões vivas (tolerante a falha/handle já encerrado), garantindo nenhum Core órfão ao fechar a app. `src/preload.cjs` expõe `window.atlas.chat.{open,send,close}` ao lado de `getStatus`/`ask`; `renderer/index.html` + `renderer.js` ganham uma superfície de chat mínima — abre a sessão ao carregar, formulário de entrada + transcript que cresce por turno (`> <input>`, `🔧 <tool> → <outcome>`, `<reply>`, `💡 lembrado: <fato>`), um turno por vez (entrada desabilitada enquanto aguarda resposta, serialização no renderer, sem locking no bridge). `TurnSnapshot`/o handle de chat/os canais IPC permanecem **locais** a `apps/desktop` (mesma regra de `StatusSnapshot`/`AskSnapshot` — promoção só com 2º consumidor real, via ADR); o valor `Conversation` **nunca** cruza o IPC (Artigo 4 + isolamento Electron). `confirm-port.ts`/`steps-view.ts` reusados **sem alteração** (diff vazio). **Diff confinado a `apps/desktop`**: zero alteração em `@atlas/contracts`/`@atlas/core`/`@atlas/runtime`/`@atlas/permissions`/`@atlas/cognitive`/`@atlas/context` (só `import type` de tipos já existentes). "Persistido pela sessão do Context Service" (Roadmap l. 147) lido como **vivo durante a sessão** (ADR-0008/0009), não durável entre reinícios — persistência durável de conversas fica fatia futura própria, que revisitaria o ADR-0009. **Smoke manual visual** (dois turnos encadeados com contexto acumulado, traço de `steps`, diálogo nativo de confirmação de `delete_file`, encerramento limpo) segue **pendente de confirmação humana** em sessão gráfica real — 3ª fatia visual seguida (0031/0032/0033) com o mesmo atrito estrutural do shell de automação sem WindowServer. Suíte: **575 testes** (era 570, 50 arquivos). Verificação verde: `pnpm lint`/`format:check`/`typecheck`/`test` + **gerência visual de memória na janela — listar e esquecer fatos, primeira linha do item 2.4 do Roadmap** (SPEC-0034, Perfil `completo`; item **2.4 Persistência e Gerência Local** do Roadmap, l. 159-163 — entrega a primeira das três linhas do item; troca de Persona em runtime e configuração de permissões por GUI ficam para as próximas fatias): `src/core-bridge.ts` ganha `resolveMemorySnapshot(deps?: { configOverride? }): Promise<readonly FactSnapshot[]>` e `forgetFact(id, deps?): Promise<boolean>` — equivalente GUI de `atlas memory list`/`atlas forget`. Diferente do chat vivo (SPEC-0033), a memória é durável em disco (ADR-0011): ambos seguem o molde **stateless** de `resolveStatusSnapshot`/`resolveAskSnapshot` — sobem o Core via `createAtlas`, operam (`atlas.memory.list()`/`atlas.memory.forget(id)`) e desligam (`finally`) a cada chamada; a leitura/remoção passam sempre pelo `MemoryService`, nunca por acesso direto ao arquivo JSON de memória (Artigo 11). `FactSnapshot { id, text, createdAt, source, category, subject? }` — plano, serializável por IPC; `resolveMemorySnapshot` resolve os mesmos defaults que `runMemoryList` resolve na CLI (`fact.source ?? 'user'`, `fact.category ?? 'fact'`) antes de cruzar o IPC; `forgetFact` devolve `true`/`false` (paridade com `runForget`), nunca lança para um id inexistente. Canais IPC novos, ao lado de `'atlas:status'`/`'atlas:ask'`/`'atlas:chat:*'`: `ipcMain.handle('atlas:memory:list', …)`/`ipcMain.handle('atlas:memory:forget', …)` em `main.ts`; `preload.cjs` expõe `window.atlas.memory.{list,forget}`. `renderer/index.html` ganha um painel de memória mínimo (lista + botão Atualizar); `renderer.js` carrega a lista ao abrir a janela, pinta uma linha por fato (id/texto/data/origem/categoria/projeto quando houver) com um botão "Esquecer" por linha — sem diálogo nativo de confirmação (paridade com `atlas forget`, que também não confirma; a ação já é explícita, botão rotulado por fato). `FactSnapshot`/canais `'atlas:memory:*'` permanecem **locais** a `apps/desktop` (mesma regra de `StatusSnapshot`/`AskSnapshot`/`TurnSnapshot` — sem 2º consumidor real, promoção só via ADR). **Diff confinado a `apps/desktop`**: zero alteração em `@atlas/contracts`/`@atlas/core`/`@atlas/memory`/demais packages do Core. Fora de escopo (candidatos futuros): editar/atualizar um fato, adicionar fatos pela GUI, filtro/busca na UI, deduplicação pela GUI. **Smoke manual visual** (semear fatos via CLI, abrir o painel, ver a lista, esquecer um, ver sumir) segue **pendente de confirmação humana** em sessão gráfica real — 4ª fatia visual seguida (0031/0032/0033/0034) com o mesmo atrito estrutural do shell de automação sem WindowServer; não bloqueia o fechamento documental. Suíte: **581 testes** (era 575, 50 arquivos). Verificação verde: `pnpm lint`/`format:check`/`typecheck`/`test` + **saída de voz (TTS) na janela — botão "🔊 Ouvir" por resposta do chat, primeira fatia do item 2.3 do Roadmap** (SPEC-0035, Perfil `completo`; Fase 2, item **2.3 Voz**, l. 151-157 — cobre só "Saída por voz (TTS)", l. 156; entrada por voz/STT e ativação por voz/wake word ficam adiadas, Escalação E1, exigem ADR novo e possivelmente um módulo Voice Service): `apps/desktop/src/speech-output.ts` (novo) exporta `createSpeechOutput({ synth }): SpeechOutput` (`speak`/`cancel`/`isAvailable`) sobre uma porta estrutural local `SpeechSynthesisPort`/`UtteranceSpec`, no molde de `src/confirm-port.ts` — `speak(text)` normaliza, é no-op em vazio, cancela a fala anterior antes da próxima, nunca lança (fail-safe); `isAvailable()` é `true` sse `synth.getVoices()` tiver ao menos uma voz. Offline por construção: nenhuma chamada de rede no módulo, toda síntese passa pelas vozes locais do SO expostas por `window.speechSynthesis`/`SpeechSynthesisUtterance` (Web Speech API embutida no Chromium — o ADR-0019 já adotou o runtime; **dispensa ADR próprio**, Escalação E2 resolvida pelo usuário). `renderer.js` ganha um adapter `synth` sobre `window.speechSynthesis` e um botão "🔊 Ouvir" por resposta (`appendReply`), desabilitado com aviso quando `isAvailable()` é `false`; **replica deliberadamente** o algoritmo de `createSpeechOutput` em JS puro (`createSpeechOutputGlue`, com comentário apontando a duplicação) porque `renderer.js` é `<script>` clássico sem bundler (ADR-0019) e não pode `import` o módulo TS em runtime — diferente de `confirm-port.ts`/`steps-view.ts`, consumidos só pelo main process via `tsx`; reavalia a disponibilidade de voz no evento `voiceschanged` do Chromium (`getVoices()` costuma vir `[]` até esse evento disparar), evitando falso-negativo. `eslint.config.js` ganha o global `SpeechSynthesisUtterance`. **Diff confinado a `apps/desktop`** (+ 1 linha de eslint): zero alteração em `@atlas/contracts`/`@atlas/core`/`main.ts`/`preload.cjs`/`core-bridge.ts`/canais IPC. Fora de escopo (candidatos futuros): fala automática, seleção/preferência de voz persistida, falar `ask`/`status`/`learned`. **Smoke manual visual** (abrir a janela, mandar um turno, clicar "Ouvir", ouvir a voz; e o fallback com voz indisponível) segue **pendente de confirmação humana** em sessão gráfica real — 5ª fatia visual seguida (0031-0035) com o mesmo atrito estrutural do shell de automação sem WindowServer; não bloqueia o fechamento documental. Suíte: **591 testes** (era 581). Verificação verde: `pnpm lint`/`format:check`/`typecheck`/`test` + **endurecimento do TTS para 100% offline garantido — restringir a saída de voz a vozes locais do SO** (SPEC-0036, Perfil `completo`; mesma fatia 2.3 da SPEC-0035, endurecida, não abre item novo do Roadmap): `apps/desktop/src/speech-output.ts` ganhou `VoiceInfo { voiceURI, name, localService }` (projeção estrutural mínima de `SpeechSynthesisVoice`, nenhum tipo de navegador importado), `SpeechSynthesisPort.getVoices()` passou a devolver `readonly VoiceInfo[]` e `UtteranceSpec` ganhou `voiceURI`; `createSpeechOutput` filtra as vozes candidatas para só `localService === true`, seleciona deterministicamente a primeira (ordem de `getVoices()`) e carimba seu `voiceURI` no `UtteranceSpec` — `isAvailable()` só é `true` havendo ao menos uma voz local; sem nenhuma voz local, `speak` é no-op e a saída fica indisponível, **nunca** cai numa voz de rede como fallback (fail-closed). `renderer.js`: o adapter `synth` projeta cada `SpeechSynthesisVoice` real para `{ voiceURI, name, localService }` em `getVoices()` e, em `speak(spec)`, localiza a voz real cujo `voiceURI` e `localService === true` casam com o `spec` recebido, define `utterance.voice` explicitamente antes de falar, e **não fala** se essa voz não for encontrada; `createSpeechOutputGlue` espelha a mesma seleção pura (`selectLocalVoiceURI`), mantendo a duplicação deliberada renderer↔módulo já documentada na SPEC-0035. A garantia depende de o SO/navegador reportarem `localService` corretamente — sinal-padrão da Web Speech API, o mais forte disponível sem embarcar motor de voz próprio (Escalação E1, não acionada). **Diff confinado a `apps/desktop`**: zero alteração em `@atlas/contracts`/`@atlas/core`/`main.ts`/`preload.cjs`/`core-bridge.ts`/canais IPC, nenhuma dependência de runtime nova. Suíte: **597 testes** (era 591). Verificação verde: `pnpm lint`/`format:check`/`typecheck`/`test`. + **seleção e troca de Persona em runtime pela interface gráfica** (SPEC-0037, Perfil `completo`; segunda das três linhas do item **2.4 Persistência e Gerência Local** do Roadmap — a primeira, gerência visual de memória, foi a SPEC-0034; a terceira, permissões por GUI, fica futura): `packages/core/src/index.ts` ganha um **re-export de catálogo** (`createPersonaService`, `PERSONA_IDS` de `@atlas/persona`), aditivo, no molde de `loadConfig`/`defaultConfig` — a porta pela qual `apps/desktop` lê o catálogo de Personas continuando a importar implementação só de `@atlas/core` (ADR-0003 / Regra de Dependência 11 preservados; diff de `packages/core` é exclusivamente essa linha, 8 linhas aditivas). `apps/desktop/src/core-bridge.ts` ganha `PersonaOption { id, name }`/`PersonaSelection { personaId, closedSessions }`, `listPersonas(deps?)` (síncrono, sem subir o Core), `selectPersona(id, deps?)` (valida `PersonaService.has(id)`, recusa com turno de chat em voo, registra a seleção em estado de módulo do main process, encerra todas as sessões de chat vivas) e `selectedPersonaId()`; a seleção corrente é aplicada como `config.persona` em todas as funções que sobem o Core (`resolveStatusSnapshot`/`resolveAskSnapshot`/`openChatSession`/`resolveMemorySnapshot`/`forgetFact`), com `configOverride.persona` explícito do chamador sempre vencendo. `sendChatTurn` passa a rastrear turno em voo (marca/desmarca a sessão em `try`/`finally`) — é o que permite `selectPersona` recusar a troca em duas camadas junto com o renderer (que desabilita o seletor durante o turno). `main.ts` registra `ipcMain.handle('atlas:persona:list'/'atlas:persona:select', …)` e sincroniza `openChatSessionIds` com `closedSessions`; `preload.cjs` expõe `window.atlas.persona.{list,select}`; `renderer/index.html`+`renderer.js` ganham um seletor de Persona mínimo, incluído na mesma serialização de turno da SPEC-0033, que ao trocar limpa o transcript, anuncia a troca, reabre a sessão e recarrega o status (em rejeição, mostra o erro sem destruir a conversa corrente). A seleção vive só em memória do main process pela sessão da app (estado de módulo, molde do `Map` de sessões da SPEC-0033) — não é persistida, reabrir a app volta à Persona da config; nenhum estado persistente novo, nenhuma tensão com o Artigo 11. `PersonaOption`/`PersonaSelection`/canais IPC **locais** a `apps/desktop` (mesma regra das SPECs 0031-0034 — sem 2º consumidor real). **Diff vazio** em `@atlas/contracts`/`@atlas/persona`/`@atlas/cognitive`/demais packages do Core/`apps/cli`. Fora de escopo (fatia futura, escalação obrigatória da Emenda v1.1 — ADR novo, exige brainstorming humano): **criar** Persona pela GUI (personalidade, voz), persistir a preferência entre reinícios, vincular voz do TTS à Persona. Suíte: **605 testes** (era 597). Verificação verde: `pnpm lint`/`format:check`/`typecheck`/`test`. + **configuração de permissões (`readRoots`/`writeRoots`) por interface gráfica** (SPEC-0038, Perfil `completo`; terceira e última linha do item **2.4 Persistência e Gerência Local** do Roadmap — a primeira, gerência visual de memória, foi a SPEC-0034; a segunda, seleção/troca de Persona, a SPEC-0037 — **fechando o item 2.4 por inteiro**): `apps/desktop/src/core-bridge.ts` ganha `PermissionRoots { readRoots; writeRoots }`, `PermissionRootsSelection extends PermissionRoots { closedSessions }`, `selectPermissionRoots(request, deps?: { confirmGrant? })` e `selectedPermissionRoots()` — equivalente GUI de `--allow-read`/`--allow-write`, estado de módulo (mesmo molde da seleção de Persona), sem estado persistente novo. `selectPermissionRoots` é fail-closed e tudo-ou-nada: normaliza (`trim`/dedup), recusa `readRoots` vazia ou caminho não absoluto, recusa com qualquer operação em voo, pede consentimento via `GrantConfirmPort` para toda raiz de escrita **nova** (recusa ou ausência da porta aborta tudo), rechecagem da operação em voo imediatamente antes de aplicar, registra a seleção e encerra todas as sessões de chat vivas. Rastreio de operação em voo generalizado (`inFlightOperations`) a **toda função que sobe um Core capaz de executar Tools** (`resolveAskSnapshot` e a janela de abertura de `openChatSession`) — deliberadamente não cobre `resolveStatusSnapshot`/`resolveMemorySnapshot`/`forgetFact`, que não executam Tools. Helper `withSelections` (generalização de `withPersonaSelection`) aplica Persona + permissões ao `AtlasConfigOverride`, com `permissions` tratado como bloco completo (override parcial do chamador não recebe a seleção corrente mesclada). `apps/desktop/src/permission-grant-dialog.ts` (novo): `GrantRequest`/`GrantConfirmPort`/`createGrantConfirmDialog`, adapter dedicado à concessão de política (texto explícito de concessão/subárvore/duração de sessão), distinto do `ConfirmPort` de ação pontual do Runtime (`confirm-port.ts`, inalterado). `main.ts` registra `ipcMain.handle('atlas:permissions:select', …)` e sincroniza `openChatSessionIds`; `preload.cjs` expõe `window.atlas.permissions.select(roots)`; `renderer/index.html`+`renderer.js` ganham um painel de permissões mínimo (listas de leitura/escrita, adicionar/remover/aplicar), na mesma serialização de turno de chat/`ask`. Painel mostra as raízes **configuradas** (`config.permissions`), não as resolvidas por `realpath` (que erram sempre para o lado mais restritivo). Tipos e canal IPC **locais** a `apps/desktop` (mesma regra das SPECs 0031-0037 — sem 2º consumidor real). **Diff vazio** em `packages/*` (inclusive `@atlas/contracts`/`@atlas/core`/`@atlas/permissions`) e em `apps/cli`; nenhum arquivo de `apps/desktop/src` importa `@atlas/permissions`. Fora de escopo (fatia futura): persistir a política entre reinícios (exigiria ADR — slot `arquivo` do ADR-0006, ou Memory), seletor nativo de diretório, exposição da política resolvida. Suíte: **623 testes** (era 605). Verificação verde: `pnpm lint`/`format:check`/`typecheck`/`test`. + **CRUD de Personas custom pela interface gráfica, com voz real vinculada** (SPEC-0039, Perfil `completo`, consumindo o [ADR-0020](../06-adr/ADR-0020-persona-persistence-voice-binding.md) — Accepted, decisão humana; estende a linha de Persona do item **2.4 Persistência e Gerência Local** do Roadmap, já entregue por inteiro pela trinca SPEC-0034/0037/0038): `@atlas/contracts` ganha `Persona.voiceURI?: string` (aditivo), `PersonaInput` (todos os campos de `Persona` exceto `id`, obrigatórios salvo `voiceURI?`) e três membros síncronos novos em `PersonaService` — `create(input)`/`update(id, input)`/`delete(id)`. `@atlas/persona` ganha uma porta interna **síncrona** `PersonaStorage { load(); save(personas) }` (divergência deliberada do `MemoryStorage` assíncrono, para `createPersonaService`/`createAtlas` seguirem compondo sem `await`) e `createFilePersonaStorage(path)` — arquivo JSON `{ "personas": [...] }`, criado sob demanda, ausência ⇒ `[]`, JSON inválido ⇒ `PersonaError` citando o caminho do arquivo (fail-high, nunca descarta/sobrescreve o arquivo do usuário). `createPersonaService(deps: { storage? })`: sem `storage`, comportamento idêntico a hoje (só `jarvis`/`neutral`, `create`/`update`/`delete` lançam `PersonaError`); com `storage`, carrega as custom uma vez (load-once) e persiste a cada mutação (write-through); `create` deriva `id` por slug do `name` com sufixo numérico em colisão; `update`/`delete` sobre id embutido ou inexistente lançam; `update` preserva o `id`. `@atlas/core` ganha `CreateAtlasDeps.personaStorage?` (opt-in, sem default — `apps/cli` não injeta, diff vazio byte a byte), `loadConfig(override, options?: { personaIds? })` (aditivo; sem o parâmetro, valida contra `PERSONA_IDS` como sempre) e o módulo novo `packages/core/src/config/data-dir.ts` com **`resolveDataDir(override?): string`** — resolve **somente** `dataDir` pela precedência de sempre, **nunca** valida `persona` nem aciona `PERSONA_IDS` (correção do bloqueante B1, ver Observações abaixo) — e **`personaStoragePath(dataDir): string`**, pura, único derivador do caminho `<dataDir>/personas.json` em todo o repositório; ambas re-exportadas por `@atlas/core`, ao lado de `createFilePersonaStorage`/`PersonaStorage`. `apps/desktop/src/core-bridge.ts` ganha `describePersona`/`createPersona`/`updatePersona`/`deletePersona` (stateless, sobre um `PersonaService` construído na hora via `personaStorageFor(configOverride)` — nunca por `loadConfig`, só por `resolveDataDir`), `PersonaOption.builtin`, `StatusSnapshot.persona.voiceURI?`; as guardas de `update`/`delete` comparam com a Persona **efetiva** (`selectedPersonaId() ?? loadConfig(..., { personaIds }).persona`, não só a seleção em memória); `delete` sobre a Persona ativa é sempre recusado, `update` sobre a ativa encerra as sessões de chat vivas; `apps/desktop/src/persona-delete-dialog.ts` (novo) — `PersonaDeleteConfirmPort`/`createPersonaDeleteDialog`, diálogo nativo de consentimento antes de apagar, molde exato de `permission-grant-dialog.ts` (SPEC-0038), default fail-closed (sem a porta injetada, nada é apagado). `apps/desktop/src/speech-output.ts` ganha `preferredVoiceURI?: () => string | undefined` (provider amostrado a cada `speak`, molde do `memoryPrompt` da SPEC-0021): entre as vozes locais, prefere a de `voiceURI` igual ao da Persona ativa; ausente/indisponível/lançando, cai na 1ª voz local (comportamento das SPECs 0035/0036, byte a byte); sem voz local alguma, `speak` segue no-op — nunca voz de rede. UI: painel de Persona (SPEC-0037) ganha formulário completo (8 campos + `<select>` de voz local, repopulado em `voiceschanged`) para criar/editar, e "Apagar" por Persona custom; `jarvis`/`neutral` seguem somente-leitura. **Diff vazio** em `@atlas/cognitive`/`@atlas/runtime`/`@atlas/tools`/`@atlas/memory`/`@atlas/context`/`@atlas/permissions`/`@atlas/skills` e `apps/cli`. **Gate em 3 rodadas**: 1º veto com 5 bloqueantes (A1-A5, principalmente o caminho do arquivo ignorando `configOverride` e a falta de confirmação para apagar); a correção do A1 introduziu um **2º veto** (bloqueante novo B1 — deadlock circular via `loadConfig`), **escalado ao usuário** (2º veto consecutivo, Emenda v1.1), que decidiu a via de correção (extrair `resolveDataDir` sem validar `persona`, D17); 3ª rodada aprovada sem bloqueantes, com 4 achados não-bloqueantes (N1-N4, todos tratados ou registrados como limitação conhecida). **Observação A8 propagada aqui** (instrução explícita da SPEC): o Artigo 11 (Memória com autoridade exclusiva sobre estado persistente) cobre **conhecimento** persistente — fatos, episódios, projetos; o arquivo de Personas é **configuração de identidade** criada explicitamente pelo usuário, não conteúdo de conversa nem candidato a recall no prompt, e por isso vive fora do Memory Service, atrás da porta própria do Persona Service (ADR-0011/ADR-0020). Suíte: **683 testes/55 arquivos** (era 623/52). Verificação verde: `pnpm lint`/`format:check`/`typecheck`/`test`. Desde a **SPEC-0040** (Desktop: Piper como motor de TTS neural local, com processo de longa duração e fallback fail-closed para a Web Speech API), consumindo o **[ADR-0021](../06-adr/ADR-0021-piper-tts-local-voice-engine.md)** (`Accepted`, decisão humana desta sessão), o Atlas passa a falar com voz **neural** (Piper), não mais só as vozes robóticas do SO: `apps/desktop/src/piper-tts.ts` (novo, puro/injetável, sem `electron`) mantém **um processo Piper de longa duração** no main process — modelo carregado uma vez, reusado entre utterances do mesmo modelo, reciclado (`kill`+novo `spawn`) ao trocar de modelo, no timeout (15 s) e no cancelamento — expondo `listVoices()`/`isAvailable()`/`synthesize(text, voiceURI)`/`cancel()`/`shutdown()`; descobre modelos em `modelsDir` pelo **esquema real** do `.onnx.json` (`audio.sample_rate`→`sampleRate`, com derivação por `audio.quality` quando ausente; `language.code`→`language`, caindo em `espeak.voice`/prefixo do `id`; `name` derivado de `dataset`, nunca lido de uma chave de topo inexistente), omitindo um modelo **só** por par `.onnx`/`.onnx.json` incompleto ou JSON inválido — nenhum outro campo ausente descarta a voz. Contrato de invocação **pinado como dado da SPEC** (Piper `v1.2.0`, D4): argv array (`--model`/`--config`/`--json-input`, nunca shell), uma linha JSON de stdin por utterance (`text`+`output_file`, texto normalizado/colapsado), conclusão pela 1ª linha de stdout sem parsear conteúdo, `output_file` sempre gerado no main process (`TmpDirProvider`+`RandomId`, nunca de entrada do renderer, D12) — qualquer divergência do binário real aciona retorno ao `spec-drafter`, nunca ajuste ad hoc. `apps/desktop/src/speech-output.ts` ganha `isPiperVoiceURI`/`piperModelIdOf`/`resolveVoiceBackend` (roteamento puro da cadeia de fallback D8: preferida Piper/SO existente → default Piper → 1ª voz local → mudo); `createSpeechOutput` **inalterado**, segue autoritativo no caminho `'os'` (D16, testado por concordância — critério 20). `apps/desktop/src/main.ts` cria a instância única de `PiperTts`, registra `'atlas:tts:{voices,speak,cancel}'` e encerra o processo no teardown; `preload.cjs` expõe `window.atlas.tts.*`; `renderer.js` replica o roteamento (duplicação deliberada, 4ª ocorrência do mesmo atrito das SPECs 0035/0036/0039), toca o áudio por `Blob`/`URL.createObjectURL`/`<audio>`, e reavalia disponibilidade em **dois gatilhos** independentes — resposta do IPC de vozes Piper e `voiceschanged` do Chromium (D17) — preservando a seleção `piper:<id>` na repopulação do `<select>` de voz do formulário de Persona (SPEC-0039); `index.html` ganha `media-src 'self' blob:` na CSP. `apps/desktop/resources/piper/README.md` (novo, fora do git) documenta a versão pinada, o layout dos assets e o esquema de chaves; nenhum binário/modelo versionado, nenhuma chamada de rede em nenhum caminho. **Diff confinado a `apps/desktop`**: zero alteração em `packages/*`, `apps/cli` e `core-bridge.ts` — `Persona.voiceURI?` (ADR-0020) já aceitava a string `piper:<id>` sem mudança de schema. **Critério de Aceitação 25 é verificação humana pós-merge, pendente**: o ambiente de automação não tem WindowServer nem o binário Piper instalado, então voz real soando, latência (~100–300 ms estimada no ADR-0021) e ausência de processo/arquivo temporário órfão nunca foram confirmados de fato — mesmo tratamento honesto do smoke visual pendente desde a SPEC-0031, registrado em `NEXT_CONTEXT.md`. Suíte: **727 testes/56 arquivos** (era 683/55). Verificação verde: `pnpm lint`/`format:check`/`typecheck`/`test`. Desde a **SPEC-0041** (Desktop: superfície de voz Piper-only — vozes do SO deixam de ser escolhíveis quando o Piper está disponível, com o fallback interno do ADR-0021(c) intacto), a pedido explícito do usuário (que comparou as vozes do Piper às do SO e preferiu Piper), o `<select>` de voz do formulário de Persona (SPEC-0039/0040) e o roteamento de fala passam a considerar **só** vozes Piper quando o Piper está de fato disponível — não apenas quando há modelos em disco: `apps/desktop/src/speech-output.ts` ganha dois helpers puros novos, `isPiperOnlyMode({ piperAvailable, piperVoiceURIs })` (`true` sse `piperAvailable === true` e catálogo não vazio) e `piperOnlyPreference({ preferredVoiceURI, piperAvailable, piperVoiceURIs })` (em modo Piper-only devolve a preferência só se for `piper:<id>`, senão `undefined`; fora do modo devolve a preferência inalterada) — camada **antes** de `resolveVoiceBackend`, que fica intocada, no molde de `createSpeechOutput`/`isPiperVoiceURI`/`piperModelIdOf`/`PIPER_VOICE_PREFIX` (diff só de adição). `piperAvailable` vem de um canal IPC novo, `'atlas:tts:available'` (`main.ts`+`preload.cjs`, uma linha cada), expondo `PiperTts.isAvailable()` já existente — que verifica **presença do arquivo** do binário e catálogo não vazio, não que o binário execute (limite conhecido, declarado, não corrigido: um binário presente mas inexecutável ainda produz `isAvailable() === true`, deixando "Testar voz" habilitado e mudo nesse cenário raro, com "🔊 Ouvir" seguindo funcional via o fallback interno). `renderer.js` deriva `offeredPiperVoices` de `isPiperOnlyMode` como ponto único de decisão para `currentVoiceBackend` (réplica dos helpers, 6ª ocorrência da duplicação deliberada) e `populatePersonaVoiceSelect` (só Piper em modo Piper-only, só SO no modo degradado, nunca as duas origens juntas), deriva o estado de "Testar voz" das opções efetivamente oferecidas, cai em "Nenhuma (voz padrão)" ao abrir o formulário de uma Persona com `voiceURI` sem `<option>` correspondente, e mostra um aviso visível (`#persona-voice-legacy`) quando a Persona editada tem voz do SO salva em modo Piper-only — nada é gravado sem submit explícito. A Web Speech API (ADR-0021(c)) **não é removida**: continua como rede de segurança interna e invisível, ativa só quando o Piper falha de fato ou está indisponível (modo degradado, comportamento das SPECs 0035/0036/0039/0040 preservado). Reverte deliberadamente a precedência da SPEC-0040/D8 (voz da Persona persistida à frente do default Piper) — decisão registrada em nota nas Observações do [ADR-0021](../06-adr/ADR-0021-piper-tts-local-voice-engine.md) e do [ADR-0020](../06-adr/ADR-0020-persona-persistence-voice-binding.md), sem editar a Decisão de nenhum dos dois. **Diff confinado a `apps/desktop`**: zero alteração em `packages/*`, `apps/cli`, `core-bridge.ts` e `piper-tts.ts`. **Gate em 3 rodadas, 2 vetos** — 1º (A1-A5, condicionar a política só à lista de modelos revivia o achado A2 da SPEC-0035); 2º (B1, afirmação textual imprecisa sobre o que `isAvailable()` prova, escalado ao usuário, resolvido por honestidade textual). Critério de Aceitação 25 da SPEC-0040 (verificação humana pendente) ganha mais cenários com esta fatia, registrados em `NEXT_CONTEXT.md`. Suíte: **745 testes/56 arquivos** (era 727/56). Verificação verde: `pnpm lint`/`format:check`/`typecheck`/`test`. Desde a **SPEC-0042** (Quebra de `apps/desktop/tests/core-bridge.test.ts`, 1.432 linhas, em sete arquivos por assunto — `core-bridge.{status-ask,chat-session,memory,persona-selection,permissions,persona-authoring,boundary}.test.ts` — mais o helper `tests/helpers/core-bridge-harness.ts`, e verificação escopada por package via `"test": "vitest run"` acrescentado aos 13 `package.json` com testes, viabilizando `pnpm --filter <package> test`/`typecheck`), a suíte de desktop deixa de exigir um arquivo de 1.432 linhas no contexto do subagent (maior arquivo resultante com 437 linhas), e a verificação de iteração deixa de exigir rodar o workspace inteiro: nova subseção "Verificação escopada" em `docs/04-engineering/ClaudeCodeAutomation.md` fixa os quatro comandos (`pnpm --filter <pkg> test`/`typecheck`, `pnpm exec eslint <caminho>`, `pnpm exec prettier --check <caminho>`) e a condição de validade da equivalência com a CI (só enquanto o `vitest.config.ts` da raiz contiver apenas `include`); `.claude/agents/{spec-implementer,spec-validator,spec-closer}.md` foram atualizados — `spec-validator`/`spec-closer` mantêm obrigatoriamente os quatro comandos completos na raiz ao menos uma vez, incluindo `pnpm format:check`, que nenhum dos dois rodava antes (a CI já rodava). A quebra corrigiu, de passagem e por exceção nomeada e estreita (D14), um **flake pré-existente** de vazamento de sessão — o caso de `updatePersona` sob operação em voo abria sessão via `openChatSession` e não a fechava, exposto só sob `--sequence.shuffle` e provado pré-existente contra o arquivo monolítico original; `__resetBridgeStateForTests()` não fecha sessões vivas, lacuna nomeada como candidato futuro (D15) em `apps/desktop/CLAUDE.md`. Zero diff em `packages/*/src`/`apps/*/src`; `vitest.config.ts` e scripts da raiz byte-idênticos; CI inalterada. Suíte: **745 testes/62 arquivos** (era 745/56). Verificação verde: `pnpm lint`/`format:check`/`typecheck`/`test`. Desde a **SPEC-0043** (Desktop: fecha os dois resíduos de voz registrados nas Observações da SPEC-0041), o Atlas para de destruir em silêncio uma escolha de voz do usuário: `apps/desktop/src/speech-output.ts` ganha `resolvePersistedVoiceSelection` (função pura, quatro desfechos exaustivos — `none`/`available`/`retained`/`dropped` —, composta **sobre** `piperOnlyPreference`, nunca uma condição paralela), que distingue "descartado por política" (voz do SO em modo Piper-only, D6/D12 da SPEC-0041, substituição anunciada e desejada, comportamento intacto) de "não ofertável por razão ambiental" (Piper indisponível, modelo Piper removido, voz do SO desinstalada); no segundo caso, a `voiceURI` persistida aparece no `<select>` do formulário de Persona como uma `<option>` **retida**, já selecionada e rotulada, com aviso ao lado (`#persona-voice-legacy`, reusado, sem diff em `index.html`) — salvar preserva o valor, trocá-lo exige gesto explícito. "Testar voz" (`refreshTestVoiceButton`) passa a habilitar-se só quando o valor selecionado é efetivamente reproduzível agora (nunca a opção retida, nunca "Nenhuma"), refinando o critério 9 da SPEC-0041. Em paralelo, `createSpeechOutputGlue` do renderer passa a receber `preferredVoiceURI` (mesmo `piperOnlyPreference` de `currentVoiceBackend`), encerrando a deriva aberta desde a SPEC-0035/0039 em que o caminho `'os'` ignorava a camada de preferência do ADR-0020(b) — em modo degradado, a voz do SO escolhida numa Persona finalmente é a que soa; em modo Piper-only, o fallback interno continua caindo na 1ª voz local (ADR-0021(c) intacto, nenhuma voz de rede em nenhum caminho). Nota de uma frase nas Observações do [ADR-0020](../06-adr/ADR-0020-persona-persistence-voice-binding.md) registra que a hierarquia (b) volta a valer também no renderer, no caminho degradado — em modo Piper-only ela segue *superseded* pela SPEC-0041/ADR-0021(c); Decisão de nenhum ADR é editada, nenhum ADR novo. Diff confinado a `apps/desktop/src/speech-output.ts`, `apps/desktop/src/renderer/renderer.js`, `apps/desktop/tests/speech-output.test.ts` e a nota do ADR-0020 — zero alteração em `packages/*`, `apps/cli`, `core-bridge.ts`, `piper-tts.ts`, `main.ts`, `preload.cjs` e `index.html`; nenhum canal IPC novo, nenhum campo novo em `Persona`. `renderer.js` segue sem cobertura automatizada (ADR-0019) — esta é a 7ª réplica renderer↔módulo do projeto, e a 2ª vez que uma dessas duplicações derivou por acidente (o próprio resíduo do glue). Suíte: **757 testes/62 arquivos** (era 745/62). Verificação verde: `pnpm lint`/`format:check`/`typecheck`/`test`. Desde a **SPEC-0044** (CLI: `atlas persona list|show|create|edit|delete`), Persona deixa de ser exclusiva da janela — a `apps/cli` ganha o equivalente de terminal do CRUD entregue pela SPEC-0039 na GUI, sobre **o mesmo arquivo** `personas.json`: o caminho é derivado uma única vez por `personaStoragePath(resolveDataDir(...))` de `@atlas/core`, nunca um `join` reinventado, então uma Persona criada no terminal aparece na janela e vice-versa, sem tradução. `apps/cli/src/gateway/persona-composition.ts` (novo) expõe `createCliPersonaStorage`/`createCliPersonaService`, compondo `createPersonaService({ storage })` diretamente no app — a mesma exceção delimitada de composição já usada por `apps/desktop` desde a SPEC-0039/D12 (`packages/core/CLAUDE.md`), sem nunca acionar `loadConfig` (achar o arquivo independe de a Persona ativa ser válida). Os subcomandos de `persona` (`apps/cli/src/commands/persona.ts`, novo) **não sobem o Core** — `run.ts` desvia para eles antes de `createAtlas`, evitando o deadlock circular B1 que a SPEC-0039/D17 já havia corrigido no desktop. Em contrapartida, **todo** comando restante (`status`/`ask`/`chat`/`remember`/`forget`/`memory`/`skills`) passa a injetar `personaStorage` em `createAtlas`, para que `--persona <id-custom>`/`ATLAS_PERSONA` resolvam Personas custom em qualquer comando — custo assumido e documentado (D10): o arranque de todo comando passa a ler `personas.json`, e um arquivo corrompido (fail-high por desenho, SPEC-0039/A6) derruba `status`/`ask`/`chat` também, mitigado só informacionalmente (mensagem citando o caminho + saída de emergência por `--data-dir`/conserto do arquivo). `edit` é patch (preserva campos não informados, inclusive `voiceURI`); `create` exige só o nome e preenche o resto com `''`/`[]` (paridade com `neutral`); `--rule` repetível substitui a lista inteira, `--rule ""` limpa; `--voice-uri` é string opaca (sem catálogo, sem validação, sem reprodução — a CLI não fala), com a ajuda declarando as formas aceitas (`piper:<id>` ou `voiceURI` de SO) sob a política Piper-only vigente (SPECs 0041/0043); `persona delete` exige consentimento explícito fail-closed (`[s/N]`/`--yes`, molde do `LineReader` da SPEC-0014), recusa sai com código 1. `jarvis`/`neutral` seguem embutidas, imutáveis e somente-leitura também pela CLI. Não há `atlas persona use` (seleção durável, fora por falta do slot `arquivo` do ADR-0006 — D2) nem guarda de Persona ativa na remoção (só aviso — D6). Em `@atlas/persona`, `createFilePersonaStorage.save` passa a gravar de forma **atômica** (arquivo temporário no mesmo diretório + `renameSync`), fechando o modo de falha que a SPEC-0039 havia aceitado só enquanto não houvesse consumidor de CLI; o lost-update lógico entre dois processos independentes (desktop load-once + CLI por invocação) permanece como limitação **ativa**, documentada em `packages/persona/CLAUDE.md` (mitigação prática: não editar Personas nas duas interfaces com a janela aberta ao mesmo tempo). Diff vazio em `packages/contracts`, `packages/core`, `packages/cognitive`, `packages/runtime`, `packages/tools`, `packages/memory`, `packages/context`, `packages/permissions`, `packages/skills`, `packages/model-gateway` e `apps/desktop` — nenhum contrato público muda, nenhuma decisão arquitetural nova. Suíte: **808 testes/64 arquivos** (era 757/62). Verificação verde: `pnpm lint`/`format:check`/`typecheck`/`test`. Desde a **SPEC-0045**, `renderer.js` deixa de ser o único arquivo de produção sem verificação automatizada: `apps/desktop/tests/helpers/renderer-harness.ts` (novo, sem `describe`/`it`) lê `src/renderer/index.html` e `src/renderer/renderer.js` **do disco** e os avalia com `jsdom` instanciado programaticamente (`runScripts: 'outside-only'`), com um epílogo de teste concatenado na avaliação — nunca um `export` acrescentado ao renderer — publicando os símbolos internos exercitados. Sobre esse harness, `renderer.speech-parity.test.ts` compara, com uma tabela de casos única por par, as **oito** réplicas de lógica hoje duplicadas entre `renderer.js` e `src/speech-output.ts` (mais a constante `PIPER_VOICE_PREFIX`, de origem real `piper-tts.ts`, não `speech-output.ts` — correção de um erro factual da própria SPEC, resolvida com um campo `moduleSource` no registro) e um **gate mecânico** falha caso `speech-output.ts` ganhe um export de valor novo não classificado como replicado (`NOT_MIRRORED`, com justificativa) nem coberto no registro; `renderer.boot.test.ts` cobre arranque sem exceção, todo `id` referenciado existente no `index.html` (extraído do fonte em runtime) e a fiação de envio do chat até `atlas.chat.send`; `renderer.voice-triggers.test.ts` cobre os dois gatilhos independentes de reavaliação de voz (`voiceschanged` e a resposta assíncrona do IPC de TTS) e a política Piper-only no `<select>`. O gate cobre só o eixo "export novo de `speech-output.ts` não classificado" — réplica vinda de outro módulo (ex.: `piper-tts.ts`) ou lógica nova escrita direto no renderer sem contraparte em TS segue protegida só por convenção documentada (limite registrado, não assumido em silêncio). `jsdom`/`@types/jsdom` entram como `devDependencies` só de `apps/desktop`; zero diff em `apps/desktop/src/` e em `packages/*`; ADR-0019 preservado literalmente (sem bundler, sem `type="module"` no renderer). Suíte: **867 testes/67 arquivos** (era 808/64). Verificação verde: `pnpm lint`/`format:check`/`typecheck`/`test`. Desde a **SPEC-0046**, consumindo o [ADR-0022](docs/06-adr/ADR-0022-whisper-cpp-local-stt-engine.md) (novo, Accepted), o desktop entrega a metade de **entrada** da voz — STT por push-to-talk: `apps/desktop/src/stt-engine.ts` (novo, espelho estrutural de `piper-tts.ts`) invoca `whisper.cpp` (`whisper-cli` v1.7.6) por subprocesso a cada transcrição, sem processo de longa duração (divergência deliberada do Piper — overhead de recarga é menor que sustentar o modelo na RAM pela sessão inteira); modelo default `ggml-small-q5_1.bin`, idioma fixado em PT-BR (`-l pt`), sem autodetecção; captura no renderer via `ScriptProcessorNode` (4096, 1×1) + `GainNode(0)` (sem `AudioWorkletNode`, para não introduzir script novo nem CSP nova); PCM `Int16` cruza o IPC como `{ pcm: ArrayBuffer, sampleRate }`, `durationMs` derivado no main; WAV montado no main; transcrição sempre anexada ao campo de entrada, nunca enviada automaticamente; nada de áudio/transcrição persistido; permissão de microfone decidida no main via `src/media-permission.ts` (novo, sem importar `electron`), fail-closed, com janela de captura explícita (`'atlas:stt:capture:begin'`/`':end'`, watchdog de 35 s, rearme quando `getUserMedia` resolve, sem carência, encerrada por 3 gatilhos de fechamento); tabela de dez desfechos exaustivos com `reason` pinado (incl. `invalid-audio`, `audio-too-long`, `io-failed`); gravação até 30 s, timeout de transcrição `clamp(20 s + 5 × duração, 20 s, 180 s)`, `SIGTERM`→2 s→`SIGKILL`; botão "Cancelar" visível também durante `transcrevendo`. Cinco canais IPC novos (`'atlas:stt:available'`/`':transcribe'`/`':cancel'`/`':capture:begin'`/`':capture:end'`), expostos em `window.atlas.stt.*` pelo `preload.cjs`. `apps/desktop/tests/helpers/renderer-harness.ts` ganha um relógio injetável na janela `jsdom` e dublês de mídia (`getUserMedia`/`AudioContext`/`ScriptProcessorNode`/`GainNode`/`MediaStreamAudioSourceNode`/`MediaStreamTrack`), sobre o harness da SPEC-0045. Zero diff em `packages/*`, `apps/cli`, `core-bridge.ts`, `speech-output.ts`, `piper-tts.ts`; nenhum tipo novo em `@atlas/contracts`; wake word segue explicitamente fora de escopo (ADR-0022, candidato futuro com ADR próprio). Suíte: **938 testes/70 arquivos** (era 867/67). Verificação verde: `pnpm lint`/`format:check`/`typecheck`/`test`. Nenhum microfone ou binário real exercitado no shell de automação — cobertura inteira por dublês, ampliando a pendência de smoke visual/sonoro desde a SPEC-0031 com o eixo de áudio de entrada. Desde a **SPEC-0047**, o gate mecânico de paridade renderer↔módulo (SPEC-0045) deixa de ser específico de `speech-output.ts` — `apps/desktop/tests/renderer.speech-parity.test.ts` passa a vigiar três módulos-fonte (`speech-output.ts`, `piper-tts.ts`, `stt-engine.ts`), enumerando em runtime (`import * as`) os exports de valor de cada um e exigindo que todos estejam classificados no registro de pares ou em `NOT_MIRRORED` (com `moduleSource` e justificativa); o registro ganha a 10ª entrada, `resolveDefaultPiperVoiceURI` (`piper-tts.ts`) ↔ `computeDefaultPiperVoiceURI` (`renderer.js`), com tabela de seis casos comparando as duas implementações sobre a mesma entrada. Duas provas negativas confirmaram o gate vermelho (mutação temporária do par e export não classificado em `piper-tts.ts`/`stt-engine.ts`), revertidas antes do commit — nenhuma sobrevive ao diff persistido. A mesma SPEC entrega cobertura comportamental dos cinco painéis do desktop sobre o harness da SPEC-0045, em quatro arquivos novos: `renderer.persona-crud.test.ts` (CRUD de Persona pelo formulário, com o caminho de recusa — listas recarregadas do estado real, transcript intacto — em pé de igualdade com o de sucesso), `renderer.permissions-panel.test.ts` (rascunho local, aplicar com sucesso/recusa), `renderer.memory-ask.test.ts` (esquecer fato, `ask` de tiro único) e `renderer.gesture-serialization.test.ts` (todos os dez controles nomeados desabilitados durante um turno de chat **ou** um `ask` em voo, reabilitados ao assentar — inclusive em falha). Achado registrado, não corrigido nesta SPEC: `#chat-send` calcula `disabled` sem somar `askInFlight` (`renderer.js:966-975`), então um turno de chat pode ser enviado com um `ask` ainda em voo. `apps/desktop/src/renderer/renderer.js` segue byte-idêntico ao pré-SPEC (o comentário "resíduo sem cobertura automatizada" sobre `computeDefaultPiperVoiceURI` fica deliberadamente desatualizado — CA de diff vazio em `src/` prevalece). Zero diff em `apps/desktop/src/`/`packages/*`; ADR-0019/0021/0022 intactos. Suíte: **969 testes/74 arquivos** (era 939/70). Verificação verde: `pnpm lint`/`format:check`/`typecheck`/`test`. Desde a **SPEC-0048**, fecha os dois resíduos nomeados pela SPEC-0047: em `refreshChatControlsForMic()`, `#chat-send` passa a calcular `disabled` como `chatTurnInFlight || askInFlight || micBusy()` (antes só `chatTurnInFlight || micBusy()`), e o manipulador de `submit` de `#chat-form` ganha uma guarda explícita (`if (chatTurnInFlight || askInFlight) return;`), porque o atributo `disabled` de um botão não impede `submit` despachado programaticamente ou por submissão implícita do `<form>`; o `.finally` do turno de chat deixa de atribuir `sendButton.disabled = micBusy()` diretamente, ficando `refreshChatControlsForMic()` (via `refreshPermissionsPanelState()`) como origem única do estado do botão. `#chat-input` permanece deliberadamente fora dessa serialização (D3): digitar não dispara round-trip contra o Core, e desabilitar o campo no meio de um `ask` roubaria foco/texto em curso. O comentário de `computeDefaultPiperVoiceURI` (`renderer.js:415-420`) é reescrito para citar `resolveDefaultPiperVoiceURI`/`renderer.speech-parity.test.ts` e deixa de dizer "resíduo sem cobertura automatizada"; o corpo da função sai byte-idêntico. `renderer.gesture-serialization.test.ts` ganha os dois casos exigidos (guarda de entrada durante um `ask` em voo; não-regressão do `.finally` do turno de chat) mais um caso de reforço do gate arquitetural (`ask` rejeitando reabilita `#chat-send`). Zero diff em `packages/*`/`apps/cli`/`core-bridge.ts`/`piper-tts.ts`/`stt-engine.ts`/`speech-output.ts`/`index.html`; ADR-0019/0021/0022 intactos. Duas direções residuais ficam deliberadamente fora (candidatos, não corrigidas): chat → ask (serializar `#ask-form` contra um turno de chat em voo) e ask × ask (`#ask-form`/`#objective`/`#ask-submit` não têm guarda nem entram em função de refresh alguma — dois cliques seguidos em "Perguntar" sobem dois Cores concorrentes). Suíte: **972 testes/74 arquivos** (era 969/74). Verificação verde: `pnpm lint`/`format:check`/`typecheck`/`test`. Desde a **SPEC-0049**, as duas direções residuais fecham: o botão de submissão de `#ask-form` ganha `id="ask-submit"` (era o único controle do painel sem `id`) e uma função `refreshAskControls()` (calcula `disabled` a partir de `chatTurnInFlight || askInFlight`, chamada por `refreshPermissionsPanelState()`); o manipulador de `submit` de `#ask-form` ganha guarda de entrada equivalente (`if (chatTurnInFlight || askInFlight) return;`, recusa silenciosa — D4) e um `.catch` entre o `.then` e o `.finally`, escrevendo `⚠️ <mensagem>` no `#ask-result` em vez de deixar a rejeição virar unhandled rejection. `#objective` permanece deliberadamente fora da serialização (D3, mesmo padrão de `#chat-input`). O helper `withSuppressedUnhandledRejection` de `renderer.gesture-serialization.test.ts` foi removido (deixou de ser necessário); o homônimo de `renderer.memory-ask.test.ts` permanece (cobre o manipulador de "Esquecer", que segue sem `.catch` — D6). O único resíduo remanescente da lista da SPEC-0048 é `sendChatTurn`/`resolveAskSnapshot` seguirem fora de `inFlightOperations` no `core-bridge` (D7 — serialização é garantia do renderer, não estrutural no main process). Zero diff em `packages/*`/`apps/cli`/`core-bridge.ts`; ADR-0019/0021/0022 intactos. Suíte: **978 testes/74 arquivos** (era 972/74). Verificação verde: `pnpm lint`/`format:check`/`typecheck`/`test`. Desde a **SPEC-0050**, esse resíduo D7 fecha: `resolveAskSnapshot` e `sendChatTurn` no `core-bridge` passam a **ler** `hasInFlightOperation()` como guarda de entrada — não só marcar —, antes de qualquer incremento/`await` e antes de tocar `busySessions`, recusando um `ask`/turno disparado durante outro `ask`, durante um turno de chat, ou durante a janela de abertura de `openChatSession`; uma recusa não altera o contador e, no caso de `sendChatTurn`, não derruba a sessão viva. `hasInFlightOperation()` passa a ter **quatro consumidores nominais / cinco chamadas** (`updatePersona`, `selectPermissionRoots` ×2, `resolveAskSnapshot`, `sendChatTurn`), as três chamadas pré-existentes preservadas byte-a-byte; `selectPersona` segue com condição parcial (`busySessions` inline, sem `inFlightOperations`), registrada como assimetria pré-existente e candidato a uniformização (D12), não corrigida por esta SPEC. As guardas do renderer (SPECs 0048/0049) permanecem intactas como caminho normal — a garantia estrutural nova é rede de segurança para todo caminho que não passa pelo `<form>`. Zero diff em `packages/*`/`apps/cli`/renderer/`main.ts`/`preload.cjs`; ADR-0019/0013/0009 intactos. Suíte: **983 testes/74 arquivos** (era 978/74, delta de arquivos = 0). Verificação verde: `pnpm lint`/`format:check`/`typecheck`/`test`. Desde a **SPEC-0051**, o desktop ganha um gesto de escape: um botão "Cancelar" por painel (`#ask-cancel`/`#chat-cancel`) chama `window.atlas.cancel()` → `'atlas:cancel'` → `cancelInFlightOperation()` (síncrona, `{ cancelled: boolean }`, idempotente), que marca como **abandonada** a operação cancelável em voo (`ask`/turno de chat — nunca `openChatSession`) e faz a promessa do gesto rejeitar **na hora**, com mensagem pinada. `busySessions`/`inFlightOperations` são substituídos por **um** registro `Set<OperationRecord>` e **quatro predicados nomeados**: `hasInFlightOperation()` (segurança, inclui abandonadas — `updatePersona`/`selectPermissionRoots` ×2), `hasActiveOperation()` (conversação, ignora abandonadas — guarda de entrada de `resolveAskSnapshot`/`sendChatTurn`), `hasAbandonedTurnForSession(session)` (quarentena de sessão — recusa turno novo na sessão afetada, nega o `ConfirmPort` daquela sessão sem abrir diálogo) e `hasChatTurnOperation()` (condição parcial preservada de `selectPersona`). O trabalho abandonado **continua rodando** até assentar sozinho — nenhum `learned`/conversa é persistido dele, o `shutdown()` do `ask` acontece normalmente, e a rejeição é sempre absorvida (nunca *unhandled rejection*). O `ConfirmPort` do chat é envolvido **por sessão** (injetado uma vez em `openChatSession`, alcance real da porta), pegajoso enquanto houver turno abandonado não assentado; o do `ask`, por operação. Sem cancelamento real dentro do Core (fora de escopo, dono previsto Runtime/Task Manager, candidato nomeado com ADR pendente). Zero diff em `packages/*`/`apps/cli`. Suíte: **1001 testes/75 arquivos** (era 983/74). Verificação verde: `pnpm lint`/`format:check`/`typecheck`/`test`. Desde a **SPEC-0052**, o desktop ganha o modo hands-free (conversa por voz contínua), consumindo o [ADR-0023](../06-adr/ADR-0023-hands-free-voice-conversation.md) (novo, `Accepted`, que supersede parcialmente o ADR-0022 — só a cláusula de auto-envio de *Alternativas Consideradas*, só dentro do modo): um toggle único abre o microfone e o mantém aberto entre turnos; `src/hands-free.ts` (novo, módulo puro) define `nextHandsFreeState` (9 estados × 18 eventos, exaustiva, regra de fecho "evento não previsto ⇒ estado inalterado"), `createTurnSegmenter` (fim de fala por VAD, sem conhecer fila/buffer de áudio), `handsFreeMicrophoneOpen` (predicado único da invariante do ADR-0023(e)) e `speakingWatchdogMs`. O detector é o Silero VAD v5 sobre `onnxruntime-web` v1.20.1, wasm-only, rodando em WASM no renderer — primeira inferência do projeto fora do main process, exceção explícita e delimitada ao ADR-0022(a)/ADR-0023(d); atrás de porta injetável (`VoiceActivityDetector`) com ponto de criação único no glue (`src/vad-resources.ts`, novo, resolve os artefatos do modelo por `ArrayBuffer` via dois canais IPC novos — `'atlas:vad:available'`/`'atlas:vad:resources'` — nunca por `fetch`). CSP ganha `'wasm-unsafe-eval'` em `script-src`, e nada mais é afrouxado. Fim de fala: 3 s contínuos de ausência de voz. A transcrição segue **direto ao Core, sem revisão** — auto-envio pelo mesmo `submit` de `#chat-form` que os demais gestos já usam, nunca por chamada direta; o `ConfirmPort` (ADR-0013) permanece **intocado** (diff vazio verificado por CA), o que sustenta a distinção "auto-envio ≠ execução não autorizada" do Artigo 8. O microfone fica **comprovadamente fechado** em `transcribing`/`sending`/`thinking`/`speaking` (invariante do ADR-0023(e)); desligar o toggle é o freio, alcançável em qualquer um dos 9 estados, aciona `cancelInFlightOperation()` (SPEC-0051) com turno em voo. Sem barge-in — decisão de produto do ADR-0023, não resíduo. Novos: `src/hands-free.ts`, `src/vad-resources.ts`, `src/renderer/vendor/vad/` (runtime/modelo não versionados). Zero módulo novo, zero Tool nova, zero campo novo em `Persona`, zero diff em `@atlas/contracts`/`packages/*` — toda a fatia vive nos Gateways de `apps/desktop`. Suíte: **1303 testes/79 arquivos** (era 1001/75). Verificação verde: `pnpm lint`/`format:check`/`typecheck`/`test`. Desde a **SPEC-0053**, o desktop substitui integralmente o layout de duas colunas (v1.x/v2.0) por uma reformulação visual v3.0, redirecionada depois de a v2.0 ter passado por inteiro nos quatro gates técnicos e reprovado no smoke visual humano (sidebar/trilho e timeline permanentes competindo com a presença, painel de Memória sobrepondo/cortando conteúdo, esfera CSS-only sem volume). A área principal vira uma única pilha central — núcleo, estado/resposta, voz, `#chat-form` — sem sidebar, trilho ou timeline permanente; toda gerência lateral (Persona, Personas, Memória, Permissões, Objetivo, Sessão) migra para um drawer overlay (`#menu-toggle`/`#panel-drawer`, `role="dialog"`, foco contido, zero-ou-um painel via `data-drawer-nav`) que não desloca nem redimensiona o núcleo; Sessão absorve a timeline por inteiro (`#timeline-region`/`#chat-transcript`/`#timeline-detail`, protocolo de eventos e rolagem condicional preservados da v2.0). Progressive disclosure determinístico substitui sobreposição/corte em Memória (título por regra 72/69+reticências), Persona (`#persona-status-toggle`), Personas (`#persona-form`), Objetivo (`#ask-result-toggle`) e Permissões (`#read-roots-toggle`/`#write-roots-toggle`). O núcleo passa a ser uma esfera holográfica volumétrica real: nuvem determinística de 400 pontos (320 Fibonacci de superfície + 80 internos por `mulberry32(0x0a71a5)`, digest SHA-256 pinado, quatro sentinelas, sem `Math.random`) renderizada em Canvas 2D nativo (`#presence-canvas`, sem WebGL/lib nova) com projeção 3D, depth-sort e reação a `pointermove` (±12°); sete perfis fecham `#presence-core[data-state]` (`ready`/`booting`/`listening`/`transcribing`/`thinking`/`speaking`/`error`) sobre a mesma origem de sinais já emitida pelas SPECs 0040/0046/0052 — `speaking` só ativa em `<audio>.playing`/`SpeechSynthesisUtterance.onstart` reais (`playbackPending`/`playbackActive`), nunca por clique/`canplay`/estado isolado do hands-free; um único `requestAnimationFrame`, com `prefers-reduced-motion: reduce` cancelando o loop e desenhando um frame estático por estado. Paleta roxo-realeza por custom properties (variantes clara/escura por `prefers-color-scheme`), zero emoji em rótulo/controle. Zero canal IPC, contrato, dependência ou módulo novo; zero diff fora de `apps/desktop/src/renderer/{index.html,styles.css,renderer.js}` e `apps/desktop/tests/renderer.layout.test.ts`; `core-bridge.ts`/`main.ts`/`preload.cjs`/`packages/*`/`apps/cli` intactos. É a primeira vez, em ~20 fatias visuais consecutivas do desktop, que o smoke visual humano roda de fato numa janela real e confirma — todos os 15 itens do smoke da v3.0 vieram `OK`, depois de a v2.0 ter sido reprovada na mesma verificação. Suíte: **1353 testes/80 arquivos** (era 1303/79). Verificação verde: `pnpm lint`/`format:check`/`typecheck`/`test`. Desde a **SPEC-0054**, consumindo o [ADR-0024](../06-adr/ADR-0024-desktop-host-resource-metrics.md) e o [ADR-0025](../06-adr/ADR-0025-desktop-token-usage-accounting.md) (ambos novos, `Accepted`, que delegaram o contrato técnico exato à SPEC), o desktop ganha um sétimo e último painel do drawer, `Sistema` — observabilidade do ambiente, requisito acrescentado ao PRD em 2026-08-18. `@atlas/contracts` ganha `TokenUsage` (`promptTokens?`/`completionTokens?`/`totalTokens?`, forma fixada pelo ADR-0025(a)) e `GenerateResult.usage?`/`AskResult.usage?`/`ConversationTurn.usage?`, todos aditivos/opcionais, no mesmo molde de `steps?`/`learned?`. Os três providers do `@atlas/model-gateway` passam a preencher `usage` a partir do que a própria resposta já traz (`remote`: `prompt_tokens`/`completion_tokens`/`total_tokens` do formato OpenAI-compatible, sem derivar `totalTokens`; `ollama`: `prompt_eval_count`/`eval_count`, `totalTokens` derivado como soma; `fake`: `countWords` determinístico), sem nenhuma chamada de rede nova. O `@atlas/cognitive` soma, campo a campo, o `usage` de todas as `generate` de um turno (planejamento, cada replanejamento, composição, extração) por um helper puro interno (`sumUsage`) e devolve a soma como dado em `AskResult`/`ConversationTurn`, nunca derivando `totalTokens` nessa camada. No `apps/desktop` (main process), dois módulos novos: `system-metrics.ts` (`createSystemMetrics`, lê CPU/memória/GPU/rede via `systeminformation`, recebida só por porta injetável `SystemInformationPort` — a lib só é importada em `main.ts` —, fail-closed por métrica com três razões nomeadas `unsupported`/`read-failed`/`timeout`, orçamento de 2500 ms por leitura, as quatro leituras em paralelo, sem cache/timer residente) e `token-usage.ts` (`createTokenUsageAccumulator`, acumulador em memória de tokens da sessão corrente, `promptTokens`/`completionTokens`/`totalTokens`/`reportedTurns`/`unreportedTurns`, resetado em `openChatSession` bem-sucedida e por `__resetBridgeStateForTests`). `core-bridge.ts` ganha uma instância única do acumulador e `readTokenUsage()` (síncrona, sem subir Core); soma em `resolveAskSnapshot`/`sendChatTurn` **inclusive para operação abandonada** (SPEC-0051 — o gasto já ocorreu; só os efeitos de domínio continuam descartados). Dois canais IPC novos, um por módulo (`'atlas:metrics:read'`/`'atlas:tokens:read'`, expostos em `window.atlas.metrics.read`/`window.atlas.tokens.read`). O painel `Sistema` (`#panel-system`, sétimo e último item do drawer, manifesto de **86** IDs estáticos) mostra relógio (data com dia da semana, hora, tick de 1 s sem `Intl`), tokens da sessão e as quatro métricas de host, atualizados a cada dois ticks (2000 ms) por um único `setInterval` criado só enquanto o painel está visível, sem reentrância, com formatação pinada (separador decimal `,`, milhar `.`, três textos de indisponibilidade nomeados, quatro desfechos exaustivos para tokens); erro de leitura fica no painel, nunca em `#global-alert`/`data-state="error"`. `renderer.speech-parity.test.ts` passa a vigiar **seis** módulos-fonte (soma `system-metrics.ts`/`token-usage.ts`, ambos sem réplica no renderer — zero réplica nova). Zero módulo/Tool/Skill/Persona novo, zero canal IPC além dos dois nomeados, zero mudança em `apps/cli`/`@atlas/runtime`/`@atlas/tools`/`@atlas/permissions`/`@atlas/context`/`@atlas/memory`/`@atlas/persona`/`@atlas/skills`, layout v3.0 (SPEC-0053) intacto. Suíte: **1461 testes/84 arquivos** (era 1353/80). Verificação verde: `pnpm lint`/`format:check`/`typecheck`/`test`. A **SPEC-0055** entregou a primeira Tool de rede do Atlas, `http_get` (`packages/tools/src/http-get.ts`/`http-port.ts`, novos), consumindo o [ADR-0026](../06-adr/ADR-0026-network-access-gate.md) (novo, `Accepted`, aberto por escalação do `spec-drafter` — um host/URL não tem "raiz" a conter): `@atlas/contracts` ganha `ResourceType: 'network'` e `ResourceRef` como união discriminada (`{ type:'file'|'directory', path }` | `{ type:'network', host }`), e `AtlasConfig`/`AtlasConfigOverride.permissions` ganham `netRoots` (allowlist de hostname, obrigatória na config resolvida, default `[]`, fail-closed). `@atlas/permissions` roteia `evaluate` por `resource.type` — FS inalterado, rede julga `host` contra `netRoots` por igualdade exata (`trim`+`toLowerCase`, sem wildcard, sem validação de IP/DNS); `isContained` permanece FS-específico, sem rota de rede (ADR-0026(b)). `HttpPort`/`nodeHttpPort` (interna a `@atlas/tools`, sem 2º consumidor real) chama `fetch` com init exaustivo `{ method:'GET', redirect:'manual', signal }` — sem header/cookie/credencial repassados —, timeout de 10 s, corpo truncado em 64 KiB, corpo não textual omitido, e uma guarda fail-closed de redirect opaco (D18: `status===0`/`type==='opaqueredirect'`, comportamento não-spec do undici embutido no Node 24, pinado como dado). `requirements()`/`run()` derivam host/URL do mesmo helper (`resolveHttpTarget`, `url = new URL(args.url).href`), nunca `null`; qualquer status HTTP recebido (inclusive 4xx/5xx) é `ok: true`, 3xx expõe `location` sem seguir o redirect; URL com credenciais embutidas é recusada. A CLI ganha `--allow-net <host>` (repetível) + `ATLAS_ALLOW_NET` (lista por vírgula, não `path.delimiter`), mesma precedência do ADR-0006; `atlas status` passa a exibir `netRoots`. `apps/desktop` fica deliberadamente sem política de rede nesta fatia (fail-closed, nenhuma UI nova; só narrowing de tipo nos três sites de `ConfirmPort`). Não fecha o item 1.4 do Roadmap (seguem candidatas: execução de comandos, leitura de estrutura de projeto, Anthropic nativo, config por arquivo, Event Bus/Plugin Manager). Residuais registrados e não fechados: a URL é um canal de saída de dados que o portão não julga (avalia só o host, nunca o path/query string — combinado com `--allow-read` já concedido, é o primeiro caminho de exfiltração da plataforma) e a injeção indireta de prompt pelo corpo remoto (primeira vez que texto de terceiro não confiável influencia a decisão do modelo); a assimetria com `@atlas/model-gateway` (que já fazia egress via `fetch` sem `evaluate`/`netRoots` desde a SPEC-0004) fica registrada — `netRoots` descreve a política de Tools, não de todo o tráfego do processo. Zero diff em `@atlas/cognitive`/`@atlas/skills`/`@atlas/memory`/`@atlas/context`/`@atlas/persona`/`@atlas/model-gateway`/`apps/desktop/src/renderer/*`, zero dependência de runtime nova. Suíte: **1546 testes/88 arquivos** (era 1461/84). Verificação verde: `pnpm lint`/`format:check`/`typecheck`/`test`. A **SPEC-0056** entregou a segunda fatia do item 1.4 — leitura de estrutura de projeto: Tool `project_info` (`packages/tools/src/project-info.ts`, novo) descreve a raiz de um projeto de software, os manifests presentes (tabela fixa e pura de 16 nomes de arquivo → ecossistema, `packages/tools/src/project-manifests.ts`, novo) e os scripts declarados no `package.json` (até 50 nomes, truncagem sinalizada fora da lista, nunca por marcador dentro dela). Reusa a descoberta de toplevel da SPEC-0028: `GitReadPort` ganha o método aditivo `toplevel(cwd)`, delegando à `resolveRepository` já existente em `git-port.ts`, sem duplicar a barreira `verify`; as três rejeições passam a lançar `GitRootError` (novo, interno a `git-port.ts`, não exportado por `index.ts`), com `reason: 'no-repository' | 'resolve-failed' | 'denied'`, permitindo à Tool classificar a falha sem casar substring de mensagem (mensagens de hoje preservadas caractere a caractere). A ascensão ao repositório git só ocorre quando `path` é **omitido** (alvo derivado do `cwd()`); `path` explícito é honrado como o alvo final, sem ascender — é o que permite descrever um subpacote de monorepo sem subir ao repositório inteiro. Quando não há repositório aplicável (não é repo, git ausente, toplevel fora das `readRoots`), a raiz degrada para o diretório-alvo, com `origem da raiz` sempre visível na saída, escolhida por um conjunto **fechado** de cinco strings literais (nunca interpolando path descoberto nem stderr do git — saída determinística). O helper de alvo `git-target.ts`/`resolveGitTarget` foi renomeado mecanicamente para `target-dir.ts`/`resolveTargetDirectory` (2º consumidor não-git, mesma assinatura e comportamento); `git-status.ts`/`git-diff.ts`/`git-log.ts` só trocam o import. `@atlas/core` registra `project_info` reusando as portas `fsRead`/`git` já compostas em `createAtlas` — nenhuma porta, config, flag ou dependência nova. Zero diff em `@atlas/contracts`/`packages/permissions`/`packages/runtime`/`apps/cli`/`apps/desktop`. Não fecha o item 1.4 do Roadmap — resta como única fatia candidata a execução de comandos sob o Permission Service (`ADR primeiro`). Residuais registrados e não fechados: `readdir` sem fecho atômico de TOCTOU, troca de ancestral do diretório-alvo, a raiz efetiva pode ser um ancestral do recurso declarado só no caminho ascendente (fechado pela segunda barreira `verify` sobre o toplevel), a degradação não diz qual repositório foi recusado, sem teto de tamanho na leitura do `package.json`, conteúdo de terceiro (nomes de script) entra no prompt, tabela de manifests com cobertura parcial (Swift/.NET/Dart/Nix fora), Windows fora de escopo, um `path` supérfluo do Planner desliga a ascensão silenciosamente. Suíte: **1592 testes/90 arquivos** (era 1546/88). Verificação verde: `pnpm lint`/`format:check`/`typecheck`/`test`. A **SPEC-0058** endureceu a composição de saídas de Tools no prompt do `@atlas/cognitive` — mitigação genérica de injeção indireta de prompt, aplicável a todas as 13 Tools existentes hoje e a qualquer Tool futura, sem uma linha em `@atlas/tools`: módulo puro novo `packages/cognitive/src/tool-output.ts` (não exportado por `index.ts`) delimita cada resultado de Tool num bloco `<tool_output id="N"> … </tool_output>` (`formatToolBlock`/`formatResults`/`summarizeFailures`, movidos de `cognitive-core.ts`), com o fechamento neutralizado contra forja (`neutralizeFence`, `<` → `‹`, 1:1 em caracteres) e teto determinístico por passo (`TOOL_OUTPUT_CHAR_LIMIT = 8_000` em `output`/`error`, `TOOL_ARGS_CHAR_LIMIT = 500` no `args` ecoado, ordem fixa truncar → neutralizar); uma mensagem `system` dedicada (`UNTRUSTED_TOOL_OUTPUT_FRAMING`) instrui o modelo a tratar o conteúdo dos blocos como dado, nunca como ordem — inserida só quando há texto de Tool, tanto na composição (`ask`/`respond`) quanto no replanejamento. Nada disso muda o que o usuário vê: `AskResult.steps`/`ConversationTurn.steps` continuam com o `ExecutedStep` cru, e `TASK_FRAMING`/`compose()`/a ordem Persona→Memória→Skill→Tarefa saem sem uma linha alterada. Sem ADR novo (mudança interna de conteúdo de string e número de mensagens `system`, `Message`/`role` de `@atlas/contracts` intactos). É mitigação, não fronteira: o residual de injeção indireta do ADR-0026/SPEC-0055 continua aberto e documentado como tal; o memo de continuidade `summarizeSteps` (persistido na `Conversation`, reentra em todo turno seguinte por causa de `withFreshSystemHead`) recebe só teto e neutralização, sem bloco nem instrução — residual conhecido e deliberadamente não coberto (fechá-lo exigiria um canal estrutural `role: 'tool'`, ADR próprio). Não fecha nenhum item do Roadmap; desbloqueia a retomada da SPEC-0057 (`web_search`, `Ready`, pausada). Zero diff fora de `packages/cognitive/src`/`tests`. Suíte: **1636 testes/91 arquivos** (era 1592/90). Verificação verde: `pnpm lint`/`format:check`/`typecheck`/`test`. A **SPEC-0057** entregou a terceira fatia do item 1.4 (Acesso à internet) — Tool `web_search` (`@atlas/tools`), busca em texto livre sobre a porta injetável `SearchPort` (`endpointUrl` publicado pela própria porta, fonte única do host declarado em `requirements` **e** do host de fato requisitado, D21 — evita as duas fontes independentes que a 1ª revisão vetou), com adaptador default `searxngSearchPort` construído sobre o `HttpPort` já endurecido da SPEC-0055 (`nodeHttpPort` ganha `bodyLimitBytes?` aditivo, default 64 KiB inalterado; a busca usa 256 KiB, decidido só dentro do adaptador, D24). Provedor **sem credencial**, compatível com a API JSON do SearXNG, configurado por `--search-url <url>`/`ATLAS_SEARCH_URL` (`AtlasConfig.tools.searchUrl`, namespace de módulo novo e obrigatório em `@atlas/contracts`, `''` = não configurado); sem endpoint configurado, `web_search` não é registrada no Tool Registry (`atlas status` mostra os dois estados). O host do provedor é julgado pelo mesmo portão `netRoots` do ADR-0026, sem nenhuma cláusula reaberta — diff vazio em `@atlas/permissions`/`@atlas/runtime`/`@atlas/cognitive`/`@atlas/skills`/`@atlas/memory`/`@atlas/context`/`@atlas/persona`/`@atlas/model-gateway`/`apps/desktop`. `parseSearchPayload` (função pura) valida, normaliza, deduplica por URL e conta descartes (`discarded`, nunca silenciosos, D25) antes de truncar em `SEARCH_MAX_RESULTS = 10`. Não fecha o item 1.4 do Roadmap — resta só a execução de comandos sob o Permission Service (`ADR primeiro`). Residuais registrados e não fechados: a consulta é um canal de saída de dados que o portão não julga (2ª instância do residual 10 da SPEC-0055, agora por um caminho novo), injeção indireta de prompt amplificada por até 10 resultados de terceiros por busca sem que o modelo tenha escolhido a fonte (residual 11, mitigada de forma genérica pela SPEC-0058 mas não fechada), acoplamento de formato ao SearXNG confinado à `SearchPort` (substituibilidade, não neutralidade de provedor), busca indisponível na GUI (`apps/desktop` segue sem `netRoots`/`tools.searchUrl`, D15). Suíte: **1728 testes/93 arquivos** (era 1636/91). Verificação verde: `pnpm lint`/`format:check`/`typecheck`/`test`. A SPEC-0059 (2026-08-22) entregou o painel de rede e busca do desktop — consumindo o [ADR-0026](../06-adr/ADR-0026-network-access-gate.md) sem reabrir nenhuma cláusula (a)–(e), `apps/desktop` ganha `network-grant-dialog.ts` (quarta porta fail-closed) e `selectNetworkAccess`/`selectedNetworkAccess` (`core-bridge.ts`), gesto de aplicação **próprio** e distinto de `selectPermissionRoots` (dois eixos de política, D2), duas seções novas (Rede/Busca) dentro de `#panel-permissions` (sem oitavo item no drawer, D1), canal IPC `'atlas:network:select'`. Consentimento por host **novo** (comparação case-insensitive contra a seleção corrente, D6), nunca por Tool — o diálogo nomeia `http_get`/`web_search` só para tornar o alcance concreto (D18). Composição de `AtlasConfigOverride` centralizada num único helper (`composeOverride`, D5/D9), consumido tanto por `withSelections` quanto pelo dry-run de validação em `loadConfig` — nunca um literal montado à mão. Mutex de aplicação de política (D16) compartilhado entre `selectNetworkAccess` e `selectPermissionRoots` (nunca dois diálogos de consentimento empilhados); rascunho de rede preservado em qualquer rejeição, distinto do bloco de FS que recarrega do status (D17, assimetria deliberada). Seleção é estado de sessão da app, nunca persistida (Artigo 11); fechar a app volta a `netRoots: []`/`searchUrl: ''`. Diff vazio em `packages/*`/`apps/cli`; fecha o residual nomeado "painel de rede/busca na GUI" das SPECs 0055/0057 (nota `# Atualização (SPEC-0059)` no ADR-0026), sem fechar o item 1.4 do Roadmap. Suíte: **1772 testes/96 arquivos** (era 1728/93). Smoke manual em janela real pendente de confirmação humana (sem WindowServer neste ambiente, mesma pendência estrutural já registrada para outras fatias visuais do desktop). Detalhe: `apps/desktop/CLAUDE.md`.
