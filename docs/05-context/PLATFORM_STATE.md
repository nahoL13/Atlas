# Estado da plataforma

> **Project Atlas — Estado vivo detalhado da plataforma**
>
> Movido do `CLAUDE.md` raiz em 2026-07-20 para reduzir o contexto carregado em toda sessão.
> Mantido pela skill `doc-sync` no fechamento de cada SPEC. O `CLAUDE.md` raiz guarda só o resumo.

**Estado em julho/2026: interface executável (SPEC-0003) + acesso a modelos de IA (SPEC-0004) + primeira resposta cognitiva ponta a ponta (SPEC-0005) + conversa interativa multi-turno (SPEC-0006) + Context Service (SPEC-0007) + Persona Service (SPEC-0008) + Memory Service (SPEC-0009) + espinha de execução Planner/Runtime/Tools (SPEC-0010) + Permission Service e primeiras Tools de leitura de sistema de arquivos (SPEC-0011) + primeira Tool de escrita (`write_file`) sob política `writeRoots` opt-in (SPEC-0012) + fluxo interativo `confirm` real + Tools destrutivas `delete_file`/`mkdir`/`append_file` (SPEC-0013) + Tools e `confirm` também em `atlas chat` (SPEC-0014, `respond` passou a orquestrar Planejamento + Execução como `ask`) + endurecimento de symlink no Permission Service via `realpath` (SPEC-0015, `evaluate` segue síncrono) + primeira infraestrutura de desenvolvimento: repositório publicado num remote privado no GitHub (`nahoL13/Atlas`) + CI em `.github/workflows/ci.yml` validando lint/typecheck/test/format:check a cada push/PR (SPEC-0016; runner `ubuntu-latest`/Node 24/pnpm via `pnpm/action-setup`, **não** corepack; sem tocar nenhum módulo de produto) + fecho atômico da janela TOCTOU do componente final em `read_file`/`write_file`/`append_file` (SPEC-0017, consumindo o [ADR-0014](docs/06-adr/ADR-0014-toctou-atomic-enforcement.md): as portas de IO abrem o fd com `O_NOFOLLOW`, ancoram na identidade do fd (`fstat` × `stat(realpath)`) e aplicam sobre o fd um veredicto de contenção do Permission Service — método novo `isContained(canonicalPath, access)`, puro/síncrono; `evaluate` segue intacto como pré-check e o Runtime inalterado) + múltiplas raízes de leitura/escrita na borda da CLI (SPEC-0018: `--allow-read`/`--allow-write` viraram flags repetíveis e `ATLAS_ALLOW_READ`/`ATLAS_ALLOW_WRITE` aceitam lista por `path.delimiter`; o núcleo já avaliava listas — a fatia foi só `apps/cli/src/gateway/input-gateway.ts`, zero linha em packages) + Observação (Etapa 5 do ciclo cognitivo) fechada como laço de replanejamento determinístico no Cognitive Core (SPEC-0019, consumindo o [ADR-0015](docs/06-adr/ADR-0015-observation-replan-loop.md)): o `runPlanCycle` — compartilhado por `ask`/`respond` — deixou de ser passe único e virou laço com teto fixo (**1 replanejamento**); a função pura `observe(executionResult)` decide `replan` (há passo `ok:false` **sem** `denialKind` = falha de Tool) × `complete` (bloqueio de permissão e recusa no `confirm` são terminais), consumindo o campo novo `ExecutedStep.denialKind?: 'blocked' | 'declined'` (única mudança em `@atlas/contracts`, rotulado pelo Runtime nos 2 branches de negação; ferramenta desconhecida e TOCTOU-no-uso ficam **sem** `denialKind` → replan); `steps` acumulam todos os passes; falha de Tool replaneja com resumo compacto das falhas; Planner/Permissões/Tools/Model Gateway intactos + Aprendizado (Etapa 6 do ciclo cognitivo) fechado como extração pós-turno (SPEC-0020, consumindo o [ADR-0016](docs/06-adr/ADR-0016-learning-proposed-extraction.md)) — **o Cognitive Lifecycle está implementado nas sete etapas**: um `learner` **puro** novo em `@atlas/cognitive` (`learner.ts`, molde do Planner/Observer: `instruction()` restrita ao Artigo 13 — só fatos afirmados/fortemente implicados pelo usuário, nunca inferidos — + `parse(saída) → readonly string[]`, teto fixo = 3 por turno, tipo interno) decide o que preservar; após a resposta final de cada turno (com e sem plano, em `ask` **e** `respond`), o `runPlanCycle` faz +1 `generate()` dedicada de extração (falha → nada aprendido, o turno nunca quebra); o Cognitive **propõe** os candidatos como dado (`AskResult.learned?`/`ConversationTurn.learned?`, aditivos) e **a borda grava e avisa**: `atlas ask`/`atlas chat` gravam via `atlas.memory.remember(texto, 'learned')` e imprimem `💡 lembrado: <fato>`; proveniência `Fact.source?: 'user' | 'learned'` (aditivo, `remember` ganha parâmetro opcional com default `'user'`), `atlas memory list` exibe a origem; Cognitive segue puro/sem estado, Memory mantém autoridade exclusiva de gravação; Planner/Observer/Runtime/Permissões/Tools/Model Gateway intactos + recomposição ao vivo do `memoryPrompt` no Cognitive Core (SPEC-0021, consumindo o [ADR-0011](docs/06-adr/ADR-0011-memory-service-persistence.md) — notas de atualização nos ADR-0008/0011/0016, sem ADR novo): `CognitiveCoreDeps.memoryPrompt` deixou de ser `string?` e passou a ser um **provider síncrono** interno — `() => string | undefined` (não sobe a `@atlas/contracts`) — **amostrado exatamente 1x por turno** no início de `ask` e de `respond`; o `systemPrompt` do turno (`compose(memoryValue) = [personaPrompt, memoryValue, TASK_FRAMING]`) é compartilhado por todas as `generate` do turno (planejamento, replanejamento, composição, extração); só a fatia de memória é recomposta (`personaPrompt`/`TASK_FRAMING` seguem estáticos). Em `respond`, o helper puro `withFreshSystemHead` **substitui** o conteúdo da primeira mensagem `system` da `Conversation` (ou **insere** uma no topo quando não houver) tanto nas mensagens enviadas ao modelo **quanto** na `Conversation` retornada (fonte única, sem prompt morto); demais mensagens `system` intactas; `respond` segue **puro** e o Core sem estado. A chamada de extração passa a **incluir os fatos já conhecidos** (`learner.instruction(knownFacts?)`) com instrução de **não os re-propor** — supressão da duplicação intra-sessão **por instrução ao modelo, sem garantia determinística** (não é dedup programático; Artigo 13 intacto). `@atlas/core` fia `memoryPrompt: () => memory.prompt()` (provider sempre definido; spread condicional removido, `exactOptionalPropertyTypes` satisfeito). **Fecha o laço de aprendizado dentro da própria sessão**: fato gravado no turno N (por `atlas remember` ou pela borda do Aprendizado) entra no `systemPrompt` do turno N+1, sem depender de nova invocação da CLI — resolvendo a limitação documentada da SPEC-0020. **Zero mudança em `@atlas/contracts` e em `apps/cli`** (diff vazio); deduplicação determinística segue fatia futura documentada. Planner/Observer/Runtime/Permissões/Tools/Model Gateway/Persona/Context/Memory intactos.** + deduplicação **determinística** dos fatos aprendidos, fechando essa mesma fatia futura (SPEC-0022, consumindo o [ADR-0011](docs/06-adr/ADR-0011-memory-service-persistence.md) por nota de atualização, sem ADR novo): `MemoryService.remember` normaliza o texto por um helper puro interno `normalize(text)` (`trim` → `toLowerCase` → colapso de `\s+`, sem IO, não sobe a `@atlas/contracts`) e, ao reconhecer duplicata (mesma normalização de um fato já carregado), faz um **no-op idempotente** — não cria `Fact`, não chama `storage.save`, não promove `source` — devolvendo o `Fact` existente; vale para todo chamador (`'user'` e `'learned'`). A assinatura de `remember(text, source?)` muda (**não-aditiva**) de `Promise<Fact>` para `Promise<{ fact: Fact; created: boolean }>`, sinalizando criado × existente; `ask.ts`/`chat.ts` só chamam `renderLearned` (`💡 lembrado:`) quando `created`, e `atlas remember` distingue "Lembrado" de "Já conhecido". A garantia é **determinística só para escritas novas** — o acervo legado (duplicatas já gravadas) permanecia intacto até a SPEC-0023 (abaixo). `Fact`/`forget`/`list`/`prompt` inalterados por esta fatia; `@atlas/cognitive`, `@atlas/runtime`, `@atlas/permissions`, `@atlas/tools`, `@atlas/model-gateway`, `@atlas/persona`, `@atlas/context` intactos + **consolidação determinística do acervo legado de fatos** (SPEC-0023, `atlas memory dedupe`, fechando o residual explícito que a SPEC-0022 deixou nomeado — nota de atualização no [ADR-0011](docs/06-adr/ADR-0011-memory-service-persistence.md), sem ADR novo): `MemoryService` ganha o método **aditivo** `dedupe(options?: { apply?: boolean }): Promise<DedupeReport>` (`@atlas/contracts` exporta `DedupeReport { applied; groups }`/`DedupeGroup { survivor; duplicates }`), reusando o mesmo `normalize(text)` interno da SPEC-0022 para agrupar os fatos carregados. Por default é **dry-run** — computa e relata os grupos com duplicata sem mutar `facts` nem chamar `storage.save`; `dedupe({ apply: true })` remove as duplicatas em memória (preservando ordem de carga) e persiste por write-through (`storage.save`, 1x). Sobrevivente de cada grupo = o de menor `createdAt` (comparação lexicográfica de string ISO, `createdAt` sempre gerado via `toISOString()`), empate desempatado pela ordem de carga; preservado intacto (`id`/`text`/`createdAt`/`source`), sem merge de campos, sem promover `source`, sem reescrever `text`. Acervo sem duplicatas é no-op determinístico (`groups` vazio, `applied: false`, sem `save` mesmo com `apply: true`). CLI: novo subcomando `atlas memory dedupe [--apply]` (`apps/cli/src/commands/memory.ts`, `runMemoryDedupe`; parsing em `input-gateway.ts`; despacho e `HELP_TEXT` em `run.ts`) — só invoca `atlas.memory.dedupe` e renderiza o `DedupeReport` (dry-run: sobreviventes + duplicatas que **seriam** removidas + dica `--apply`; aplicado: confirma o que **foi** removido; sem grupos: "Nenhuma duplicata encontrada"). `remember`/`forget`/`list`/`prompt` inalterados. **Achado do gate (F1, corrigido no `architecture-reviewer`):** adicionar um método a `MemoryService` — contrato **já público** — não é "promoção de tipo" no sentido do ADR-0007; o fundamento para subir a operação ao contrato é o Artigo 11 (autoridade exclusiva da Memory) + Artigo 5 (a CLI não acessa `normalize`/`storage` internos). O membro aditivo à interface de contrato ainda quebrou o **typecheck** dos fakes de `MemoryService` tipados diretamente (só `apps/cli/tests/status.test.ts`); os fakes com `as unknown as` em `ask.test.ts`/`chat.test.ts` seguiram escapando (só `pnpm test` os pega) — 3ª+ confirmação do padrão recorrente. `@atlas/cognitive`, `@atlas/runtime`, `@atlas/permissions`, `@atlas/tools`, `@atlas/model-gateway`, `@atlas/persona`, `@atlas/context` intactos (diff de produção vazio). Roadmap 1.3/l. 93: candidato "consolidação do acervo legado no storage" marcado **entregue**. Além de `@atlas/contracts` e `@atlas/core` (`createAtlas()` sobe até `ready`, expõe config validada/congelada, compõe o Model Gateway + Tool Registry + Permission Service + Runtime + Cognitive Core + Context Service + Persona Service + Memory Service e desliga com segurança), existe `apps/cli` (`@atlas/cli`): `atlas status` sobe a plataforma pelo terminal, mostra estado e config resolvida (precedência `flags > env > defaults`, incluindo `readRoots`) e desliga com segurança; `atlas ask "<objetivo>"` envia um objetivo ao núcleo cognitivo e imprime a resposta (default provider `local`/Ollama; `remote`/`fake` por config); `atlas chat` abre uma conversa interativa multi-turno (mesmos provedores; histórico lembrado durante a sessão via `@atlas/context`, ADR-0008/0009; desde a SPEC-0014, cada turno também planeja e executa Tools — incluindo `delete_file` com o fluxo `confirm` inline, apoiado no `LineReader` da própria sessão, injetado via `CreateAtlasDeps.confirm`, sem abrir um segundo `readline`; o traço de `steps` de cada turno é impresso reusando a renderização do `ask`); `atlas --help`/`--version` também existem. Execução do fonte via `tsx` (ADR-0005), sem `dist/`. Existe também `@atlas/model-gateway`: `createModelGateway(config)` expõe `generate()` (geração única) com provedor selecionável por config — `fake` (testes), `local`/Ollama (grátis) e `remote` (pago, OpenAI-compatible); provedores de rede recebem `fetch` por parâmetro (testados sem rede) e há um smoke script para verificação real. Seu primeiro consumidor é `@atlas/cognitive`: `createCognitiveCore({ gateway, runtime, personaPrompt?, memoryPrompt? })` → `ask(objetivo)` **orquestra** Planejamento + Execução (ADR-0012, deixou de ser tiro único): 1ª chamada `generate` (system prompt composto — `personaPrompt` (identidade) + `memoryPrompt` (fatos) + `TASK_FRAMING`, na ordem identidade → memória → tarefa, ADR-0010/0011 — mais a instrução do Planner com o catálogo de Tools) → o **Planner** puro (consolidado no package: `createPlanner()` com `instruction`/`parse`, sem gateway) parseia a saída num `Plan` ou `null`; sem plano, responde direto em 1 chamada; com plano, `runtime.execute` roda as Tools e uma 2ª chamada compõe a resposta final. `ask` retorna `AskResult { text; steps? }`. `startConversation()`/`respond(conversation, input)` sustentam conversa multi-turno como dado (ADR-0008) e, desde a SPEC-0014, `respond` orquestra Planejamento + Execução no mesmo padrão de `ask` (instrução do Planner na 1ª chamada; sem plano, resposta direta; com plano, `runtime.execute` + 2ª chamada de composição), permanecendo função pura e o Core sem estado; `ConversationTurn` ganha `steps?: readonly ExecutedStep[]`, e quando Tools rodam a `Conversation` retornada ganha uma mensagem `system` compacta de resumo (nunca exibida ao usuário), para que o modelo lembre do que executou nos turnos seguintes. Existe `@atlas/tools` (`createToolRegistry` + Tools puras `clock`/`calc`, sem `requirements`, `calc` sem `eval`; as primeiras Tools com **IO de leitura** — `read_file`/`list_dir` — que declaram `requirements(args) → ActionRequest` (`access: 'read'`) e leem o disco por uma porta injetável `FsReadPort`, default `nodeFsReadPort()`; a Tool de **escrita** — `write_file` (`{ path, content }`, cria/sobrescreve) — que declara `requirements` com `access: 'write'`; e as três Tools novas da SPEC-0013 — `delete_file` (`{ path }`, remove um arquivo, `access: 'delete'`, a única ação irreversível), `mkdir` (`{ path }`, cria diretório recursivamente/idempotente) e `append_file` (`{ path, content }`, acrescenta conteúdo, cria se ausente; ambas `access: 'write'`) — todas sobre a porta injetável `FsWritePort` (`writeFile`/`deleteFile`/`mkdir`/`appendFile`), default `nodeFsWritePort()`; desde a SPEC-0017, `readFile`/`writeFile`/`appendFile` executam internamente o fecho atômico de TOCTOU — `open(O_NOFOLLOW)` → ancoragem de identidade do fd (`fstat`×`stat(realpath)`) → predicado estreito injetado `Verify = (realpath, access) => boolean` (tipo interno, não sobe a `@atlas/contracts`) → opera sobre o fd e fecha; `nodeFsReadPort`/`nodeFsWritePort` recebem `{ verify?, primitives? }` (`verify` default permissivo, `@atlas/core` fia `permissions.isContained`; `primitives` interno para fakes nos testes); `readdir`/`deleteFile`/`mkdir` **não mudam**) e `@atlas/runtime` (`createRuntime({ registry, permissions, confirm })` → `execute(plan)` sequencial com falhas estruturadas que nunca derrubam a execução + `tools()`; por passo com `requirements`, consulta `permissions.evaluate` antes de rodar a Tool — veredicto `allowed` executa; veredicto `confirm` **pausa e aguarda** `confirm.request(requirement)` (`ConfirmPort`, real via `nodeReadlineConfirmPort()` sobre `node:readline`, nunca trava em EOF/não-TTY) — aprovado executa a Tool, recusado vira `ExecutedStep` negado com motivo distinto de bloqueio; qualquer outro veredicto vira `ExecutedStep` negado e a Tool não roda); o modelo produz o plano em JSON (Planner-driven) e o Runtime o executa deterministicamente, com o Model Gateway **intacto**. `atlas ask` mostra um traço compacto das Tools executadas (incluindo passos bloqueados, com o motivo). O contrato do Model Gateway e o `CognitiveCore` vivem em `@atlas/contracts` (ADR-0007). Existe também `@atlas/context` (`createContextService()` → store de sessão em memória que guarda a `Conversation` por sessão; o `atlas chat` usa-o como detentor, ADR-0008/0009; `respond` segue puro). Existe também `@atlas/persona` (`createPersonaService()` → registro de Personas `jarvis`/`neutral`; a identidade da Persona ativa é injetada na geração do Cognitive, ADR-0010; `atlas ask`/`atlas chat` respondem na voz da Persona; `atlas status` a exibe, `atlas chat` a saúda; seleção por `--persona`/`ATLAS_PERSONA`, default `jarvis`). Existe também `@atlas/memory` (`createMemoryService({ storage })` → autoridade de conhecimento persistente; fatos/preferências explícitos guardados atrás de uma porta de storage injetável, adapter JSON default; `memory.prompt()` é injetado na geração do Cognitive, ADR-0011; `atlas remember "<fato>"`/`atlas forget <id>`/`atlas memory list` gerenciam; caminho por `--memory-path`/`ATLAS_MEMORY_PATH`, default `~/.atlas/memory.json`; Memória persistente × Contexto temporário seguem distintos; **desde a SPEC-0022**, `remember` normaliza o texto (`trim`/`toLowerCase`/colapso de `\s+`) e faz no-op idempotente em duplicata — `Promise<{ fact, created }>` sinaliza criado × existente; **desde a SPEC-0023**, `dedupe(options?: { apply? })` consolida o acervo já persistido — dry-run por default, `--apply` remove duplicatas e persiste, sobrevivente = mais antigo por `createdAt`, `atlas memory dedupe [--apply]` na CLI). Existe também `@atlas/permissions` (`createPermissionService({ readRoots, writeRoots, pathResolver? })` → `evaluate(action)` **puro e síncrono** do ponto de vista de decisão; roteia por `access` — `read` contra `readRoots`, `write`/`delete` contra `writeRoots` — julgando por **contenção lexical** (dentro da raiz → `allowed` para `write`, `confirm` para `delete`; fora/vazio → `blocked` com motivo) sobre caminhos **resolvidos por `realpath`** desde a SPEC-0015 (fecha o escape por symlink dentro de uma raiz permitida, limitação conhecida desde o ADR-0013): nova porta injetável síncrona interna `PathResolverPort` (`realpathSync`, default `nodePathResolverPort()` sobre `node:fs`, sem 2º consumidor → não sobe a `@atlas/contracts`); `readRoots`/`writeRoots` resolvidas uma única vez na criação e o alvo a cada `evaluate`, ambos pelo algoritmo do ancestral existente mais profundo (sobe aos pais até achar um segmento existente, recompõe por cima); fail-closed em qualquer erro de resolução ≠ `ENOENT` (nunca lança — no alvo vira `blocked`, numa raiz na criação só aquela raiz fica fail-closed); `evaluate` segue síncrono, Runtime inalterado. Grants de leitura e escrita **independentes**; `confirm` real desde a SPEC-0013 — quem aplica o veredicto (pausar e perguntar) é o Runtime, nunca o Permission Service, que segue puro; ADR-0013; desde a SPEC-0017/ADR-0014, `PermissionService` ganha `isContained(canonicalPath, access): boolean` — método novo, puro/síncrono, sem IO, sem re-resolver caminho, reusando `within()` sobre as raízes já resolvidas — julgado sobre um caminho **já canônico** (o `realpath` que uma porta de IO ancorou num fd), fiado por `@atlas/core` como o `verify` de `@atlas/tools`; é a segunda barreira do fecho atômico de TOCTOU, aplicada no instante do uso, sem duplicar a autoridade de contenção). `config.permissions.readRoots` (default `[cwd]`, override `--allow-read`/`ATLAS_ALLOW_READ`) e `config.permissions.writeRoots` (default `[]` — não escreve sem opt-in explícito, override `--allow-write`/`ATLAS_ALLOW_WRITE`), precedência `flags > env > defaults`, definem as políticas de leitura e escrita. Desde a SPEC-0018 **ambos aceitam múltiplas raízes**: a flag é repetível (`--allow-read A --allow-read B`) e a env é lista por `path.delimiter` (`ATLAS_ALLOW_READ=A:B`); a lista da fonte de maior precedência substitui a inferior por inteiro (sem merge), segmentos vazios são filtrados e fonte que colapsa para vazio cai no default. **O default do predicado `verify` das portas de FS agora é fail-closed** (SPEC-0024, ramo micro da Emenda v1.2, fechando o residual explícito da lição da SPEC-0017): em `packages/tools/src/fs-port.ts`, `nodeFsReadPort`/`nodeFsWritePort` trocaram `deps.verify ?? (() => true)` por `deps.verify ?? (() => false)` — uma porta construída **sem** `verify` injetado passa a **recusar** `readFile`/`writeFile`/`appendFile` no instante do uso, em vez de permitir por omissão; `readdir`/`deleteFile`/`mkdir` não passam pelo fecho atômico e seguem inalterados. Comportamento em produção **idêntico** ao de hoje — `createAtlas` sempre fiou `verify = permissions.isContained.bind(permissions)` desde a SPEC-0017, então o default nunca é consultado nesse caminho; o efeito é só sobre portas cruas/futuras. Sem mudança de assinatura (`Verify`/`NodeFsPortDeps` intactos, `verify` segue opcional); diff de produção confinado a `packages/tools/src/fs-port.ts`; `@atlas/core`/`@atlas/contracts`/`@atlas/permissions`/`@atlas/runtime`/`apps/cli` intactos + **o conceito de Skill passa a existir na implementação, não só documentado** (SPEC-0025, consumindo o [ADR-0017](docs/06-adr/ADR-0017-skills-registry-builder.md) — Registry passivo + Builder, decisão humana da Emenda v1.1): novo package `packages/skills` (`@atlas/skills`), com os dois componentes de Extensão reservados desde sempre pelo Module Catalog. **Skill Registry** — `createSkillRegistry({ skills? })` → catálogo **em memória**, no molde do Tool Registry (SPEC-0010): `register`/`get`/`deactivate`/`remove`/`list`; distingue `scope: 'permanent' | 'temporary'` e ativo/inativo; `register` faz upsert por `id` mas **rejeita** (`SkillError`, `ATLAS_SKILL`) qualquer registro que envolva uma Skill `permanent` já registrada por aquele `id` — uma `temporary` nunca sobrescreve nem é sobrescrita numa colisão `permanent`; sem persistência, o Registry vive só no processo. **Skill Builder** — `createSkillBuilder({ gateway, registry, tools })` → `build({ capability })` percorre os 8 passos do Module Catalog: **uma única** `gateway.generate` (passo 5) produz um `SkillDraft` estruturado em JSON (`name`/`description`/`instructions`/`toolIds`, todos derivados do modelo), parseado no molde do learner (ADR-0016) — JSON malformado/incompleto vira `issues`, nunca lança; validação estática (passos 6-7) checa contrato bem-formado (campos presentes, `name`/`instructions` não vazios após `trim`, `toolIds` array de strings) **e** que todo `toolId` resolve no `ToolRegistry` injetado; em sucesso (passo 8), completa **deterministicamente** `id` (namespace `tmp-…` via `crypto.randomUUID()`), `version` (`'1.0.0'`) e `scope: 'temporary'` — nunca do modelo, mesmo que o JSON tente injetá-los — e registra; em falha, devolve `{ ok: false, issues }` sem registrar. **Nunca** promove `temporary`→`permanent`. `BUILTIN_SKILLS` semeia o Registry com ao menos uma Skill `permanent` (dado inerte, ids fora do namespace `tmp-`). Contratos novos em `@atlas/contracts` (`skill.ts`): `Skill`/`SkillScope`/`SkillDescriptor`/`SkillDraft`/`SkillRegistry`/`SkillBuilder`/`SkillBuildRequest`/`SkillBuildResult`, todos aditivos; `AtlasPlatform` ganha `readonly skills: SkillRegistry` e `readonly skillBuilder: SkillBuilder`. `@atlas/core` cria/semeia o Registry e fia o Builder com `gateway`+`registry`+Tool Registry já compostos, expondo `atlas.skills`/`atlas.skillBuilder`. CLI: `atlas skills list` (catálogo com `id`/nome/`scope`/ativo-inativo/versão) e `atlas skills build "<capacidade>"` (dispara o Builder, imprime a Skill produzida ou as pendências de validação); `HELP_TEXT` atualizado. **Nada no laço cognitivo consome Skills nesta fatia** — Planner, Cognitive Core, Runtime e Observer têm diff de produção vazio (decisão humana registrada no ADR-0017); sem seleção pelo usuário (sem flag/config de Skill ativa); sem persistência em disco (uma Skill construída por `atlas skills build` não sobrevive à invocação); sem execução real da Skill no "teste controlado" (validação é só estática); Skill sem canal com o usuário (Module Catalog, regra 4). Fecha o gate 1.4 do Roadmap ("Skills existem como conceito implementado, não só documentado"), item 1.4 marcado **parcialmente entregue** (consumo de Skills no laço cognitivo segue candidato futuro) + **o laço cognitivo passa a selecionar e aplicar Skills automaticamente** (SPEC-0026, consumindo o [ADR-0018](docs/06-adr/ADR-0018-planner-skill-consumption.md) — decisão humana, forma A: seleção pelo Planner/modelo, não pelo usuário — que ativa o consumo e ganha nota de atualização no [ADR-0017](docs/06-adr/ADR-0017-skills-registry-builder.md)): `Plan.skillId?: string` (`@atlas/contracts`, aditivo, molde do `denialKind?` da SPEC-0019) registra a Skill selecionada; `planner.instruction(tools, skills?)` passou a listar também o catálogo de Skills **ativas** por `id`/`name`/`description` (sem `instructions`), com framing pedindo **no máximo uma** Skill por `id` — `skills` vazio/ausente preserva a string de antes; `planner.parse` lê `skillId` opcional ao lado de `steps`. `CognitiveCoreDeps` ganha a porta **opcional** `skillCatalog?: SkillCatalogPort` (`{ list(): readonly SkillDescriptor[]; get(id): Skill | undefined }`, tipo interno a `@atlas/cognitive`, não sobe a `@atlas/contracts` — mesmo molde do provider `memoryPrompt` da SPEC-0021, menor privilégio: o Cognitive só lê, nunca `register`/`deactivate`/`remove`); amostrada (`list()`) 1x por turno em `ask` **e** `respond` e passada a `planner.instruction`. O `runPlanCycle` rastreia o `skillId` do **plano em vigor** (`activeSkillId`, reatribuído a cada replanejamento) e, só na `generate()` de **composição**, resolve-o contra o catálogo (`get`) — resolve → as `instructions` da Skill entram no `systemPrompt` na ordem Persona → Memory → **Skill** → `TASK_FRAMING`; não resolve/ausente → turno segue idêntico ao comportamento anterior, sem lançar. Em `respond`, a injeção usa `withFreshSystemHead` (SPEC-0021) só na mensagem enviada à chamada de composição — a `Conversation` retornada **nunca** carrega a Skill, preservando `respond` puro e a invisibilidade da Skill (Constituição, Artigos 2 e 9); `AskResult`/`ConversationTurn` não ganham `skillId`. `toolIds` da Skill **não** filtram/priorizam Tools no Runtime nesta fatia — Runtime, Permission Service, Tools, Model Gateway e `apps/cli` **intactos** (diff de produção vazio). `@atlas/core` fia a projeção somente-leitura `{ list: () => skills.list(), get: (id) => skills.get(id) }` no `createCognitiveCore`. O consumo alcança só as Skills `permanent` semeadas (`BUILTIN_SKILLS`); Skills `temporary` construídas por `atlas skills build` seguem não-consumíveis entre invocações — persistência de Skills fica fora desta fatia (mesma fronteira adiada pelo ADR-0017). **O gate 1.4 do Roadmap fecha por inteiro.** Nenhum campo obrigatório novo em contrato consumido por fakes — `Plan.skillId?`/`CognitiveCoreDeps.skillCatalog?`/`instruction(tools, skills?)` são todos opcionais/aditivos, sem disparar o padrão recorrente de quebra de `typecheck` em fakes. Suíte: 423 testes. Verificação verde: `pnpm lint`/`format:check`/`typecheck`/`test` + **busca/recuperação determinística de fatos no Memory Service** (SPEC-0027, Perfil `completo`, nota de atualização no [ADR-0011](docs/06-adr/ADR-0011-memory-service-persistence.md), sem ADR novo — candidato independente de 1.3/l. 91 do Roadmap, **não** fecha o gate de 1.3 por inteiro, que segue exigindo mais de uma categoria de conhecimento): `MemoryService` ganha o método **aditivo** `search(query: string, options?: { readonly limit?: number }): readonly Fact[]` em `@atlas/contracts`, implementado em `packages/memory/src/memory-service.ts` — puro, síncrono, sem IO, opera só sobre os `facts` já carregados. Reusa o **mesmo** `normalize(text)` interno da SPEC-0022 (não duplicado) para tokenizar consulta e fato; ranking determinístico por overlap de tokens (`score = |tokens(query) ∩ tokens(fact)|`), exclui `score = 0`, ordena por `score` desc, empate desempatado pela ordem de carga (índice ascendente em `facts`, mesmo critério de desempate do `dedupe`/SPEC-0023). `options.limit` opcional corta os `N` primeiros já ordenados; ausente devolve todos os fatos com overlap; consulta vazia/sem tokens após `normalize` devolve `[]`; retorno `Fact[]` homogêneo com `list()`, **sem** expor scores (detalhe interno). CLI: novo subcomando `atlas memory search "<consulta>"` (`apps/cli/src/gateway/input-gateway.ts` reconhece o subcomando + captura a consulta; `apps/cli/src/commands/memory.ts` ganha `runMemorySearch`, só invoca e renderiza — id + texto, ou "Nenhum fato relevante encontrado."; despacho e `HELP_TEXT` em `apps/cli/src/run.ts`). **Não** altera `prompt()` nem o caminho de injeção de memória na geração — o `memoryPrompt` do Cognitive Core continua recebendo o dump de todos os fatos via `prompt()`; consumir `search` a partir da consulta do turno no Cognitive Core segue fatia futura explícita. `remember`/`forget`/`list`/`prompt`/`dedupe`/`Fact` inalterados. **Intactos** (diff de produção vazio): `@atlas/cognitive`, `@atlas/runtime`, `@atlas/permissions`, `@atlas/tools`, `@atlas/model-gateway`, `@atlas/persona`, `@atlas/context`, `@atlas/skills`, `@atlas/core`. **Padrão recorrente confirmado de novo (6ª+ ocorrência)**: membro novo obrigatório em `MemoryService` (interface de contrato já pública) quebrou o `typecheck` só do fake tipado diretamente (`apps/cli/tests/status.test.ts`), coerente com SPECs 0017/0019/0022/0023/0025/0026. Suíte: 437 testes (era 423). Verificação verde: `pnpm lint`/`format:check`/`typecheck`/`test` + **as primeiras Tools da especialidade declarada do Atlas — desenvolvimento de software** (SPEC-0028, Perfil `completo` — o `architecture-reviewer` vetou a 1ª versão da SPEC no gate `Draft → Ready`, 1 rodada de correção, sem escalação; item **1.4 (Capacidades de Plataforma)** do Roadmap, l. 100, `candidato · SPEC direta`, marcado **parcialmente entregue** — entrega a primeira das três fatias nomeadas nesse item; **não** fecha nenhum gate, o único gate de 1.4 (Skills) já fechado pela SPEC-0025): `@atlas/tools` ganha `createGitStatusTool({ git?, cwd? })`/`createGitDiffTool({ git?, cwd? })`/`createGitLogTool({ git?, cwd? })` — Tools **somente-leitura** que declaram `requirements(args) → { resource: { type: 'directory', path }, access: 'read' }` (o mesmo `AccessMode`/`ResourceType` já existentes, nenhuma mudança em `@atlas/contracts`) e chamam a porta interna `GitReadPort` (`status`/`diff`/`log`, sem 2º consumidor, molde de `FsReadPort`), adaptador default `nodeGitReadPort({ verify?, exec?, realpath? })` sobre `node:child_process` `execFile` (argv array, nunca `shell: true`). **O achado que definiu a fatia**: `git status`/`diff`/`log` **descobrem o repositório subindo pelos diretórios pais** — diferente de `list_dir`, que nunca lê acima do path declarado; um alvo em subdiretório de um repositório maior faria o git relatar (e, no `diff`, imprimir o conteúdo de) toda a árvore acima dele, inclusive do que `read_file` seria `blocked` de ler. Por isso a porta, antes de qualquer subcomando, **descobre o toplevel real** (`git --no-optional-locks -C <alvo> rev-parse --show-toplevel`), resolve o `realpath` e aplica `verify(toplevel, 'read')` — o mesmo predicado estreito `Verify` do ADR-0014, reusado de `fs-port.ts`, **fail-closed por default** (SPEC-0024); `@atlas/core` fia `verify: permissions.isContained.bind(permissions)`, a mesma fiação já usada para `nodeFsReadPort`/`nodeFsWritePort` — nenhuma config nova. Toplevel fora das `readRoots` → recusa estruturada com o toplevel descoberto na mensagem, e o subcomando **não roda** — asseverado por spy sobre o `exec` fake (exatamente 1 chamada, a de `rev-parse`). Todo argv começa com `--no-optional-locks` (inclusive no `rev-parse`), porque `git status` pode atualizar `.git/index` e tomar lock — escrita real sob veredicto `read` num repositório onde `writeRoots` é vazio por default; sem a flag o gate mentiria sobre o que a ação faz (Artigo 8). `execFile` com `maxBuffer` explícito de 10 MiB (estouro → erro estruturado, nunca opaco) e a saída capturada truncada em 64 KiB com marcador visível + `truncated: true` (vale para os três, `git_diff` inclusive — o pior caso de volume). `GitOutput.repository` (o toplevel real) entra no `output` de sucesso antes do texto do git, mesmo com `path` ausente (Artigo 7 — o alvo é o dado que o usuário não forneceu). `git_diff` aceita `staged?: boolean` (`--cached`); `git_log` aceita `maxCount?: number`, default **20**, valor inválido cai no default sem erro (allowlist bounded — é o que mantém esta fatia fora do território `ADR primeiro` de "execução de comandos" reservado no mesmo item do Roadmap). Saída em texto cru do git, sem parsing estruturado (molde de `read_file`). **Residuais conhecidos, documentados e não fechados** (mesmo critério do ADR-0014 para `list_dir`): sem fecho atômico de TOCTOU do diretório-alvo (quem lê é um subprocesso sobre um diretório, não há fd a ancorar); troca de ancestral; Windows fora de escopo; `~/.gitconfig` do usuário segue lido fora do portão; a mensagem de recusa expõe um toplevel **descoberto** (não um path que o chamador já forneceu), um vazamento pequeno com contrapartida de transparência — candidato natural à fatia futura de "leitura de estrutura de projeto" nomeada no mesmo item do Roadmap. **Diff vazio** em `@atlas/contracts`, `@atlas/permissions`, `@atlas/runtime` — nenhum `access` novo, nenhum contrato tocado, nenhum ADR aberto. Fora de escopo (explicitamente, mesmo item do Roadmap): mutação do repositório (`git_commit`/`git_checkout`/`git_push`/…), execução de comandos arbitrários (novo `access`, `ADR primeiro`), leitura de estrutura de projeto, contexto de ambiente lido pelo Planner/Cognitive (ADR-0009). Suíte: 477 testes (era 437). Verificação verde: `pnpm lint`/`format:check`/`typecheck`/`test` + **categorias de conhecimento no Memory Service — memória episódica e memória de projetos** (SPEC-0029, Perfil `completo`, nota de atualização no [ADR-0011](docs/06-adr/ADR-0011-memory-service-persistence.md), sem ADR novo; item **1.3 (Memory Service — Próximas Fatias)** do Roadmap, l. 90, `gate · SPEC direta` — **fecha por inteiro o último gate aberto da Fase 1**, com 1 veto do `architecture-reviewer` no gate, corrigido numa rodada): `@atlas/contracts` ganha `MemoryCategory = 'fact' | 'episode' | 'project'` (exportado); `Fact` ganha `category?: MemoryCategory` e `subject?: string`, ambos **opcionais** (ausência de `category` ≡ `'fact'`, acervo legado não é migrado nem revalidado no `load`); `MemoryService.remember` ganha um terceiro parâmetro opcional `options?: { category?; subject? }`, 100% compatível com os chamadores de 1/2 argumentos; `MemoryService.list` ganha `options?: { category? }`. Em `@atlas/memory` (`packages/memory/src/memory-service.ts`): a invariante do modelo persistido — "memória de projeto exige projeto" — passa a viver **dentro do módulo** (Artigo 11), rejeitando com `MemoryError` (classe já existente, nenhuma nova), **antes** de qualquer mutação/`storage.save`, tanto `category: 'project'` sem `subject` válido quanto `subject` presente com categoria diferente de `'project'`; um `subject` que colapse para vazio após `normalize` (`''`/`'   '`/`'\t\n'`) conta como **ausente e inválido**, não como "sem projeto" — achado F2 do gate. A **chave de duplicata** (SPECs 0022/0023) passa a incluir categoria+`subject` normalizados (tupla com separador que não pode aparecer no texto), reusada em `remember` e `dedupe`, para o mesmo texto em categorias/projetos diferentes não colapsar; acervo legado sem `category` segue tratado como `'fact'` para efeito de duplicata (não-regressão da SPEC-0022). `list(options?)` filtra por categoria (ausente ≡ `'fact'`). `prompt()` passa a compor **seções por categoria**, ordem fixa `fact → project → episode`, separadas por uma linha em branco, seções vazias omitidas; a seção `fact` é **byte a byte idêntica** à de antes desta SPEC (não-regressão com string literal); a seção `project` agrupa por `subject` com subcabeçalho `[projeto <subject>]` (texto original, não normalizado), ordem entre projetos pela **primeira ocorrência** na ordem de carga — formato e ordem fixados literalmente porque o texto entra no system prompt de toda geração (achado F3 do gate). `dedupe`/`search` mantêm assinatura e comportamento (agrupamento/varredura por categoria coerente com a nova chave; `search` não ganha filtro por categoria — fora de escopo por observação do próprio `architecture-reviewer`). CLI: `atlas remember "<texto>" --category <fact|episode|project> [--subject <projeto>]` e `atlas memory list [--category <c>]` (`apps/cli/src/gateway/input-gateway.ts`, `commands/remember.ts`, `commands/memory.ts`, `run.ts` + `HELP_TEXT`) — a CLI **antecipa** a invariante com `CliUsageError` amigável (categoria/`subject` inválidos, `subject` em branco, `subject` sem `category: 'project'`), mas a garantia real vive no módulo. O Learner (SPEC-0020) continua produzindo só `category: 'fact'`, `source: 'learned'` — nenhuma mudança em `@atlas/cognitive`/`@atlas/core`. **Intactos** (diff de produção vazio): `@atlas/cognitive`, `@atlas/core`, `@atlas/runtime`, `@atlas/permissions`, `@atlas/tools`, `@atlas/model-gateway`, `@atlas/persona`, `@atlas/context`, `@atlas/skills`. **Padrão recorrente NÃO disparado (2ª vez, após a SPEC-0026)**: como `Fact.category?`/`subject?` e os parâmetros novos de `remember`/`list` são todos opcionais/aditivos, o fake tipado diretamente (`apps/cli/tests/status.test.ts`) compilou sem alteração. **Com esta SPEC, todos os gates da Fase 1 estão entregues** — o Critério de Conclusão da Fase 1 ("a Memory Service cobre mais de uma categoria de conhecimento") passa a ser atendido; a decisão de iniciar a Fase 2 continua sendo do usuário, não automática. Suíte: 532 testes (era 477). Verificação verde: `pnpm lint`/`format:check`/`typecheck`/`test`.
