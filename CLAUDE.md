# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## O que é este repositório

Project Atlas: plataforma de IA pessoal organizada como **monólito modular**, orientada por um ciclo cognitivo (compreensão → raciocínio → planejamento → execução → observação → aprendizado → resposta). A primeira Persona oficial será **Jarvis**. Este repositório é o workspace raiz; a plataforma principal viverá em `atlas-core`.

**Estado em julho/2026 (resumo — detalhe completo em `docs/05-context/PLATFORM_STATE.md`):**

- **Ciclo cognitivo completo nas 7 etapas** (compreensão → raciocínio → planejamento → execução → observação → aprendizado → resposta), SPECs 0002–0020 `Done`.
- **Packages**: `@atlas/contracts` (contratos públicos; desde a SPEC-0055/ADR-0026, `ResourceType` ganha `'network'` e `ResourceRef` vira união discriminada — `{ type:'file'|'directory', path }` | `{ type:'network', host }`; desde a SPEC-0057, também o namespace de módulo `AtlasConfig.tools: { searchUrl }` — endpoint do provedor de busca, `''` = não configurado), `@atlas/core` (`createAtlas()`, único orquestrador), `@atlas/model-gateway` (provedores `fake`/`local`-Ollama/`remote`), `@atlas/cognitive` (Planner + Observer + Learner puros; `ask`/`respond` orquestram planejamento + execução com teto de 1 replanejamento; desde a SPEC-0026/ADR-0018, o Planner também **seleciona automaticamente** no máximo uma Skill por turno e o Cognitive Core injeta suas `instructions` na composição, invisível ao usuário), `@atlas/runtime` (executa planos, aplica veredictos, fluxo `confirm`), `@atlas/tools` (`clock`/`calc`/`read_file`/`list_dir`/`write_file`/`delete_file`/`mkdir`/`append_file`, portas de IO com fecho atômico TOCTOU; desde a SPEC-0028, também `git_status`/`git_diff`/`git_log` somente-leitura, primeiras Tools da especialidade de desenvolvimento de software, com o toplevel real do repositório contido às `readRoots`; desde a SPEC-0055/ADR-0026, também `http_get` — primeira Tool de rede, somente `GET`, sobre a porta injetável `HttpPort`/`nodeHttpPort`, `redirect: 'manual'`, timeout de 10 s, corpo truncado em 64 KiB; desde a SPEC-0056, também `project_info` — descreve a raiz de um projeto de software, manifests reconhecidos (tabela fixa de 16) e scripts do `package.json`, reusando a descoberta de toplevel da SPEC-0028 por um método aditivo da `GitReadPort` (`toplevel(cwd)`), com ascensão ao repositório só quando `path` é omitido; desde a SPEC-0057, também `web_search` — busca em texto livre sobre a porta injetável `SearchPort` (`endpointUrl` publicado pela própria porta, fonte única do host declarado em `requirements` e do host requisitado), adaptador default `searxngSearchPort` sobre o `HttpPort` já endurecido da SPEC-0055 (`nodeHttpPort` ganha `bodyLimitBytes?` aditivo; a busca usa 256 KiB), provedor sem credencial compatível com a API JSON do SearXNG configurado por `--search-url`/`ATLAS_SEARCH_URL`; sem endpoint configurado, a Tool não é registrada; host julgado pelo mesmo `netRoots` (ADR-0026), sem cláusula reaberta), `@atlas/permissions` (contenção lexical sobre `realpath`, `evaluate` puro/síncrono + `isContained`; desde a SPEC-0055/ADR-0026, `evaluate` também roteia `resource.type === 'network'` contra a política irmã `netRoots` — allowlist de hostname por igualdade exata, fail-closed default `[]`; `isContained` permanece FS-específico, sem rota de rede), `@atlas/context` (conversa por sessão), `@atlas/persona` (`jarvis`/`neutral`), `@atlas/memory` (fatos persistentes, JSON, proveniência `user`/`learned`; desde a SPEC-0029, classificado por categoria — `fact`/`episode`/`project`, `subject` obrigatório em `project`, invariante garantida no módulo — fechando o último gate da Fase 1), `@atlas/skills` (Skill Registry em memória + Skill Builder de 8 passos, ADR-0017; **consumido automaticamente pelo Planner desde a SPEC-0026/ADR-0018** — gate 1.4 do Roadmap fechado por inteiro).
- **CLI** (`apps/cli`, via `tsx`, sem `dist/`): `atlas status` / `ask` / `chat` (multi-turno com Tools e `confirm`) / `remember "<texto>" [--category fact|episode|project] [--subject <projeto>]` / `forget` / `memory list [--category <c>]` / `memory dedupe [--apply]` (consolidação determinística do acervo legado, dry-run por default) / `memory search "<consulta>"` (busca determinística por overlap de tokens) / `skills list` / `skills build "<capacidade>"`; desde a SPEC-0044, também `persona list|show <id>|create "<nome>" [flags]|edit <id> [flags]|delete <id> [--yes]` — CRUD de Personas custom pelo terminal sobre o mesmo `personas.json` do desktop (`personaStoragePath(resolveDataDir(...))` de `@atlas/core`, `createCliPersonaStorage`/`createCliPersonaService`), sem subir o Core; todos os demais comandos passam a injetar `personaStorage` em `createAtlas`, então `--persona <id-custom>`/`ATLAS_PERSONA` resolvem Personas custom em qualquer comando; config por `flags > env > defaults`; múltiplas raízes `--allow-read`/`--allow-write` (repetíveis; env com `path.delimiter`); `writeRoots` default vazio (opt-in); desde a SPEC-0055, também `--allow-net <host>` (repetível) + `ATLAS_ALLOW_NET` (lista por **vírgula**, não `path.delimiter` — hostname não é caminho), mesma precedência; `netRoots` default vazio (opt-in); `atlas status` passa a exibir `netRoots`; desde a SPEC-0057, também `--search-url <url>`/`ATLAS_SEARCH_URL` (não repetível — é um endpoint, não uma lista), mesma precedência; `atlas status` passa a exibir também `search: <url>` ou `search: (não configurado)`.
- **Desktop** (`apps/desktop`, `@atlas/desktop`; SPEC-0031/0032/0033/0034/0035/0036/0037/0038/0039/0040/0041/0043/0046/0052/0053/0054, [ADR-0019](docs/06-adr/ADR-0019-desktop-electron-stack.md), Electron): primeira interface gráfica do Atlas — abre a **Fase 2** do Roadmap. Round-trip `status` (SPEC-0031) e round-trip `ask` de tiro único (SPEC-0032, stateless: sobe e desliga o Core por chamada), com `ConfirmPort` de diálogo nativo (`dialog.showMessageBox`, fail-closed) para Tools destrutivas e traço visual de `steps` (`formatSteps`); desde a SPEC-0033, também um **chat visual multi-turno** com o Core mantido **vivo entre turnos** no main process (`openChatSession`/`sendChatTurn`/`closeChatSession`, equivalente GUI do `atlas chat`), sessão viva pelo Context Service (`atlas.context`), reusando os mesmos adapters de diálogo/traço; desde a SPEC-0034, também um painel de **gerência visual de memória** — listar e esquecer fatos (`resolveMemorySnapshot`/`forgetFact`, stateless, equivalente GUI de `atlas memory list`/`atlas forget`), primeira das três linhas do item 2.4; desde a SPEC-0035, também **saída de voz (TTS)** — botão "🔊 Ouvir" por resposta do chat, via `window.speechSynthesis`/`SpeechSynthesisUtterance` (Web Speech API embutida no Chromium, sem dependência de runtime nova), num módulo puro `src/speech-output.ts` (`createSpeechOutput`), primeira fatia do item 2.3; desde a SPEC-0036, essa saída fica restrita a **vozes locais do SO** (`SpeechSynthesisVoice.localService === true`, explicitamente vinculada ao enunciado), fail-closed — sem voz local, não fala, nunca cai numa voz de rede como alternativa (garantia condicional a o SO/navegador reportarem `localService` corretamente); desde a SPEC-0037, também **seleção e troca de Persona em runtime** — um seletor lista as Personas (`jarvis`/`neutral`), marca a ativa e troca sem reiniciar a app, via `listPersonas`/`selectPersona`/`selectedPersonaId` (`core-bridge.ts`; catálogo lido por re-export em `@atlas/core`, `createPersonaService`/`PERSONA_IDS`, preservando o composition root do ADR-0003), segunda das três linhas do item 2.4; trocar de Persona encerra as sessões de chat vivas e um turno em voo bloqueia a troca; a seleção não é durável (volta à Persona da config ao reabrir a app); desde a SPEC-0038, também um painel de **configuração de permissões** (`readRoots`/`writeRoots`) em runtime — via `selectPermissionRoots`/`selectedPermissionRoots` (`core-bridge.ts`), ampliar `writeRoots` exige consentimento explícito num diálogo de **concessão de política** dedicado (`permission-grant-dialog.ts`, distinto do `ConfirmPort` de ação pontual), aplicação é tudo-ou-nada e recusada com qualquer operação em voo (rastreio generalizado a toda função que sobe um Core capaz de executar Tools), encerra as sessões de chat vivas ao aplicar, e não é durável (volta às raízes de `flags > env > defaults` ao reabrir a app) — **terceira e última linha do item 2.4, que fecha por inteiro**; desde a SPEC-0039, consumindo o [ADR-0020](docs/06-adr/ADR-0020-persona-persistence-voice-binding.md) (Accepted), o painel de Persona também permite **criar, editar e apagar Personas custom** — formulário com os 8 campos de `Persona` mais uma voz real do SO, persistidas por uma porta `PersonaStorage` injetável no Persona Service (`@atlas/persona`, molde do ADR-0011) via `describePersona`/`createPersona`/`updatePersona`/`deletePersona` (`core-bridge.ts`), com `Persona.voiceURI?` vinculando a Persona ativa à saída de voz (TTS) com fallback fail-closed para a seleção determinística das SPECs 0035/0036 (nunca voz de rede); apagar exige consentimento explícito em diálogo nativo dedicado (`persona-delete-dialog.ts`, fail-closed); `jarvis`/`neutral` seguem embutidas, imutáveis e somente-leitura; estende a linha de Persona do item 2.4 (já fechado por inteiro pela trinca SPEC-0034/0037/0038); desde a SPEC-0040, consumindo o [ADR-0021](docs/06-adr/ADR-0021-piper-tts-local-voice-engine.md) (Accepted), a saída de voz fala por um motor **neural** local — Piper —, não mais só as vozes do SO: `apps/desktop/src/piper-tts.ts` (novo) mantém um **processo Piper de longa duração** no main process (modelo carregado uma vez, reusado entre falas, reciclado ao trocar de modelo/timeout/cancelamento), com contrato de invocação pinado como dado da SPEC (Piper v1.2.0, argv array, uma linha JSON por utterance via stdin, `output_file` sempre gerado no main process); `speech-output.ts` ganha `resolveVoiceBackend` (cadeia de fallback: voz preferida da Persona → default Piper → 1ª voz local do SO → mudo) e a Web Speech API das SPECs 0035/0036 segue intacta como **fallback fail-closed** — sem Piper instalado, nada regride. `Persona.voiceURI?` (ADR-0020) passa a aceitar também `piper:<id>`, sem mudança de schema. Diff confinado a `apps/desktop` (zero alteração em `packages/*`/`apps/cli`/`core-bridge.ts`); desde a SPEC-0041, a pedido explícito do usuário (que comparou as vozes e preferiu Piper), a **superfície de escolha e de uso** de voz passa a ser exclusivamente Piper quando ele está de fato disponível (`PiperTts.isAvailable()`, checado via canal IPC novo `'atlas:tts:available'`, não apenas modelos em disco): o `<select>` do formulário de Persona deixa de listar vozes do SO e uma `Persona.voiceURI` de SO já persistida deixa de ser honrada (reverte deliberadamente a precedência da SPEC-0040/D8, com aviso visível no formulário), mas a Web Speech API das SPECs 0035/0036 **nunca é removida** — segue como rede de segurança interna e invisível (ADR-0021(c) intacto); sem Piper disponível, a app volta ao modo degradado com as vozes do SO; desde a SPEC-0043, fecha os dois resíduos registrados nas Observações da SPEC-0041: uma `Persona.voiceURI` persistida não-ofertável **por razão ambiental** (Piper indisponível, modelo removido, voz do SO desinstalada) aparece no `<select>` como uma `<option>` **retida**, já selecionada e rotulada, em vez de apagada em silêncio no submit (`resolvePersistedVoiceSelection`, quatro desfechos exaustivos, composta sobre a mesma política Piper-only da SPEC-0041, que sai intacta); e `createSpeechOutputGlue` do renderer passa a receber `preferredVoiceURI`, restaurando ADR-0020(b) no caminho `'os'` — em modo degradado a voz do SO escolhida numa Persona finalmente soa, deriva aberta desde a SPEC-0035/0039. desde a SPEC-0046, consumindo o [ADR-0022](docs/06-adr/ADR-0022-whisper-cpp-local-stt-engine.md) (novo, Accepted), o desktop entrega também a **entrada de voz (STT)** por push-to-talk — `apps/desktop/src/stt-engine.ts` (novo) invoca `whisper.cpp` (`whisper-cli` v1.7.6, modelo `ggml-small-q5_1.bin` empacotado, PT-BR fixo) por subprocesso a cada transcrição, sem processo residente (diverge deliberadamente do Piper); captura no renderer via `ScriptProcessorNode`+`GainNode(0)` (sem `AudioWorkletNode`/CSP nova); permissão de microfone decidida no main, fail-closed, via `src/media-permission.ts` (novo) com janela de captura explícita e watchdog; transcrição sempre anexada ao campo de entrada, nunca enviada automaticamente **fora do modo hands-free** (ver SPEC-0052 abaixo); nada de áudio/transcrição persistido; zero diff em `packages/*`/`apps/cli`/`core-bridge.ts`/`speech-output.ts`/`piper-tts.ts`. Wake word segue fora de escopo (ADR-0022, candidato futuro com ADR próprio) — fecha o item 2.3 do Roadmap por inteiro; a CLI equivalente de Persona foi entregue pela SPEC-0044. Desde a SPEC-0052, consumindo o [ADR-0023](docs/06-adr/ADR-0023-hands-free-voice-conversation.md) (novo, Accepted, que **supersede parcialmente** o ADR-0022 — só a cláusula de auto-envio de *Alternativas Consideradas*, só dentro do modo hands-free), o desktop entrega também a **conversa por voz contínua (modo hands-free)** — um toggle único abre o microfone e o mantém aberto entre turnos; o fim de fala é detectado pelo **Silero VAD** (`onnxruntime-web`, WASM) rodando no **renderer** (primeira inferência do projeto fora do main process, exceção explícita ao ADR-0022(a)/ADR-0023(d)) após 3 s contínuos de ausência de voz; a transcrição vai **direto ao Core, sem revisão** pelo mesmo `submit` de `#chat-form` dos demais gestos; a resposta é falada; o microfone fica comprovadamente fechado durante o processamento e a fala (`apps/desktop/src/hands-free.ts`/`vad-resources.ts`, novos, máquina de estados de 9 estados × 18 eventos); o `ConfirmPort` (ADR-0013) permanece intocado — o que sustenta a distinção "auto-envio ≠ execução não autorizada" do Artigo 8; sem barge-in; zero módulo/Tool/campo novo em `Persona`, zero diff em `packages/*`/`@atlas/contracts`. Desde a SPEC-0053 (v3.0, substitui integralmente as direções v1.x/v2.0 reprovadas em smoke humano), a interface ganha uma reformulação visual completa: o núcleo é uma esfera holográfica volumétrica de partículas em **Canvas 2D nativo** (nuvem determinística de 400 pontos, sete perfis de estado ligados a sinais reais de voz), sidebar/trilho e timeline permanentes dão lugar a um **drawer overlay** sob demanda (`#menu-toggle`, seis áreas: Persona/Personas/Memória/Permissões/Objetivo/Sessão), Sessão absorve a timeline inteira, progressive disclosure evita sobreposição/corte, e a paleta é roxo-realeza sem emoji; zero IPC/contrato/dependência nova, diff confinado ao renderer. Desde a SPEC-0054, consumindo o [ADR-0024](docs/06-adr/ADR-0024-desktop-host-resource-metrics.md) e o [ADR-0025](docs/06-adr/ADR-0025-desktop-token-usage-accounting.md) (ambos novos, Accepted), o drawer ganha um sétimo e último painel, `Sistema` — **observabilidade do ambiente** (requisito acrescentado ao PRD em 2026-08-18): mostra consumo de CPU/memória/GPU/rede do host (`src/system-metrics.ts`, novo, lê via `systeminformation` recebida por porta injetável, fail-closed por métrica), consumo de tokens da sessão corrente (`src/token-usage.ts`, novo, acumulador em memória resetado por sessão; `@atlas/contracts` ganha `TokenUsage`/`GenerateResult.usage?`/`AskResult.usage?`/`ConversationTurn.usage?`, aditivos, preenchidos pelos três providers do `@atlas/model-gateway` e somados por turno no `@atlas/cognitive`) e relógio com data/dia da semana/hora; dois canais IPC novos (`'atlas:metrics:read'`/`'atlas:tokens:read'`), atualizados a cada 2 s por um único timer enquanto o painel está visível; zero módulo/Tool/Persona novo, layout v3.0 intacto. `pnpm --filter @atlas/desktop start` abre a janela. O Core vive só no main process (`src/core-bridge.ts` testável sem Electron + `src/main.ts` casca fina); o renderer fala com o main só por IPC (`contextIsolation: true`, `nodeIntegration: false`). Sem `dist/`: main process via `NODE_OPTIONS=--import=tsx electron ./src/main.ts` (extensão do ADR-0005 ao Electron).
- **Infra**: repo privado `nahoL13/Atlas` (single-branch `main`), CI em `.github/workflows/ci.yml` (lint/typecheck/test/format:check).
- Antes de mexer em qualquer módulo: `docs/03-architecture/ModuleCatalog.md`; APIs e limitações exatas: `PLATFORM_STATE.md` e os ADRs.

**Nova sessão ou retomada de trabalho: leia `docs/05-context/NEXT_CONTEXT.md` antes de qualquer coisa.**

**Disciplina de contexto (2026-07-30):** os documentos lidos no arranque de toda sessão e de todo subagent têm teto de tamanho, porque são relidos a cada requisição do agente e dominavam o custo em tokens do pipeline. `NEXT_CONTEXT.md` (~8 KB) responde só "o que fazer agora"; `LESSONS_LEARNED.md` (~25 KB) mantém as últimas 5 SPECs mais um índice de padrões recorrentes; `apps/desktop/CLAUDE.md` (~15 KB) descreve o estado atual do código, não o histórico. O conteúdo anterior está preservado sem edição em `NEXT_CONTEXT-ARCHIVE.md`, `LESSONS_LEARNED-ARCHIVE.md` e `apps/desktop/CLAUDE-ARCHIVE.md` — **arquivos de consulta sob demanda, nunca de arranque**. Ao fechar uma SPEC, o `spec-closer` acrescenta ao vivo e move o excedente para o arquivo; não deixe os vivos crescerem sem poda. Na mesma linha, a **SPEC-0042** escopou a *verificação*, não só a leitura: `apps/desktop/tests/core-bridge.test.ts` (1.432 linhas) virou 7 arquivos por assunto + helper, os 13 packages/apps com testes ganharam `"test": "vitest run"` (`pnpm --filter <pkg> test`/`typecheck`, ver "Verificação escopada" em `ClaudeCodeAutomation.md`). A **SPEC-0043** fechou os dois resíduos de voz da SPEC-0041 (`voiceURI` persistida não-ofertável por razão ambiental preservada como `<option>` retida; `createSpeechOutputGlue` do renderer restaurando ADR-0020(b) no caminho `'os'`). A **SPEC-0044** entregou o CRUD de Persona pela CLI (`atlas persona list|show|create|edit|delete`), 2º consumidor real do re-export de catálogo (SPEC-0037/D4). A **SPEC-0045** cobriu `apps/desktop/src/renderer/renderer.js` por teste automatizado sem tocar a stack do renderer — `apps/desktop/tests/helpers/renderer-harness.ts` carrega `index.html`/`renderer.js` do disco num `jsdom` instanciado programaticamente, e um gate mecânico novo (`renderer.speech-parity.test.ts`) falha se `speech-output.ts` ganhar um export não classificado como replicado no renderer, fechando a lacuna registrada no gate da SPEC-0043; zero diff em `apps/desktop/src/`/`packages/*`, ADR-0019 intacto; `jsdom` como `devDependency` só de `apps/desktop`. A **SPEC-0046** entregou a entrada de voz (STT) do desktop — [ADR-0022](docs/06-adr/ADR-0022-whisper-cpp-local-stt-engine.md) (novo, Accepted, `whisper.cpp`), `apps/desktop/src/stt-engine.ts`/`media-permission.ts` (novos), fechando o item 2.3 do Roadmap (wake word segue candidato não comprometido, ADR próprio). A **SPEC-0047** generalizou o gate mecânico de paridade renderer↔módulo (SPEC-0045) para vigiar três módulos-fonte (`speech-output.ts`, `piper-tts.ts`, `stt-engine.ts`, enumerados em runtime via `import * as`) em vez de só `speech-output.ts`, amarrando a 10ª réplica hoje sem cobertura (`resolveDefaultPiperVoiceURI`/`piper-tts.ts` ↔ `computeDefaultPiperVoiceURI`/`renderer.js`) e entregando cobertura comportamental dos cinco painéis do desktop (Persona, permissões, memória, `ask`, serialização de gestos) sobre o harness da SPEC-0045, em quatro arquivos de teste novos; zero diff em `apps/desktop/src/`/`packages/*`, ADR-0019/0021/0022 intactos. A **SPEC-0052** entregou o modo hands-free (conversa por voz contínua) do desktop, consumindo o [ADR-0023](docs/06-adr/ADR-0023-hands-free-voice-conversation.md) (novo, Accepted, supersede parcial do ADR-0022 — só a cláusula de auto-envio, só dentro do modo); o gate mecânico de paridade renderer↔módulo passa a vigiar **quatro** módulos-fonte (soma `hands-free.ts`). A **SPEC-0053** (v3.0) redirecionou integralmente o layout visual do desktop depois de a v2.0 ter passado por inteiro nos quatro gates técnicos e ainda assim reprovado no smoke visual humano (sidebar/trilho/timeline permanentes, Memória sobrepondo conteúdo, esfera CSS-only sem volume) — pela primeira vez em ~20 fatias visuais consecutivas o smoke humano rodou de fato e confirmou uma implementação (15/15 `OK`): núcleo holográfico volumétrico em Canvas 2D (400 pontos determinísticos, digest SHA-256 pinado), navegação por drawer overlay com seis painéis, Sessão absorvendo a timeline por inteiro, progressive disclosure e paleta roxo-realeza sem emoji; zero IPC/contrato/dependência nova. A **SPEC-0054** entregou o painel `Sistema` (observabilidade do ambiente: recursos do host, tokens da sessão, relógio) — sétimo e último item do drawer v3.0, dois módulos novos no main process (`system-metrics.ts`/`token-usage.ts`, portas injetáveis, fail-closed por métrica), dois canais IPC (`'atlas:metrics:read'`/`'atlas:tokens:read'`) e `TokenUsage` aditivo em `@atlas/contracts`/`@atlas/model-gateway`/`@atlas/cognitive`, consumindo o [ADR-0024](docs/06-adr/ADR-0024-desktop-host-resource-metrics.md)/[ADR-0025](docs/06-adr/ADR-0025-desktop-token-usage-accounting.md) (ambos novos, Accepted); o gate mecânico de paridade renderer↔módulo passa a vigiar **seis** módulos-fonte. A **SPEC-0055** entregou a primeira Tool de rede do Atlas, `http_get` (`@atlas/tools`), consumindo o [ADR-0026](docs/06-adr/ADR-0026-network-access-gate.md) (novo, Accepted, aberto por escalação do `spec-drafter` — host/URL não tem "raiz" a conter): `ResourceType`/`ResourceRef` de `@atlas/contracts` e a política `netRoots` do Permission Service, ambos descritos acima; CLI equivalente (`--allow-net`/`ATLAS_ALLOW_NET`) na mesma fatia; `apps/desktop` fica deliberadamente sem política de rede (fail-closed, só narrowing de tipo no `ConfirmPort`); não fecha o item 1.4 do Roadmap. Residuais registrados e não fechados: a URL é canal de saída de dados que o portão não julga (só o host, nunca path/query — primeiro caminho de exfiltração da plataforma) e a injeção indireta de prompt pelo corpo remoto (primeira vez que texto de terceiro não confiável entra no prompt); a assimetria com `@atlas/model-gateway` (egress sem `evaluate`/`netRoots` desde a SPEC-0004) fica documentada. A **SPEC-0056** entregou a segunda fatia do item 1.4 (leitura de estrutura de projeto), continuação incremental da SPEC-0028: Tool `project_info` (`@atlas/tools`) descreve a raiz de um projeto de software, os manifests reconhecidos (tabela fixa e pura de 16 nomes de arquivo → ecossistema) e os scripts declarados no `package.json`, reusando a descoberta de toplevel da SPEC-0028 por um método aditivo, `GitReadPort.toplevel(cwd)`, que delega à mesma `resolveRepository`/`verify` já existente (sem duplicar a barreira); as três rejeições passam a lançar `GitRootError` (novo, interno a `@atlas/tools`), classificadas por `reason` em vez de mensagem crua. A ascensão ao repositório git só ocorre quando `path` é omitido; `path` explícito é honrado como o alvo final sem ascender, o que permite descrever um subpacote de monorepo — e deixa a Tool atrás de **uma única barreira** (`evaluate`) nesse caminho, igual a `list_dir`. Sem repositório aplicável, a raiz degrada para o diretório-alvo, com `origem da raiz` sempre visível na saída (conjunto fechado de cinco strings, nunca interpolando path descoberto nem stderr do git). Zero diff em `@atlas/contracts`/`packages/permissions`/`packages/runtime`/`apps/cli`/`apps/desktop`; não fecha o item 1.4 do Roadmap — resta só a execução de comandos sob o Permission Service, `ADR primeiro`. A **SPEC-0058** endureceu a composição de saídas de Tools no prompt de `@atlas/cognitive` — bloco delimitado `<tool_output id="N">`, instrução fixa de conteúdo não confiável e teto de tamanho por passo (`packages/cognitive/src/tool-output.ts`, novo, não exportado pelo `index.ts`), mitigação genérica de injeção indireta de prompt aplicável às 13 Tools existentes e a qualquer Tool futura, sem ADR novo e sem uma linha em `@atlas/tools`; mitiga, não fecha, o residual do ADR-0026/SPEC-0055 (que segue aberto); o memo de continuidade `summarizeSteps` (persistido entre turnos) recebe só teto e neutralização, deliberadamente sem bloco/instrução (residual conhecido); não fecha item do Roadmap, desbloqueia a retomada da SPEC-0057 (`web_search`, `Ready`, pausada). A **SPEC-0057** entregou a terceira fatia do item 1.4 (Acesso à internet) — Tool `web_search` (`@atlas/tools`), busca em texto livre sobre uma porta injetável `SearchPort` que publica o próprio `endpointUrl` (fonte única do host declarado em `requirements` **e** do host de fato requisitado, D21, correção do veto da 1ª revisão que evita duas deps independentes divergirem), com adaptador default `searxngSearchPort` construído sobre o `HttpPort` já endurecido da SPEC-0055 (`nodeHttpPort` ganha `bodyLimitBytes?` aditivo, default 64 KiB inalterado; a busca usa 256 KiB, decidido só dentro do adaptador); provedor **sem credencial**, compatível com a API JSON do SearXNG, configurado por `--search-url`/`ATLAS_SEARCH_URL` (`AtlasConfig.tools.searchUrl`, namespace de módulo novo e obrigatório em `@atlas/contracts`); sem endpoint configurado, a Tool não é registrada no Tool Registry (`atlas status` mostra os dois estados); o host do provedor é julgado pelo mesmo portão `netRoots` do ADR-0026, sem nenhuma cláusula reaberta — diff vazio em `@atlas/permissions`/`@atlas/runtime`/`@atlas/cognitive`/`@atlas/skills`/`@atlas/memory`/`@atlas/context`/`@atlas/persona`/`@atlas/model-gateway`/`apps/desktop`. Não fecha o item 1.4 do Roadmap — resta só a execução de comandos sob o Permission Service (`ADR primeiro`). Residuais registrados e não fechados: a consulta é um canal de saída de dados que o portão não julga (2ª instância do residual 10 da SPEC-0055), injeção indireta de prompt amplificada por até 10 resultados de terceiros por busca (mitigada de forma genérica, não fechada, pela SPEC-0058), busca indisponível na GUI (D15). A suíte total é **1728 testes / 93 arquivos**. `spec-validator`/`spec-closer` continuam obrigados aos quatro comandos completos na raiz (`typecheck`/`lint`/`test`/`format:check`) antes de fechar qualquer SPEC.

## Comandos

- `pnpm install` — instala o workspace (Node ≥ 24, pnpm ≥ 11 via corepack)
- `pnpm lint` — ESLint (flat config, typescript-eslint)
- `pnpm format` / `pnpm format:check` — Prettier (arquivos `.md` são ignorados por design)
- `pnpm typecheck` — TypeScript sem emissão
- `pnpm test` — Vitest
- `pnpm build` — build recursivo (no-op até existirem packages)

Nota: `typescript` está pinado na série 5 até o typescript-eslint suportar o TS 7 (ver LESSONS_LEARNED).

## Invariantes (não negociáveis)

Destilados de `docs/00-project/ArchitectureConstitution.md` (15 artigos) — em conflito, a Constituição prevalece:

1. **A documentação é a fonte oficial da verdade.** Nenhuma decisão arquitetural existe apenas no código; mudanças estruturais são documentadas antes ou junto da implementação.
2. **Nenhuma implementação sem SPEC aprovada.** Nunca implemente a partir de pedido informal; uma SPEC por vez. Desde a Emenda v1.1 da Constituição (2026-07-20), a aprovação `Draft → Ready` é o gate do `architecture-reviewer` — não exige veto humano.
3. **A IA é colaboradora, não arquiteta.** Não crie módulos/Skills/Personas sem documentação correspondente, não mova responsabilidades. Respeite o Module Catalog. Emenda à Constituição, módulo novo e ADR novo continuam decisões humanas (escalação obrigatória da Emenda v1.1).
4. **O Core é o único orquestrador.** Módulos têm responsabilidade única e se comunicam apenas por contratos públicos, nunca por detalhes internos de outro módulo.
5. **Ferramentas são adaptadores**: interagem com recursos externos e não contêm lógica de negócio nem tomada de decisão.
6. **Memória tem autoridade exclusiva sobre estado persistente.** Contexto (estado da execução) e memória (conhecimento persistente) são conceitos distintos e separados.
7. **O usuário percebe uma única Persona.** Skills, planejadores e coordenação interna são invisíveis para ele.
8. **Em caso de dúvida ou inconsistência entre documentos, pare e registre** — nunca assuma comportamento não documentado.

Teste para qualquer decisão: *"isto torna o Atlas mais simples, mais modular, mais transparente e mais sustentável?"* Se não, reconsidere antes de incorporar.

<!-- ATLAS-SPEC-PIPELINE:START -->
## Pipeline autônomo de SPEC

Pedidos para criar, continuar, revisar, implementar, validar ou fechar uma SPEC devem carregar e seguir a skill `spec-pipeline`. O fio principal é somente despachante: não desenha nem implementa inline, repassa relatórios sem re-narrar e aplica apenas as transições de Status atribuídas ao despachante. Se um agente obrigatório não estiver disponível, pare e reporte; nunca absorva silenciosamente a fase.
<!-- ATLAS-SPEC-PIPELINE:END -->

## Fluxo de desenvolvimento

Ideia → existe no PRD? → existe módulo responsável? → SPEC → implementação → testes → atualização da documentação → review → merge. Nenhuma etapa é pulada.

- Processo completo, características de uma boa SPEC e diretrizes para IA: `docs/04-engineering/DevelopmentGuide.md`
- Estrutura obrigatória de toda SPEC: `docs/implementation/templates/SPEC-TEMPLATE.md`; SPECs vivem em `docs/implementation/specs/`
- Ao concluir uma SPEC: registre as lições aprendidas em `docs/implementation/LESSONS_LEARNED.md` — é parte da Definition of Done
- Antes de começar uma SPEC nova: consulte `docs/05-context/TOKEN_USAGE_LOG.md` para estimar o custo em tokens pela SPEC concluída mais parecida em tamanho, e decidir se cabe na sessão atual ou se é melhor esperar a próxima janela (regenere com `python3 scripts/claude-usage-report.py`)
- Conflito entre documentos: prevalece a ordem de prioridade definida em `docs/04-engineering/DevelopmentGuide.md` (PROJECT.md → Vision → Constitution → ... → SPEC).

## Mapa da documentação (leia sob demanda)

| Se a tarefa envolve... | Leia |
|---|---|
| Propósito, missão, o que o Atlas NÃO é | `docs/01-vision/Vision.md` |
| Regras permanentes; conflito entre solução técnica e arquitetura | `docs/00-project/ArchitectureConstitution.md` |
| Criar/remover módulos, Skills ou Personas; mudanças estruturais | `docs/00-project/ArchitectureDecisionProcess.md` |
| Significado preciso de termos (Persona, Skill, Tool, Planner, SPEC...) | `docs/00-project/Glossary.md` |
| Requisitos funcionais/não funcionais, escopo do MVP, fora de escopo | `docs/02-product/ProductRequirementsDocument.md` |
| As 7 etapas do ciclo cognitivo que a arquitetura implementa | `docs/03-architecture/CognitiveLifecycle.md` |
| O porquê do desenho; critérios para novos componentes | `docs/03-architecture/ArchitecturePrinciples.md` |
| Camadas (Platform, Intelligence, Execution, Support) e fluxo principal | `docs/03-architecture/SystemArchitecture.md` |
| Responsabilidades e limites de cada módulo — **obrigatório antes de mexer em qualquer módulo** | `docs/03-architecture/ModuleCatalog.md` |
| Onde cada tipo de arquivo deve ficar (apps/, packages/, tooling/...) | `docs/03-architecture/ProjectStructure.md` |
| Context Service (`packages/context`): detentor do estado temporário de conversa por sessão; quem medeia (app vs. Cognitive) | `docs/06-adr/ADR-0009-context-service-value-store.md` |
| Persona Service (`packages/persona`): identidade injetada na geração; por que o Cognitive não conhece o conceito de Persona | `docs/06-adr/ADR-0010-persona-injected-generation.md` |
| Memory Service (`packages/memory`): conhecimento persistente atrás de porta de storage injetável; memória injetada na geração; Memória × Contexto | `docs/06-adr/ADR-0011-memory-service-persistence.md` |
| Espinha de execução (`packages/planner` consolidado em `packages/cognitive`, `packages/runtime`, `packages/tools`): plano estruturado Planner-driven, Runtime executa Tools, Gateway intacto, passos independentes | `docs/06-adr/ADR-0012-planner-runtime-execution.md` |
| Permission Service (`packages/permissions`): portão puro/síncrono na execução; Tools declaram `requirements` como dado; Runtime aplica o veredicto; contenção lexical (symlink adiado); `confirm` reservado | `docs/06-adr/ADR-0013-permission-service-execution-gate.md` |
| Workspace multi-projeto (atlas-core, atlas-desktop...) e dependências entre projetos | `docs/03-architecture/WorkspaceStrategy.md` |
| Processo oficial de desenvolvimento e SPECs | `docs/04-engineering/DevelopmentGuide.md` |
| Escrever uma nova SPEC | `docs/implementation/templates/SPEC-TEMPLATE.md` |
| Concluir uma SPEC; consultar aprendizados recentes e padrões recorrentes | `docs/implementation/LESSONS_LEARNED.md` (últimas 5 SPECs; anteriores em `LESSONS_LEARNED-ARCHIVE.md`) |
| Subagents (`spec-drafter`/`spec-implementer`/`spec-validator`), skills, hooks e log de custo de token por SPEC no Claude Code | `docs/04-engineering/ClaudeCodeAutomation.md` |
| Macro-fases de longo prazo (Núcleo completo → Interface `apps/desktop` → Expansão), sem datas; qual fase uma ideia nova pertence | `docs/04-engineering/Roadmap.md` |
| Entry point para humanos; ordem de leitura em camadas | `PROJECT.md` |

Os termos do projeto têm significado técnico preciso — na dúvida sobre um termo, consulte o Glossary antes de inferir.

## Referenciado na documentação, mas ainda não criado

Para evitar buscas inúteis — os itens abaixo são citados pela documentação, porém **ainda não existem**:

- demais packages do catálogo conforme SPECs futuras

Quando um desses artefatos for criado, atualize esta seção (e remova-a quando esvaziar).
